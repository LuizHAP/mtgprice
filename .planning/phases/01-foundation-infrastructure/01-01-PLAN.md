---
phase: 01-foundation-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.js
  - biome.json
  - .gitignore
  - .env.example
  - .husky/pre-commit
autonomous: true
requirements: []
must_haves:
  truths:
    - "Next.js project runs in development mode"
    - "TypeScript compiles without errors"
    - "Biome lints and formats code"
    - "Vitest test runner executes"
    - "Project structure follows monorepo pattern"
  artifacts:
    - path: "package.json"
      provides: "Dependencies and scripts"
      contains: "next, react, typescript, drizzle-orm, grammy, ioredis"
    - path: "src/"
      provides: "Monorepo structure"
      contains: "api/, bot/, db/, lib/, types/, web/ directories"
    - path: "tsconfig.json"
      provides: "TypeScript configuration"
      contains: "strict mode enabled"
  key_links:
    - from: "package.json"
      to: "next.config.js"
      via: "Next.js configuration"
      pattern: "next.config"
    - from: "tsconfig.json"
      to: "src/"
      via: "Path aliases"
      pattern: "@/* alias"
---

<objective>
Initialize Next.js project with TypeScript, tooling, and monorepo structure.

Purpose: Establish the development foundation with proper tooling, directory structure, and configuration for the MTG Price Monitor project.

Output: Working Next.js project with TypeScript, Biome, Vitest, and monorepo directory structure ready for development.
</objective>

<execution_context>
@/Users/luizpansarini/.claude/get-shit-done/workflows/execute-plan.md
@Users/luizpansarini/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-infrastructure/01-CONTEXT.md
@.planning/phases/01-foundation-infrastructure/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js project with TypeScript</name>
  <files>package.json, tsconfig.json, next.config.js, .gitignore, .env.example</files>
  <action>
Initialize Next.js 15+ project with App Router:

1. Create package.json with dependencies from RESEARCH.md:
   - Core: next@latest, react@latest, react-dom@latest
   - TypeScript: typescript@5.9+, @types/react, @types/node
   - Database: drizzle-orm, postgres
   - Auth: jsonwebtoken, bcrypt
   - Telegram: grammy
   - Rate limiting: ioredis
   - Dev: drizzle-kit, @biomejs/biome, husky, lint-staged, vitest, @testing-library/react, winston, node-cron

2. Use pnpm as package manager (per CONTEXT.md decision)

3. Create tsconfig.json with:
   - Strict mode enabled
   - Path alias: "@/*" -> "./src/*"
   - Target ES2020+ for Node.js 20+ compatibility

4. Create next.config.js with:
   - No special config needed initially
   - Ready for future middleware/CORS configuration

5. Create .gitignore with:
   - node_modules/, .next/, dist/, coverage/
   - .env files (never commit secrets)
   - *.log files

6. Create .env.example with placeholder variables:
   - DATABASE_URL
   - REDIS_URL
   - JWT_SECRET
   - TELEGRAM_BOT_TOKEN
   - TELEGRAM_CHAT_ID
   - BOT_PASSWORD

7. Initialize pnpm workspace with lock file
  </action>
  <verify>
    <automated>pnpm install && pnpm next dev -- --version</automated>
  </verify>
  <done>Next.js project initializes, dependencies install, TypeScript compiles</done>
</task>

<task type="auto">
  <name>Task 2: Set up Biome, Husky, and package scripts</name>
  <files>biome.json, .husky/pre-commit, package.json</files>
  <action>
Configure development tooling:

1. Create biome.json with configuration from RESEARCH.md:
   - Enabled: formatter, linter, organizeImports
   - JavaScript: single quotes, semicolons asNeeded, trailing commas
   - Linter: recommended presets enabled
   - Ignore: node_modules, .next, coverage, dist

2. Initialize Husky with lint-staged:
   - Run: npx husky install
   - Create .husky/pre-commit hook
   - Configure lint-staged in package.json to run biome check --write on staged files

3. Add scripts to package.json:
   - "lint": "biome check ."
   - "lint:fix": "biome check --write ."
   - "dev": "next dev"
   - "build": "next build"

Note: Test scripts (test, test:coverage, test:ui) are added by Plan 01-00 (Wave 0). This task only creates Biome, Husky, and dev/build scripts. Verify Vitest configuration exists from Plan 01-00.
  </action>
  <verify>
    <automated>pnpm biome check . && test -f vitest.config.ts && test -f test/setup.ts</automated>
  </verify>
  <done>Biome lints, Husky pre-commit hook runs on git commit, Vitest config exists from 01-00</done>
</task>

<task type="auto">
  <name>Task 3: Create monorepo directory structure</name>
  <files>src/api, src/bot, src/db, src/lib, src/types, src/web, src/scraper</files>
  <action>
Create directory structure from CONTEXT.md decision:

1. Create src/ directory with subdirectories:
   - src/api/ - Next.js API routes (will be app/api/*)
   - src/bot/ - Telegram bot logic
   - src/scraper/ - Web scraping modules (for Phase 2)
   - src/db/ - Database client and migrations
   - src/lib/ - Shared utilities
   - src/types/ - Shared TypeScript types
   - src/web/ - Dashboard UI (will be app/*)

2. Create placeholder index.ts files in each directory to prevent "module not found" errors

3. Create src/lib/placeholder.ts with a comment describing what utilities will go here (logger, auth, rate-limiter, telegram)

4. Verify structure matches CONTEXT.md lines 108-119

Note: This creates the monorepo foundation. Actual implementation will happen in subsequent plans.
  </action>
  <verify>
    <automated>ls -la src/ && test -d src/api && test -d src/bot && test -d src/db && test -d src/lib && test -d src/types && test -d src/web && test -d src/scraper</automated>
  </verify>
  <done>All directories exist with placeholder files, structure matches CONTEXT.md specification</done>
</task>

</tasks>

<verification>
After completing all tasks, verify:
1. [ ] `pnpm install` succeeds without errors
2. [ ] `pnpm dev` starts Next.js dev server
3. [ ] `pnpm biome check .` passes (no linting errors)
4. [ ] `pnpm test` runs Vitest (even with no tests yet)
5. [ ] Git commit triggers Husky pre-commit hook
6. [ ] All src/ subdirectories exist with placeholder files
</verification>

<success_criteria>
1. Next.js 15+ project initialized with TypeScript strict mode
2. All required dependencies installed (Next.js, React, Drizzle, grammY, ioredis, Biome, Vitest)
3. Development tooling configured (Biome linting, Husky hooks, Vitest testing)
4. Monorepo directory structure created following CONTEXT.md decisions
5. Project can be started with `pnpm dev` and accessed at localhost:3000
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-infrastructure/01-01-SUMMARY.md`
</output>
