---
phase: 01-foundation-infrastructure
plan: 03
type: tdd
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/lib/rate-limiter.ts
  - src/middleware/rate-limit.ts
  - src/lib/rate-limiter.test.ts
  - test/mocks/redis.ts
autonomous: true
requirements:
  - PRICE-06
must_haves:
  truths:
    - "Token bucket algorithm enforces rate limits"
    - "Redis stores rate limit state atomically using Lua scripts"
    - "Rate limiter prevents more than N requests per interval"
    - "Rate limiter refills tokens after interval expires"
    - "Middleware applies rate limiting to external API calls"
  artifacts:
    - path: "src/lib/rate-limiter.ts"
      provides: "Token bucket rate limiting"
      exports: ["checkRateLimit", "RateLimitResult"]
    - path: "src/middleware/rate-limit.ts"
      provides: "Rate limiting middleware for API routes"
      exports: ["rateLimitMiddleware"]
    - path: "src/lib/rate-limiter.test.ts"
      provides: "Rate limiter unit tests"
      contains: "tests for token bucket algorithm"
  key_links:
    - from: "src/lib/rate-limiter.ts"
      to: "Redis"
      via: "ioredis client"
      pattern: "redis.eval"
    - from: "src/middleware/rate-limit.ts"
      to: "src/lib/rate-limiter.ts"
      via: "Import"
      pattern: "checkRateLimit"
    - from: "Lua script"
      to: "Redis"
      via: "EVAL command"
      pattern: "redis.call"
---

<objective>
Implement Redis-backed token bucket rate limiting to prevent external API blocks.

Purpose: Enforce rate limits for external APIs (Scryfall: 10 req/sec, Telegram: 100 req/60sec) using atomic Redis operations with token bucket algorithm.

Output: Rate limiter utility with Lua script for atomic operations, middleware for API routes, and comprehensive tests.
</objective>

<execution_context>
@/Users/luizpansarini/.claude/get-shit-done/workflows/execute-plan.md
@Users/luizpansarini/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-infrastructure/01-CONTEXT.md
@.planning/phases/01-foundation-infrastructure/01-RESEARCH.md
</context>

<feature>
  <name>Redis-backed token bucket rate limiting</name>
  <files>src/lib/rate-limiter.ts, src/lib/rate-limiter.test.ts</files>
  <behavior>
    Given a rate limit key (e.g., "scryfall"), limit (10), interval (1 second), and tokens to consume (1):
    - If bucket has enough tokens: consume tokens, return { allowed: true, remaining: N }
    - If bucket doesn't have enough tokens: don't consume, return { allowed: false, remaining: 0 }
    - Tokens refill to limit after interval elapses
    - State persists in Redis with TTL = interval
    - Lua script ensures atomic operation (no race conditions)

    Edge cases:
    - First request: bucket starts full (remaining = limit)
    - Exhausted bucket: all requests rejected until refill
    - Concurrent requests: Lua script prevents race conditions
    - Expired keys: bucket refills to limit after interval
  </behavior>
  <implementation>
    1. Create Redis client using ioredis
    2. Implement checkRateLimit function with Lua script from RESEARCH.md
    3. Add rate limit presets for Scryfall (10 req/sec) and Telegram (100 req/60sec)
    4. Create middleware wrapper for API routes
    5. Add comprehensive unit tests with Redis mock
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write rate limiter tests (RED phase)</name>
  <files>src/lib/rate-limiter.test.ts, test/mocks/redis.ts</files>
  <behavior>
    Test suite for token bucket rate limiter:

    Test 1: First request should succeed with full bucket
    - Given: limit=10, interval=1, tokens=1
    - When: First call to checkRateLimit
    - Then: returns { allowed: true, remaining: 9 }

    Test 2: Exhausted bucket should reject requests
    - Given: limit=10, interval=1
    - When: 11 requests made
    - Then: First 10 return allowed: true, 11th returns allowed: false

    Test 3: Bucket should refill after interval
    - Given: limit=10, interval=0.1 (100ms for testing)
    - When: Exhaust bucket, wait 150ms, request again
    - Then: New request returns allowed: true with remaining=9

    Test 4: Concurrent requests should be handled atomically
    - Given: limit=10, interval=1
    - When: 10 concurrent requests
    - Then: All 10 return allowed: true (no race conditions)

    Test 5: Redis key should expire after interval
    - Given: limit=10, interval=0.1
    - When: Make request, wait 200ms, check Redis
    - Then: Key no longer exists (TTL expired)

    Test 6: Scryfall preset should enforce 10 req/sec
    - Given: Scryfall preset (10 req/sec)
    - When: 11 requests in 1 second
    - Then: 10 succeed, 1 fails

    Use ioredis-mock or create a mock Redis client for testing.
  </behavior>
  <action>
Create test file with all test cases. Use Vitest test framework. Create Redis mock if ioredis-mock doesn't support Lua scripts (many mocks don't).

Mock Redis should support:
- eval method (for Lua scripts)
- return value format matching ioredis
  </action>
  <verify>
    <automated>pnpm test src/lib/rate-limiter.test.ts</automated>
  </verify>
  <done>Test file created, all tests fail (RED), Redis mock works</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement token bucket rate limiter (GREEN phase)</name>
  <files>src/lib/rate-limiter.ts</files>
  <behavior>
    After implementation, all tests from Task 1 should pass:

    - Token bucket algorithm works correctly
    - Redis Lua script executes atomically
    - Rate limits are enforced properly
    - Bucket refills after interval
    - Presets work for Scryfall and Telegram
  </behavior>
  <action>
Implement the rate limiter:

1. Create src/lib/rate-limiter.ts
2. Import Redis from ioredis
3. Create Redis client instance
4. Implement checkRateLimit function:
   - Accept: key (string), limit (number), interval (number), tokens (number)
   - Use Lua script from RESEARCH.md (lines 243-296)
   - Return: { allowed: boolean, remaining: number }

5. Add rate limit presets:
   - RATE_LIMITS.SCRYFALL: { limit: 10, interval: 1 }
   - RATE_LIMITS.TELEGRAM: { limit: 100, interval: 60 }
   - RATE_LIMITS.TCGPLAYER: { limit: 50, interval: 60 } (example, adjust per API docs)

6. Export checkRateLimit and RATE_LIMITS

Use exact Lua script from RESEARCH.md. It handles token refill, consumption, and expiration in a single atomic operation.
  </action>
  <verify>
    <automated>pnpm test src/lib/rate-limiter.test.ts</automated>
  </verify>
  <done>All tests pass (GREEN), rate limiter enforces limits correctly</done>
</task>

<task type="auto">
  <name>Task 3: Create rate limiting middleware</name>
  <files>src/middleware/rate-limit.ts, src/lib/redis.ts</files>
  <action>
Create middleware and Redis client singleton:

1. Create src/lib/redis.ts:
   - Export singleton Redis client instance
   - Initialize from process.env.REDIS_URL
   - Handle connection errors gracefully
   - Export getClient() function

2. Create src/middleware/rate-limit.ts:
   - Import checkRateLimit from src/lib/rate-limiter
   - Create rateLimitMiddleware factory function
   - Accept options: key, limit, interval
   - Return Next.js middleware function
   - On rate limit exceeded: return 429 Too Many Requests with Retry-After header
   - On allowed: call next() to continue

3. Export middleware for use in API routes

Middleware pattern:
```typescript
export function createRateLimitMiddleware(options: { key: string; limit: number; interval: number }) {
  return async (req: NextRequest) => {
    const { allowed, remaining } = await checkRateLimit(options.key, options.limit, options.interval)

    if (!allowed) {
      return NextResponse.json({ error: 'Rate limit exceeded' }, { status: 429 })
    }

    return NextResponse.next()
  }
}
```
  </action>
  <verify>
  <automated>grep -q "createRateLimitMiddleware" src/middleware/rate-limit.ts && grep -q "export.*getClient" src/lib/redis.ts</automated>
  </verify>
  <done>Middleware factory exists, Redis client singleton exists, ready to use in API routes</done>
</task>

</tasks>

<verification>
After completing all tasks, verify:
1. [ ] All rate limiter tests pass (pnpm test src/lib/rate-limiter.test.ts)
2. [ ] Token bucket algorithm enforces limits correctly
3. [ ] Lua script executes atomically in Redis
4. [ ] Rate limit presets defined for Scryfall and Telegram
5. [ ] Middleware factory creates rate limit middleware
6. [ ] Redis client singleton exported for reuse
7. [ ] Code matches RESEARCH.md patterns (lines 243-296 for Lua script)
</verification>

<success_criteria>
1. Token bucket rate limiter implemented with Redis + Lua scripts
2. Rate limits enforced atomically (no race conditions)
3. Presets defined for Scryfall (10 req/sec) and Telegram (100 req/60sec)
4. Middleware factory creates Next.js middleware for API routes
5. All tests pass with ≥80% coverage
6. Ready to integrate with external API calls in Phase 2
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-infrastructure/01-03-SUMMARY.md`
</output>
