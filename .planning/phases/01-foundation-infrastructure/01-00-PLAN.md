---
phase: 01-foundation-infrastructure
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - vitest.config.ts
  - test/setup.ts
  - src/db/__tests__/schema.test.ts
  - src/db/__tests__/migrations.test.ts
  - src/db/__tests__/hypertables.test.ts
  - src/lib/auth/__tests__/hash.test.ts
  - src/lib/auth/__tests__/jwt.test.ts
  - src/api/auth/__tests__/login.test.ts
  - src/bot/__tests__/telegram.test.ts
  - src/lib/ratelimit/__tests__/token-bucket.test.ts
  - src/lib/ratelimit/__tests__/redis.test.ts
  - src/web/__tests__/middleware.test.ts
  - package.json
autonomous: true
requirements: []
must_haves:
  truths:
    - "Vitest test framework is configured and can execute tests"
    - "All test stub files exist for phase 1 implementation"
    - "Test setup file provides global test utilities"
    - "Test runner can be invoked via pnpm test command"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Vitest configuration"
      contains: "test environment, setup file, path aliases"
    - path: "test/setup.ts"
      provides: "Global test configuration"
      contains: "testing-library setup, global test utilities"
    - path: "src/db/__tests__/schema.test.ts"
      provides: "Database schema test stubs"
      contains: "test placeholders for PRICE-06"
    - path: "src/lib/auth/__tests__/hash.test.ts"
      provides: "Password hashing test stubs"
      contains: "test placeholders for AUTH-01"
    - path: "src/lib/auth/__tests__/jwt.test.ts"
      provides: "JWT test stubs"
      contains: "test placeholders for AUTH-01"
    - path: "src/api/auth/__tests__/login.test.ts"
      provides: "Login endpoint test stubs"
      contains: "test placeholders for AUTH-02"
    - path: "src/bot/__tests__/telegram.test.ts"
      provides: "Telegram bot test stubs"
      contains: "test placeholders for AUTH-01"
    - path: "src/lib/ratelimit/__tests__/token-bucket.test.ts"
      provides: "Rate limiter algorithm test stubs"
      contains: "test placeholders for PRICE-08"
    - path: "src/lib/ratelimit/__tests__/redis.test.ts"
      provides: "Redis rate limiter test stubs"
      contains: "test placeholders for PRICE-08"
    - path: "src/web/__tests__/middleware.test.ts"
      provides: "Middleware test stubs"
      contains: "test placeholders for AUTH-02"
  key_links:
    - from: "vitest.config.ts"
      to: "test/setup.ts"
      via: "setupFiles configuration"
      pattern: "setupFiles.*setup.ts"
    - from: "test/setup.ts"
      to: "all test files"
      via: "global imports"
      pattern: "import.*@testing-library"
---

<objective>
Generate Vitest configuration and test stub files for all Phase 1 implementation tasks.

Purpose: Establish test infrastructure BEFORE implementation begins (Nyquist compliance). This creates the test skeleton that implementation plans will fill with real tests in TDD RED→GREEN→REFACTOR cycles.

Output: Working Vitest configuration, test setup file, and 11 test stub files matching VALIDATION.md requirements.
</objective>

<execution_context>
@/Users/luizpansarini/.claude/get-shit-done/workflows/execute-plan.md
@Users/luizpansarini/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-infrastructure/01-CONTEXT.md
@.planning/phases/01-foundation-infrastructure/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Vitest configuration</name>
  <files>vitest.config.ts, test/setup.ts</files>
  <action>
  Create Vitest configuration for the project:

  1. Create vitest.config.ts:
     - Environment: 'jsdom' (for React components and DOM testing)
     - Globals: true (describe, it, expect available globally)
     - Setup file: './test/setup.ts'
     - Path alias: '@' -> './src' (matches tsconfig.json)
     - Coverage: configure with c8 or v8 provider
     - Test match pattern: **/*.test.ts, **/*.test.tsx
     - Exclude: node_modules, .next, dist

  2. Create test/setup.ts:
     - Import @testing-library/jest-dom for custom matchers (toBeInTheDocument, etc.)
     - Import @testing-library/react for render utilities
     - Set up global test configuration
     - Mock console methods in test environment if needed

  Use exact configuration from RESEARCH.md recommendations for Vitest.

  This enables all subsequent test files to use Testing Library utilities.
  </action>
  <verify>
    <automated>pnpm vitest --run --version</automated>
  </verify>
  <done>Vitest configuration exists, test setup file provides globals, vitest command executes</done>
</task>

<task type="auto">
  <name>Task 2: Create database test stubs (PRICE-06)</name>
  <files>src/db/__tests__/schema.test.ts, src/db/__tests__/migrations.test.ts, src/db/__tests__/hypertables.test.ts</files>
  <action>
  Create test stub files for database schema and migrations:

  1. Create src/db/__tests__/schema.test.ts:
     - Placeholder describe block: "Database schema validation"
     - Test stub: "should define users table with correct columns"
     - Test stub: "should define cards table with oracle_id unique"
     - Test stub: "should define prices table with time-series columns"
     - Test stub: "should define wishlists table with foreign keys"
     - Add TODO comment: "Implement in Plan 01-02 after schema created"

  2. Create src/db/__tests__/migrations.test.ts:
     - Placeholder describe block: "Database migrations"
     - Test stub: "should generate migration SQL from schema"
     - Test stub: "should apply migrations without errors"
     - Test stub: "should rollback migrations safely"
     - Add TODO comment: "Implement in Plan 01-02 after migration setup"

  3. Create src/db/__tests__/hypertables.test.ts:
     - Placeholder describe block: "TimescaleDB hypertables"
     - Test stub: "should convert prices table to hypertable"
     - Test stub: "should create composite index on (card_id, timestamp DESC)"
     - Test stub: "should query time-series data efficiently"
     - Add TODO comment: "Implement in Plan 01-02 after TimescaleDB setup"

  All test stubs should use `it.todo()` or `describe.skip()` to clearly indicate they are not yet implemented.

  These stubs ensure VALIDATION.md requirements are satisfied (nyquist_compliant: true).
  </action>
  <verify>
    <automated>grep -l "should define users table" src/db/__tests__/*.test.ts | wc -l | grep -q 3</automated>
  </verify>
  <done>Three database test stub files exist with placeholder tests marked as TODO</done>
</task>

<task type="auto">
  <name>Task 3: Create auth test stubs (AUTH-01, AUTH-02)</name>
  <files>src/lib/auth/__tests__/hash.test.ts, src/lib/auth/__tests__/jwt.test.ts, src/api/auth/__tests__/login.test.ts, src/bot/__tests__/telegram.test.ts</files>
  <action>
  Create test stub files for authentication system:

  1. Create src/lib/auth/__tests__/hash.test.ts (AUTH-01):
     - Placeholder describe block: "Password hashing with bcrypt"
     - Test stub: "should hash password with 10 salt rounds"
     - Test stub: "should compare correct password successfully"
     - Test stub: "should reject incorrect password"
     - Add TODO: "Implement in Plan 01-04 TDD cycle"

  2. Create src/lib/auth/__tests__/jwt.test.ts (AUTH-01):
     - Placeholder describe block: "JWT token generation and verification"
     - Test stub: "should sign token with user payload"
     - Test stub: "should verify valid token"
     - Test stub: "should reject invalid token"
     - Test stub: "should reject expired token"
     - Add TODO: "Implement in Plan 01-04 TDD cycle"

  3. Create src/api/auth/__tests__/login.test.ts (AUTH-02):
     - Placeholder describe block: "Login API endpoint"
     - Test stub: "should return 401 for invalid credentials"
     - Test stub: "should return 200 with user object for valid credentials"
     - Test stub: "should set httpOnly cookie with JWT token"
     - Test stub: "should handle database errors gracefully"
     - Add TODO: "Implement in Plan 01-04 after endpoint created"

  4. Create src/bot/__tests__/telegram.test.ts (AUTH-01):
     - Placeholder describe block: "Telegram bot authentication"
     - Test stub: "should reject commands from non-whitelisted chat IDs"
     - Test stub: "should accept /start command with correct password"
     - Test stub: "should link Telegram chat ID to user account"
     - Test stub: "should reject /start with wrong password"
     - Add TODO: "Implement in Plan 01-05 after bot created"

  Use `it.todo()` or `describe.skip()` to mark unimplemented tests.

  This satisfies VALIDATION.md test stub requirements for auth features.
  </action>
  <verify>
    <automated>grep -l "should hash password\|should sign token\|should return 200\|should reject commands" src/lib/auth/__tests__/*.test.ts src/api/auth/__tests__/*.test.ts src/bot/__tests__/*.test.ts | wc -l | grep -q 4</automated>
  </verify>
  <done>Four auth test stub files exist with placeholder tests marked as TODO</done>
</task>

<task type="auto">
  <name>Task 4: Create rate limiter test stubs (PRICE-08)</name>
  <files>src/lib/ratelimit/__tests__/token-bucket.test.ts, src/lib/ratelimit/__tests__/redis.test.ts</files>
  <action>
  Create test stub files for rate limiting system:

  1. Create src/lib/ratelimit/__tests__/token-bucket.test.ts (PRICE-08):
     - Placeholder describe block: "Token bucket rate limiting algorithm"
     - Test stub: "should allow requests within limit"
     - Test stub: "should reject requests exceeding limit"
     - Test stub: "should refill tokens after interval"
     - Test stub: "should handle concurrent requests atomically"
     - Test stub: "should expire keys after interval"
     - Test stub: "should enforce Scryfall preset (10 req/sec)"
     - Add TODO: "Implement in Plan 01-03 TDD cycle"

  2. Create src/lib/ratelimit/__tests__/redis.test.ts (PRICE-08):
     - Placeholder describe block: "Redis-backed rate limiter"
     - Test stub: "should store rate limit state in Redis"
     - Test stub: "should execute Lua script atomically"
     - Test stub: "should handle Redis connection errors"
     - Test stub: "should persist state across server restarts"
     - Add TODO: "Implement in Plan 01-03 after Redis client setup"

  Use `it.todo()` or `describe.skip()` to mark unimplemented tests.

  This satisfies VALIDATION.md test stub requirements for rate limiting.
  </action>
  <verify>
    <automated>grep -l "should allow requests\|should store rate limit state" src/lib/ratelimit/__tests__/*.test.ts | wc -l | grep -q 2</automated>
  </verify>
  <done>Two rate limiter test stub files exist with placeholder tests marked as TODO</done>
</task>

<task type="auto">
  <name>Task 5: Create middleware test stubs (AUTH-02)</name>
  <files>src/web/__tests__/middleware.test.ts</files>
  <action>
  Create test stub file for Next.js middleware:

  1. Create src/web/__tests__/middleware.test.ts (AUTH-02):
     - Placeholder describe block: "Next.js authentication middleware"
     - Test stub: "should allow access to protected routes with valid token"
     - Test stub: "should redirect to login without token"
     - Test stub: "should redirect to login with invalid token"
     - Test stub: "should exclude public routes from protection"
     - Test stub: "should clear invalid token and redirect"
     - Add TODO: "Implement in Plan 01-04 after middleware created"

  Use `it.todo()` or `describe.skip()` to mark unimplemented tests.

  This completes the VALIDATION.md test stub requirements.
  </action>
  <verify>
    <automated>grep -l "should allow access\|should redirect to login" src/web/__tests__/middleware.test.ts</automated>
  </verify>
  <done>Middleware test stub file exists with placeholder tests marked as TODO</done>
</task>

<task type="auto">
  <name>Task 6: Update package.json with test scripts</name>
  <files>package.json</files>
  <action>
  Add test scripts to package.json:

  1. Ensure vitest and testing dependencies are in devDependencies:
     - vitest
     - @vitest/ui
     - @testing-library/react
     - @testing-library/jest-dom
     - jsdom (environment)

  2. Add scripts to package.json:
     - "test": "vitest"
     - "test:run": "vitest --run"
     - "test:coverage": "vitest --coverage"
     - "test:ui": "vitest --ui"

  3. Ensure these scripts don't conflict with existing scripts from Plan 01-01

  This enables running tests via `pnpm test` command.
  </action>
  <verify>
    <automated>grep -q '"test".*vitest' package.json && pnpm vitest --run --version</automated>
  </verify>
  <done>Package.json has test scripts, vitest command executes successfully</done>
</task>

</tasks>

<verification>
After completing all tasks, verify:
1. [ ] vitest.config.ts exists with proper configuration
2. [ ] test/setup.ts provides Testing Library utilities
3. [ ] All 11 test stub files exist (3 db + 4 auth + 2 ratelimit + 1 middleware)
4. [ ] Each test stub uses it.todo() or describe.skip()
5. [ ] package.json has test scripts
6. [ ] `pnpm test --run` executes without crashing (stubs should pass or show as TODO)
7. [ ] VALIDATION.md Wave 0 requirements satisfied (nyquist_compliant: true)
</verification>

<success_criteria>
1. Vitest framework configured and executable
2. Test setup file provides global test utilities
3. 11 test stub files created matching VALIDATION.md requirements
4. All stubs clearly marked as TODO for implementation phases
5. Test scripts added to package.json
6. Ready for TDD implementation in subsequent plans (RED phase already complete)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-infrastructure/01-00-SUMMARY.md`
</output>
