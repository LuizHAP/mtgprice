---
phase: 01-foundation-infrastructure
plan: 04
type: tdd
wave: 3
depends_on:
  - 01-02
  - 01-03
files_modified:
  - src/lib/auth.ts
  - src/api/auth/login/route.ts
  - src/api/auth/logout/route.ts
  - src/api/auth/verify/route.ts
  - src/api/auth/link-telegram/route.ts
  - middleware.ts
  - src/lib/auth.test.ts
  - src/types/auth.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
must_haves:
  truths:
    - "User can log in with email and password"
    - "JWT token is generated and stored in httpOnly cookie"
    - "Session persists across browser restarts (cookie-based)"
    - "User can log out and clear session cookie"
    - "Protected routes redirect to login if token invalid"
    - "User can link Telegram account via chat_id"
    - "Password is hashed with bcrypt (never stored plain text)"
  artifacts:
    - path: "src/lib/auth.ts"
      provides: "JWT signing/verification and password hashing"
      exports: ["signToken", "verifyToken", "hashPassword", "comparePassword"]
    - path: "src/api/auth/login/route.ts"
      provides: "Login endpoint"
      contains: "POST /api/auth/login"
    - path: "src/api/auth/logout/route.ts"
      provides: "Logout endpoint"
      contains: "POST /api/auth/logout"
    - path: "src/api/auth/verify/route.ts"
      provides: "Token verification endpoint"
      contains: "GET /api/auth/verify"
    - path: "src/api/auth/link-telegram/route.ts"
      provides: "Telegram account linking"
      contains: "POST /api/auth/link-telegram"
    - path: "middleware.ts"
      provides: "Next.js middleware for protected routes"
      contains: "JWT verification logic"
  key_links:
    - from: "src/api/auth/login/route.ts"
      to: "src/lib/auth.ts"
      via: "Import"
      pattern: "comparePassword.*signToken"
    - from: "src/api/auth/logout/route.ts"
      to: "httpOnly cookie"
      via: "Cookie deletion"
      pattern: "cookies.delete.*auth_token"
    - from: "src/lib/auth.ts"
      to: "jsonwebtoken"
      via: "JWT library"
      pattern: "jwt.sign.*jwt.verify"
    - from: "middleware.ts"
      to: "src/lib/auth.ts"
      via: "Import verifyToken"
      pattern: "verifyToken"
    - from: "src/api/auth/link-telegram/route.ts"
      to: "src/db/schema"
      via: "Update user.telegramChatId"
      pattern: "telegramChatId"
---

<objective>
Implement JWT-based authentication system with login, logout, and Telegram account linking.

Purpose: Enable secure user authentication for web dashboard (AUTH-02) and Telegram bot integration (AUTH-01) using JWT tokens with httpOnly cookies and bcrypt password hashing. Session management requires both login AND logout for complete AUTH-02 compliance.

Output: Auth API endpoints (login, logout, verify, link-telegram), JWT utility functions, Next.js middleware for protected routes, and comprehensive tests.
</objective>

<execution_context>
@/Users/luizpansarini/.claude/get-shit-done/workflows/execute-plan.md
@Users/luizpansarini/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-infrastructure/01-CONTEXT.md
@.planning/phases/01-foundation-infrastructure/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create auth type definitions</name>
  <files>src/types/auth.ts</files>
  <action>
Create TypeScript type definitions for authentication:

1. Define User interface:
   - id: number
   - email: string
   - telegramChatId: string | null
   - createdAt: Date

2. Define LoginInput interface:
   - email: string
   - password: string

3. Define AuthResponse interface:
   - user: User
   - token: string

4. Define JwtPayload interface:
   - userId: number
   - email: string
   - iat?: number
   - exp?: number

5. Define LinkTelegramInput interface:
   - chatId: string
   - password: string

Export all types for use in auth components and API routes.

This is the contract that downstream code will implement against.
  </action>
  <verify>
    <automated>grep -E "export.*interface.*User|export.*interface.*LoginInput|export.*interface.*JwtPayload" src/types/auth.ts</automated>
  </verify>
  <done>Type definitions exported, auth contracts defined</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Write auth utility tests (RED phase)</name>
  <files>src/lib/auth.test.ts</files>
  <behavior>
    Test suite for auth utilities:

    Test 1: hashPassword should hash password with bcrypt
    - Given: plain text password
    - When: hashPassword called
    - Then: returns hash different from plain text
    - And: hash can be verified with comparePassword

    Test 2: comparePassword should verify correct password
    - Given: hashed password and correct plain text
    - When: comparePassword called
    - Then: returns true

    Test 3: comparePassword should reject wrong password
    - Given: hashed password and wrong plain text
    - When: comparePassword called
    - Then: returns false

    Test 4: signToken should create valid JWT
    - Given: user payload (userId, email)
    - When: signToken called
    - Then: returns JWT string
    - And: token can be verified with verifyToken

    Test 5: verifyToken should decode valid token
    - Given: valid JWT token
    - When: verifyToken called
    - Then: returns decoded payload (userId, email)

    Test 6: verifyToken should throw on invalid token
    - Given: invalid/malformed token
    - When: verifyToken called
    - Then: throws error

    Test 7: verifyToken should throw on expired token
    - Given: expired token (use very short expiry for test)
    - When: verifyToken called after expiry
    - Then: throws "Token expired" error
  </behavior>
  <action>
Create test file with all test cases. Use Vitest. Mock jwt library for predictable testing.

Test file structure:
- Import auth functions
- Mock bcrypt and jsonwebtoken
- Test password hashing/verification
- Test JWT signing/verification
- Test error handling
  </action>
  <verify>
    <automated>pnpm test src/lib/auth.test.ts</automated>
  </verify>
  <done>Test file created, all tests fail (RED), mocks configured</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement auth utilities (GREEN phase)</name>
  <files>src/lib/auth.ts</files>
  <behavior>
    After implementation, all tests from Task 1 should pass:

    - Password hashing uses bcrypt with 10 salt rounds
    - Password comparison verifies hashes correctly
    - JWT signing creates tokens with proper payload
    - JWT verification decodes valid tokens
    - JWT verification rejects invalid/expired tokens
    - Token expiry set to 1 day per CONTEXT.md decision
  </behavior>
  <action>
Implement auth utility functions:

1. Create src/lib/auth.ts
2. Import bcrypt, jsonwebtoken, and types from src/types/auth.ts
3. Implement hashPassword:
   - Use bcrypt.hash with 10 salt rounds
   - Return hashed password string

4. Implement comparePassword:
   - Use bcrypt.compare
   - Return boolean

5. Implement signToken:
   - Use jwt.sign
   - Payload: { userId, email }
   - Secret: process.env.JWT_SECRET
   - Expiry: '1d' (1 day per CONTEXT.md)
   - Return token string

6. Implement verifyToken:
   - Use jwt.verify
   - Secret: process.env.JWT_SECRET
   - Return decoded payload as JwtPayload
   - Throw on error (invalid, expired, etc.)

7. Export all functions

Use jwt and bcrypt libraries per RESEARCH.md recommendations.
  </action>
  <verify>
    <automated>pnpm test src/lib/auth.test.ts</automated>
  </verify>
  <done>All tests pass (GREEN), auth utilities work correctly</done>
</task>

<task type="auto">
  <name>Task 3: Create login API endpoint</name>
  <files>src/api/auth/login/route.ts</files>
  <action>
Create POST /api/auth/login endpoint:

1. Create app/api/auth/login/route.ts (Next.js App Router API route)
2. Import auth functions, db client, and types
3. Handle POST request:
   - Parse body: { email, password }
   - Validate input (email format, password not empty)
   - Query database for user by email:
     * Import db from src/db/index.ts
     * Import users schema from src/db/schema
     * Use db.select().from(users).where(eq(users.email, email))
   - If user not found: return 401 Unauthorized
   - Compare password hash with comparePassword
   - If password wrong: return 401 Unauthorized
   - Generate JWT token with signToken
   - Set httpOnly cookie with token:
     - Name: 'auth_token'
     - Value: JWT token
     - httpOnly: true (XSS protection)
     - secure: process.env.NODE_ENV === 'production'
     - maxAge: 24 hours (1 day)
     - path: '/'
   - Return 200 OK with user object (without password)

Use httpOnly cookies per CONTEXT.md decision (line 86).
  </action>
  <verify>
    <automated>grep -q "export.*async.*function.*POST" app/api/auth/login/route.ts && grep -q "cookies.set.*auth_token" app/api/auth/login/route.ts</automated>
  </verify>
  <done>Login endpoint exists, validates credentials, sets httpOnly cookie, returns user</done>
</task>

<task type="auto">
  <name>Task 4: Create logout API endpoint (AUTH-02)</name>
  <files>src/api/auth/logout/route.ts</files>
  <action>
Create POST /api/auth/logout endpoint:

1. Create app/api/auth/logout/route.ts
2. Handle POST request:
   - Clear auth_token httpOnly cookie:
     - Name: 'auth_token'
     - Value: '' (empty string)
     - httpOnly: true
     - secure: process.env.NODE_ENV === 'production'
     - maxAge: 0 (immediate expiry)
     - path: '/'
   - Return 200 OK with { success: true, message: 'Logged out successfully' }

This endpoint completes the session management requirement for AUTH-02. Users can now both log in (create session) and log out (destroy session).

Security: Clearing the cookie invalidates the session immediately. No server-side state to clear due to JWT stateless nature.
  </action>
  <verify>
    <automated>grep -q "export.*async.*function.*POST" app/api/auth/logout/route.ts && grep -q "cookies.delete.*auth_token" app/api/auth/logout/route.ts</automated>
  </verify>
  <done>Logout endpoint exists, clears httpOnly cookie, returns success message</done>
</task>

<task type="auto">
  <name>Task 5: Create token verification endpoint</name>
  <files>src/api/auth/verify/route.ts</files>
  <action>
Create GET /api/auth/verify endpoint:

1. Create app/api/auth/verify/route.ts
2. Handle GET request:
   - Read auth_token from httpOnly cookie
   - If no cookie: return 401 Unauthorized
   - Verify token with verifyToken
   - If invalid: return 401 Unauthorized
   - Query database for user by userId from token payload
   - If user not found: return 401 Unauthorized
   - Return 200 OK with user object

This endpoint is used by frontend to check if user is logged in and refresh user data.
  </action>
  <verify>
    <automated>grep -q "export.*async.*function.*GET" app/api/auth/verify/route.ts && grep -q "verifyToken" app/api/auth/verify/route.ts</automated>
  </verify>
  <done>Verify endpoint exists, validates token, returns user data</done>
</task>

<task type="auto">
  <name>Task 6: Create Telegram linking endpoint</name>
  <files>src/api/auth/link-telegram/route.ts</files>
  <action>
Create POST /api/auth/link-telegram endpoint (for AUTH-01):

1. Create app/api/auth/link-telegram/route.ts
2. Handle POST request:
   - Verify user is authenticated (read auth_token cookie, verify token)
   - Parse body: { chatId, password }
   - Validate input (chatId not empty)
   - Update user in database: set telegramChatId = chatId
   - Return 200 OK with updated user object

This links the authenticated user's account to their Telegram chat ID for notifications.

Security: Only authenticated users can link Telegram. This prevents unauthorized linking.
  </action>
  <verify>
    <automated>grep -q "export.*async.*function.*POST" app/api/auth/link-telegram/route.ts && grep -q "telegramChatId" app/api/auth/link-telegram/route.ts</automated>
  </verify>
  <done>Link Telegram endpoint exists, updates user.telegramChatId</done>
</task>

<task type="auto">
  <name>Task 7: Create Next.js middleware for protected routes</name>
  <files>middleware.ts</files>
  <action>
Create Next.js middleware to protect dashboard routes:

IMPORTANT: This plan extends middleware.ts created by Plan 01-03. Plan 01-03 creates root middleware with rate limiting for /api/external/* routes. This plan adds auth protection for /dashboard/* routes. Both must be combined in the single root middleware.ts file.

1. Check if middleware.ts exists from Plan 01-03:
   - If exists: Import existing rate limiting logic
   - If not exists: Create new middleware.ts

2. Structure combined middleware.ts using pattern from RESEARCH.md lines 200-234:

```typescript
import { NextRequest, NextResponse } from 'next/server'
import { verifyToken } from '@/lib/auth'
import { checkRateLimit, RATE_LIMITS } from '@/lib/rate-limiter'
import { getClient } from '@/lib/redis'

export async function middleware(request: NextRequest) {
  const { pathname } = request.nextUrl

  // CONCERN 1: Rate limiting for /api/external/* routes (from Plan 01-03)
  if (pathname.startsWith('/api/external/')) {
    const identifier = request.ip || request.headers.get('x-forwarded-for') || 'anonymous'
    const source = pathname.split('/').pop() || 'default'
    const limit = RATE_LIMITS.SCRYFALL

    const redis = getClient()
    const result = await checkRateLimit(`${source}:${identifier}`, limit.limit, limit.interval, 1)

    if (!result.allowed) {
      return NextResponse.json(
        { error: 'Rate limit exceeded' },
        { status: 429, headers: { 'Retry-After': '60' } }
      )
    }
  }

  // CONCERN 2: Auth protection for /dashboard/* routes (this plan)
  if (pathname.startsWith('/dashboard/')) {
    const token = request.cookies.get('auth_token')?.value

    if (!token) {
      return NextResponse.redirect(new URL('/login', request.url))
    }

    try {
      const payload = verifyToken(token)
      // Token is valid, allow request
    } catch (error) {
      // Token is invalid, clear cookie and redirect
      const response = NextResponse.redirect(new URL('/login', request.url))
      response.cookies.delete('auth_token')
      return response
    }
  }

  return NextResponse.next()
}

export const config = {
  matcher: ['/api/external/:path*', '/dashboard/:path*']
}
```

3. Import verifyToken from src/lib/auth.ts
4. Import NextResponse, NextRequest from next/server
5. Define config matcher:
   - Protect: /dashboard/*, /api/protected/*
   - Include: /api/external/* (rate limiting from Plan 01-03)
   - Exclude: /api/auth/*, /login, /_next/*

6. Implement auth logic for dashboard routes:
   - Read auth_token cookie
   - If no cookie and accessing protected route: redirect to /login
   - If cookie exists: verify token with verifyToken
   - If invalid: clear cookie, redirect to /login
   - If valid: allow request (NextResponse.next())
  </action>
  <verify>
    <automated>grep -q "export.*function.*middleware" middleware.ts && grep -q "matcher.*dashboard" middleware.ts && grep -q "verifyToken" middleware.ts</automated>
  </verify>
  <done>Middleware exists, protects dashboard routes, combines auth + rate limiting, redirects to login if unauthorized</done>
</task>

</tasks>

<verification>
After completing all tasks, verify:
1. [ ] All auth tests pass (pnpm test src/lib/auth.test.ts)
2. [ ] Password hashing uses bcrypt with 10+ salt rounds
3. [ ] JWT tokens expire in 1 day
4. [ ] Login endpoint (/api/auth/login) sets httpOnly cookie
5. [ ] Logout endpoint (/api/auth/logout) clears httpOnly cookie (AUTH-02 complete)
6. [ ] Verify endpoint (/api/auth/verify) validates tokens
7. [ ] Telegram link endpoint (/api/auth/link-telegram) updates user
8. [ ] Middleware protects /dashboard routes
9. [ ] Middleware combines both rate limiting (from 01-03) and auth protection (this plan)
10. [ ] Protected routes redirect to /login if unauthorized
11. [ ] Code matches RESEARCH.md patterns (lines 200-234 for middleware)
</verification>

<success_criteria>
1. JWT-based authentication system implemented with signToken/verifyToken
2. Password hashing with bcrypt (10 salt rounds minimum)
3. Login API endpoint validates credentials and sets httpOnly cookie
4. Logout API endpoint clears httpOnly cookie (completes AUTH-02 session management)
5. Token verification endpoint checks session validity
6. Telegram linking endpoint connects user account to chat_id
7. Next.js middleware protects dashboard routes and combines auth + rate limiting
8. All tests pass with ≥80% coverage
9. Ready for Phase 3 (web dashboard) and Phase 4 (Telegram bot integration)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-infrastructure/01-04-SUMMARY.md`
</output>
