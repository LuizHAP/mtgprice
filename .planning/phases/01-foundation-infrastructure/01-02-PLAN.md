---
phase: 01-foundation-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema/index.ts
  - src/db/schema/users.ts
  - src/db/schema/cards.ts
  - src/db/schema/prices.ts
  - src/db/schema/wishlists.ts
  - src/db/index.ts
  - drizzle.config.ts
  - package.json
autonomous: true
requirements:
  - PRICE-08
must_haves:
  truths:
    - "Database schema defines users, cards, prices, wishlists tables"
    - "Prices table uses TimescaleDB hypertable for time-series optimization"
    - "Composite index on (card_id, timestamp DESC) exists for fast time-series queries"
    - "Foreign keys enforce data integrity between tables"
    - "Drizzle migrations can generate SQL from schema"
  artifacts:
    - path: "src/db/schema/index.ts"
      provides: "Schema exports"
      exports: ["users", "cards", "prices", "wishlists"]
    - path: "src/db/schema/prices.ts"
      provides: "Time-series price storage"
      contains: "cardId, source, priceBrl, timestamp columns"
    - path: "src/db/schema/users.ts"
      provides: "User storage with Telegram linking"
      contains: "email, passwordHash, telegramChatId columns"
    - path: "drizzle.config.ts"
      provides: "Drizzle Kit configuration"
      contains: "schema path, database URL, out folder"
  key_links:
    - from: "src/db/schema/prices.ts"
      to: "PostgreSQL"
      via: "Drizzle ORM migration"
      pattern: "CREATE TABLE prices"
    - from: "prices.cardId"
      to: "cards.oracleId"
      via: "Foreign key constraint"
      pattern: "references.*cards.*oracleId"
    - from: "src/db/index.ts"
      to: "src/db/schema"
      via: "Schema imports"
      pattern: "from.*schema"
---

<objective>
Create database schema with Drizzle ORM, TimescaleDB hypertables, and proper indexes.

Purpose: Establish the data model for users, cards, prices, and wishlists with time-series optimization for price history queries.

Output: Drizzle schema definitions, database connection client, and migration configuration ready for database creation.
</objective>

<execution_context>
@/Users/luizpansarini/.claude/get-shit-done/workflows/execute-plan.md
@Users/luizpansarini/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-infrastructure/01-CONTEXT.md
@.planning/phases/01-foundation-infrastructure/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Drizzle schema definitions</name>
  <files>src/db/schema/users.ts, src/db/schema/cards.ts, src/db/schema/prices.ts, src/db/schema/wishlists.ts, src/db/schema/index.ts</files>
  <action>
Create database schema files using Drizzle ORM pg-core:

1. Create src/db/schema/users.ts with:
   - id: serial, primary key
   - email: varchar(255), not null, unique
   - passwordHash: varchar(255), not null
   - telegramChatId: varchar(255), nullable (for AUTH-01)
   - createdAt: timestamp, not null, defaultNow()

2. Create src/db/schema/cards.ts with:
   - id: serial, primary key
   - oracleId: varchar(255), not null, unique (Scryfall oracle_id per CONTEXT.md)
   - name: varchar(255), not null
   - set: varchar(10), nullable
   - rarity: varchar(50), nullable
   - color: varchar(50), nullable
   - imageUrl: text, nullable
   - lastFetched: timestamp, nullable (for cache TTL)
   - Index on oracleId for fast lookups

3. Create src/db/schema/prices.ts with:
   - id: serial, primary key
   - cardId: varchar(255), not null, references cards.oracleId
   - source: varchar(50), not null ('liga_magic', 'tcgplayer', 'cardmarket', 'cardkingdom')
   - priceBrl: numeric(10, 2), not null (use numeric type, not varchar in production)
   - timestamp: timestamp, not null, defaultNow()
   - Composite index on (cardId, timestamp DESC) per CONTEXT.md decision

4. Create src/db/schema/wishlists.ts with:
   - id: serial, primary key
   - userId: integer, not null, references users.id
   - cardId: varchar(255), not null, references cards.oracleId
   - addedAt: timestamp, not null, defaultNow()

5. Create src/db/schema/index.ts to export all schemas

Use exact schema structure from RESEARCH.md lines 407-466 (Drizzle schema example).
  </action>
  <verify>
    <automated>cat src/db/schema/index.ts | grep -E "export.*users|export.*cards|export.*prices|export.*wishlists"</automated>
  </verify>
  <done>All schema files created with proper types, indexes, and foreign keys</done>
</task>

<task type="auto">
  <name>Task 2: Set up Drizzle database connection and configuration</name>
  <files>src/db/index.ts, drizzle.config.ts</files>
  <action>
Create database connection and Drizzle Kit configuration:

1. Create src/db/index.ts:
   - Import drizzle-orm postgres package
   - Create database client using postgres(process.env.DATABASE_URL)
   - Export db instance for use in API routes and migrations
   - Export schema types from src/db/schema

2. Create drizzle.config.ts:
   - Schema path: "./src/db/schema"
   - Database URL: from process.env.DATABASE_URL
   - Out folder: "./drizzle" (for migration SQL files)
   - Driver: "pg" (PostgreSQL)

3. Add migration script to package.json:
   - "db:generate": "drizzle-kit generate"
   - "db:migrate": "drizzle-kit push"
   - "db:studio": "drizzle-kit studio"

Note: This sets up the database client. Actual TimescaleDB hypertable conversion happens in Task 3.
  </action>
  <verify>
  <automated>grep -q "drizzle-orm" src/db/index.ts && grep -q "DATABASE_URL" src/db/index.ts && test -f drizzle.config.ts</automated>
  </verify>
  <done>Database client connects, Drizzle Kit configuration exists</done>
</task>

<task type="auto">
  <name>Task 3: Create TimescaleDB hypertable migration</name>
  <files>drizzle/0001_timescale_hypertable.sql, drizzle/0002_indexes.sql</files>
  <action>
Create manual SQL migrations for TimescaleDB optimization:

1. Create drizzle/ directory if it doesn't exist

2. Create drizzle/0001_timescale_hypertable.sql with:
   - Standard CREATE TABLE statements (these will be auto-generated by Drizzle, but we need to add TimescaleDB-specific commands)
   - SELECT create_hypertable('prices', 'timestamp', chunk_time_interval => INTERVAL '7 days');
   - This converts the prices table to a hypertable for time-series optimization

3. Create drizzle/0002_indexes.sql with:
   - CREATE INDEX idx_prices_card_timestamp ON prices (card_id, timestamp DESC);
   - This composite index covers 90% of queries per CONTEXT.md

4. Add migration instructions in README:
   - After running `drizzle-kit push`, manually execute the TimescaleDB hypertable conversion SQL
   - Verify hypertable: SELECT * FROM timescaledb_information.hypertables WHERE hypertable_name = 'prices';

Important: TimescaleDB hypertable conversion cannot be done through Drizzle migrations alone. It requires manual SQL execution after table creation.
  </action>
  <verify>
  <automated>grep -q "create_hypertable" drizzle/0001_timescale_hypertable.sql && grep -q "idx_prices_card_timestamp" drizzle/0002_indexes.sql</automated>
  </verify>
  <done>TimescaleDB hypertable SQL exists, composite index SQL exists, migration instructions documented</done>
</task>

</tasks>

<verification>
After completing all tasks, verify:
1. [ ] All schema files exist in src/db/schema/
2. [ ] Schema defines all required tables (users, cards, prices, wishlists)
3. [ ] Foreign keys are properly defined (prices.cardId -> cards.oracleId, wishlists.userId -> users.id)
4. [ ] Composite index on (card_id, timestamp DESC) is defined
5. [ ] TimescaleDB hypertable SQL exists in drizzle/ directory
6. [ ] drizzle.config.ts exists with correct configuration
7. [ ] `pnpm db:generate` would generate migration SQL (dry run only, don't apply yet)
</verification>

<success_criteria>
1. Drizzle schema defines all 4 tables (users, cards, prices, wishlists) with correct columns
2. Prices table schema supports time-series queries (card_id, source, price_brl, timestamp)
3. Foreign keys enforce referential integrity (card_id references oracle_id, user_id references users.id)
4. TimescaleDB hypertable SQL ready for manual execution after table creation
5. Composite index (card_id, timestamp DESC) defined for fast time-series lookups
6. Database connection client configured in src/db/index.ts
7. Drizzle Kit can generate migrations from schema
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-infrastructure/01-02-SUMMARY.md`
</output>
