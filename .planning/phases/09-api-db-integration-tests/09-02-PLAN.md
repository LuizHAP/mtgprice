---
phase: 09
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/api/__tests__/cards/search.test.ts
autonomous: true
requirements: [TEST-08]
tags: [testing, card-search, integration-tests, vitest]

must_haves:
  truths:
    - "All 5 previously-skipped tests in src/api/__tests__/cards/search.test.ts are active (no `test.skip` remains in the file)"
    - "Test 'returns matching cards by name' passes: seeds 3 cards, asserts query 'Black Lotus' returns both Black Lotus variants and excludes Lotus Petal"
    - "Test 'returns empty array if no matches' passes: searchCards('NonExistentXYZ123') returns []"
    - "Test 'is case-insensitive' passes: searchCards('black lotus') and searchCards('BLACK LOTUS') both find the seeded 'Black Lotus'"
    - "Test 'limits results to 10 cards' passes: 15 seeded cards matching 'Test Card' yield at most 10 results"
    - "Test 'requires at least 2 characters' passes: searchCards('A') rejects with the exact message 'Query must be at least 2 characters long'"
    - "beforeEach truncates the cards table so tests do not bleed data between runs"
    - "Full test suite shows +5 active tests vs the post-Plan-01 baseline (166 passed | 177 skipped) with no other test regressions"
  artifacts:
    - path: "src/api/__tests__/cards/search.test.ts"
      provides: "5 active integration tests for searchCards() against a real PostgreSQL DB"
      contains: "import { searchCards } from '@/lib/cards/queries'"
      min_lines: 60
  key_links:
    - from: "src/api/__tests__/cards/search.test.ts"
      to: "src/lib/cards/queries.ts"
      via: "searchCards direct import (no HTTP/route handler layer per D-07)"
      pattern: "from '@/lib/cards/queries'"
    - from: "src/api/__tests__/cards/search.test.ts"
      to: "src/test/helpers/db.ts"
      via: "seedTestCard + truncateTable imports"
      pattern: "from '@/test/helpers/db'"
---

<objective>
Activate the 5 `test.skip` stubs in `src/api/__tests__/cards/search.test.ts` by writing real Vitest integration tests that call `searchCards` from `@/lib/cards/queries` directly against a live PostgreSQL database. No mocks, no `NextRequest`, no HTTP layer — per D-07.

Purpose: TEST-08 requires active coverage of the card-search behavior (name match, empty result, case-insensitive, limit 10, min-2-chars). Plan 09-01 created the dependencies (`searchCards`, helper file, fixed DB credentials). This plan delivers the test bodies that exercise them end-to-end against real Postgres.

Output: One modified test file with 5 newly active tests. Net suite change: +5 active tests (161 passed → 166 passed; 182 skipped → 177 skipped). No production code touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-api-db-integration-tests/09-CONTEXT.md
@.planning/phases/09-api-db-integration-tests/09-RESEARCH.md
@.planning/phases/09-api-db-integration-tests/09-PATTERNS.md
@.planning/phases/09-api-db-integration-tests/09-VALIDATION.md

# Plan 01 SUMMARY (foundation files this plan consumes)
@.planning/phases/09-api-db-integration-tests/09-01-SUMMARY.md

# The test file to modify (5 test.skip → test)
@src/api/__tests__/cards/search.test.ts

# Production source under test (D-07 — tested directly, not through the route)
@src/lib/cards/queries.ts

# Helpers (created in Plan 01)
@src/test/helpers/db.ts

# Schema for table reference in truncateTable(cards)
@src/db/schema/cards.ts

# Test setup with fixed DATABASE_URL (Plan 01)
@test/setup.ts

# Vitest config (timeout, alias @ → src/)
@vitest.config.ts

# Biome rules
@biome.json

<interfaces>
<!-- Contracts consumed by this test file. All from Plan 01 deliverables + existing schema. -->

From @/lib/cards/queries (created by Plan 01):
```typescript
export type CardSearchResult = {
  oracleId: string
  name: string
  set: string | null
  imageUrl: string | null
}
export async function searchCards(query: string): Promise<CardSearchResult[]>
// Throws Error('Query must be at least 2 characters long') when query.length < 2
// Otherwise returns up to 10 rows from cards where name ILIKE %query%
```

From @/test/helpers/db (created by Plan 01):
```typescript
export async function seedTestCard(cardData?: Partial<typeof cards.$inferInsert>): Promise<typeof cards.$inferSelect>
export async function truncateTable(table: any): Promise<void>
// seedTestUser and seedTestWishlist also exported but NOT used in this plan (no FK chain needed for search tests)
```

From @/db/schema (re-exports `cards`):
```typescript
export const cards: PgTable  // used as the table argument to truncateTable
```

vitest globals (already enabled via vitest.config.ts):
```typescript
import { beforeEach, describe, expect, test } from 'vitest'
```
</interfaces>

<critical_rules>
1. **DO NOT mock `@/db`** in this file. The whole point of Phase 9 is real DB assertions (RESEARCH.md "Anti-Patterns to Avoid"). No `vi.mock(...)`.
2. **DO NOT import NextRequest/NextResponse** or test through the HTTP route handler. D-07 says: tests call `searchCards` directly.
3. **Biome import order:** internal `@/` imports first (alphabetised by path), then external (`vitest`). Single quotes, no semicolons, trailing commas. Run `pnpm biome check --write src/api/__tests__/cards/search.test.ts` after editing.
4. **`test.skip` → `test`:** replace each stub's `test.skip(...)` with `test(...)`. Replace the entire body (including any placeholder assertion). Keep the existing describe block label `'Card Search API endpoint'`.
5. **Use explicit `oracleId` values when seeding multiple cards in one test.** `Date.now()` is only millisecond-unique — rapid consecutive seeds can collide on the `cards.oracleId` UNIQUE constraint (RESEARCH.md Pitfall 4). Use suffixes like `'test-bl-001'`, `'test-bl-002'`, `` `test-limit-${i}` ``.
6. **Truncate only `cards`** in `beforeEach` — these tests do not touch wishlists or users. No FK chain to manage (RESEARCH.md Pitfall 3 applies only when seeding wishlists).
7. **Assert the EXACT error message** for the min-2-chars test: `'Query must be at least 2 characters long'` — must match `searchCards`'s thrown message verbatim.
8. **DO NOT touch** the `// TODO: Implement these tests in plan 03-02` comment — leave it or remove it, but do not edit it in a way that affects test semantics. (Recommendation: remove the obsolete TODO line; this phase activates them.)
9. **Vitest async tests:** every test body that awaits DB calls must be `async`. Use `await expect(searchCards('A')).rejects.toThrow('...')` for the rejection assertion.
10. **PostgreSQL must be running** for tests to pass. If `pnpm test:run` fails with connection errors, ensure `docker compose up -d postgres` was executed.
</critical_rules>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Activate 5 search.test.ts integration tests against real PostgreSQL</name>

  <files>src/api/__tests__/cards/search.test.ts</files>

  <read_first>
    - src/api/__tests__/cards/search.test.ts (current state — 5 test.skip stubs at lines 7, 18, 28, 39, 50)
    - src/lib/cards/queries.ts (the function under test — confirm exported signature and throw message after Plan 01)
    - src/test/helpers/db.ts (confirm seedTestCard signature and truncateTable signature after Plan 01)
    - src/db/schema/cards.ts (confirm `cards` is exported from `@/db/schema`)
    - .planning/phases/09-api-db-integration-tests/09-PATTERNS.md (Pattern 3 + concrete examples for all 5 tests)
    - .planning/phases/09-api-db-integration-tests/09-RESEARCH.md (Pitfalls 1, 4, 6)
    - biome.json (import order, quote style)
  </read_first>

  <action>
Replace the entire contents of `src/api/__tests__/cards/search.test.ts` with an active version that imports the helpers, sets up `beforeEach` truncation, and implements all 5 test bodies. The describe block label stays `'Card Search API endpoint'`. The `test.skip` calls all become `test`.

Imports (Biome order — internal `@/` first, then external):
- `searchCards` from `@/lib/cards/queries`
- `cards` from `@/db/schema`
- `seedTestCard`, `truncateTable` from `@/test/helpers/db`
- `beforeEach`, `describe`, `expect`, `test` from `vitest`

beforeEach hook inside the describe block:
```
beforeEach(async () => { await truncateTable(cards) })
```
This truncates the cards table before every test so no state bleeds across runs (D-12, D-13).

Test 1 — `'GET /api/cards/search returns matching cards by name'`:
- Seed three cards with explicit `oracleId` values: `{ name: 'Black Lotus', oracleId: 'test-bl-001' }`, `{ name: 'Black Lotus (Arena)', oracleId: 'test-bl-002' }`, `{ name: 'Lotus Petal', oracleId: 'test-lp-001' }`.
- Call `const results = await searchCards('Black Lotus')`.
- Assert `results.some((c) => c.name === 'Black Lotus')` is true.
- Assert `results.some((c) => c.name === 'Black Lotus (Arena)')` is true (substring match — `%Black Lotus%` matches the parenthesised variant).
- Assert `results.some((c) => c.name === 'Lotus Petal')` is false.

Test 2 — `'GET /api/cards/search returns empty array if no matches'`:
- No seeding (beforeEach already truncated cards).
- Call `const results = await searchCards('NonExistentXYZ123')`.
- Assert `results` deeply equals `[]` (`expect(results).toEqual([])`).

Test 3 — `'GET /api/cards/search is case-insensitive'`:
- Seed one card: `{ name: 'Black Lotus', oracleId: 'test-bl-ci-001' }`.
- Call both `await searchCards('black lotus')` and `await searchCards('BLACK LOTUS')`.
- Assert both result arrays contain a card whose `name === 'Black Lotus'`.

Test 4 — `'GET /api/cards/search limits results to 10 cards'`:
- Seed 15 cards in a for-loop: `for (let i = 0; i < 15; i++) { await seedTestCard({ name: \`Test Card ${i}\`, oracleId: \`test-limit-${i}\` }) }`. Sequential `await` (not `Promise.all`) keeps inserts deterministic and avoids any transient FK-cache or oracleId-collision edge cases.
- Call `const results = await searchCards('Test Card')`.
- Assert `results.length <= 10` (`expect(results.length).toBeLessThanOrEqual(10)`).

Test 5 — `'GET /api/cards/search requires at least 2 characters'`:
- No seeding required.
- Assert `await expect(searchCards('A')).rejects.toThrow('Query must be at least 2 characters long')`.
- The message string must be the exact literal that `searchCards` throws (Plan 01 Task 2).

After writing the file, run `pnpm biome check --write src/api/__tests__/cards/search.test.ts` to enforce style.

Run `npx vitest run src/api/__tests__/cards/search.test.ts` and confirm 5/5 pass. If PostgreSQL is not running, surface the blocker (`docker compose up -d postgres`) before fixing test logic. If the suite fails with `password authentication failed`, double-check Plan 01 Task 1 actually fixed `test/setup.ts` line 6.

Optional cleanup: remove the now-obsolete `// TODO: Implement these tests in plan 03-02` comment (line 5 in the current file). It is no longer accurate.
  </action>

  <verify>
    <automated>pnpm biome check src/api/__tests__/cards/search.test.ts && [ "$(grep -c 'test.skip' src/api/__tests__/cards/search.test.ts)" = "0" ] && [ "$(grep -cE "^\s*test\(" src/api/__tests__/cards/search.test.ts)" = "5" ] && grep -q "beforeEach" src/api/__tests__/cards/search.test.ts && grep -q "truncateTable(cards)" src/api/__tests__/cards/search.test.ts && grep -q "from '@/lib/cards/queries'" src/api/__tests__/cards/search.test.ts && grep -q "from '@/test/helpers/db'" src/api/__tests__/cards/search.test.ts && npx vitest run src/api/__tests__/cards/search.test.ts 2>&1 | tee /tmp/phase9-plan02-test.log | grep -E "Tests +5 passed"</automated>
  </verify>

  <acceptance_criteria>
    - File `src/api/__tests__/cards/search.test.ts` contains 0 occurrences of `test.skip` (grep returns 0)
    - File `src/api/__tests__/cards/search.test.ts` contains exactly 5 active `test(` declarations
    - File imports `searchCards` from `@/lib/cards/queries`
    - File imports `seedTestCard` and `truncateTable` from `@/test/helpers/db`
    - File contains a `beforeEach(async () => { await truncateTable(cards) })` block inside the describe
    - Test names match the original stub labels: `'GET /api/cards/search returns matching cards by name'`, `'GET /api/cards/search returns empty array if no matches'`, `'GET /api/cards/search is case-insensitive'`, `'GET /api/cards/search limits results to 10 cards'`, `'GET /api/cards/search requires at least 2 characters'`
    - Min-2-chars test asserts the exact string `'Query must be at least 2 characters long'`
    - Limit-10 test seeds 15 cards with `oracleId` suffixed `test-limit-0` through `test-limit-14`
    - `pnpm biome check src/api/__tests__/cards/search.test.ts` exits 0
    - `npx vitest run src/api/__tests__/cards/search.test.ts` reports `Tests  5 passed (5)` (0 skipped, 0 failed)
    - No `vi.mock(` calls anywhere in the file (verified by `grep -c "vi.mock" src/api/__tests__/cards/search.test.ts` returning 0)
  </acceptance_criteria>

  <done>All 5 search tests are active and green against the real PostgreSQL DB; no production code touched; Biome and TypeScript happy.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run full suite to confirm +5 active tests and zero regressions</name>

  <files>(verification-only — no file modifications)</files>

  <read_first>
    - .planning/phases/09-api-db-integration-tests/09-VALIDATION.md (sampling rate)
    - src/lib/wishlist/__tests__/actions.test.ts (confirm 6 tests still `test.skip` — they are activated in Plan 09-03, not here)
  </read_first>

  <action>
Run the full Vitest suite and confirm the net delta is exactly +5 active tests (5 from search.test.ts moved from skipped → passing) and no other test changed state.

Step 1 — confirm wishlist `actions.test.ts` is still entirely skipped (it is the target of Plan 09-03, not this plan). `grep -c "test.skip" src/lib/wishlist/__tests__/actions.test.ts` should return `6`.

Step 2 — run `pnpm test:run` and inspect the summary line. Expected: `Tests  166 passed | 177 skipped | 38 todo (381)`. If the totals differ, account for the difference: only the 5 search tests should have flipped from skipped to passed.

Step 3 — confirm no production file was touched: `git diff --name-only` (compared to the post-Plan-01 state) should report only `src/api/__tests__/cards/search.test.ts`.

If the suite contains failures unrelated to this plan (e.g., a Plan-01 file regressed), surface them — do not patch this plan beyond its scope.
  </action>

  <verify>
    <automated>[ "$(grep -c 'test.skip' src/lib/wishlist/__tests__/actions.test.ts)" = "6" ] && pnpm test:run 2>&1 | tee /tmp/phase9-plan02-suite.log | grep -E "Tests +166 passed \| 177 skipped"</automated>
  </verify>

  <acceptance_criteria>
    - `pnpm test:run` summary line contains `166 passed` AND `177 skipped`
    - `grep -c "test.skip" src/lib/wishlist/__tests__/actions.test.ts` returns `6` (Plan 09-03's targets remain skipped)
    - `grep -c "test.skip" src/api/__tests__/cards/search.test.ts` returns `0`
    - `git diff --name-only HEAD~1 HEAD` shows only `src/api/__tests__/cards/search.test.ts` was changed by this plan (or only files within this plan's `files_modified` list)
    - No Vitest open-handle warnings or unhandled rejections in the output log
  </acceptance_criteria>

  <done>Suite shows 166 passed | 177 skipped; only the search test file changed; wishlist actions tests untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner → PostgreSQL | Same boundary as Plan 01. Local dev DB only; no external network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Tampering (test data pollution) | Shared test DB with no isolation between runs | mitigate | `beforeEach(async () => { await truncateTable(cards) })` ensures each test starts with an empty cards table. D-12 explicitly accepts the trade-off (single-user project, no concurrent runs). |
| T-09-05 | Tampering (oracleId collisions in seed loop) | Seeding 15 cards in limit-10 test | mitigate | Use explicit counter-suffixed oracleIds (`test-limit-${i}`) rather than `Date.now()` defaults — guarantees uniqueness across the 15 inserts (Pitfall 4). |
</threat_model>

<verification>
- `pnpm biome check src/api/__tests__/cards/search.test.ts` exits 0
- `npx vitest run src/api/__tests__/cards/search.test.ts` shows 5 passed, 0 skipped
- `pnpm test:run` shows `166 passed | 177 skipped`
- Wishlist actions tests still entirely `test.skip` (6 stubs preserved for Plan 09-03)
- No production code modified
</verification>

<success_criteria>
- TEST-08 satisfied: 5 active integration tests for card search, all green against real PostgreSQL
- All 5 ROADMAP success-criterion test cases (name match, empty result, case-insensitive, limit-10, min-2-chars) verified
- D-07 honored: tests call `searchCards` directly (no HTTP layer mocking)
- D-12 honored: `beforeEach` truncation only
- Plan 09-03's targets remain untouched
</success_criteria>

<output>
After completion, create `.planning/phases/09-api-db-integration-tests/09-02-SUMMARY.md` using the project's summary template (test cases activated, decisions implemented with D-IDs, runtime measured for the 5 tests, before/after suite counts).
</output>
