---
phase: 09
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/setup.ts
  - src/test/helpers/db.ts
  - src/lib/cards/queries.ts
  - src/app/api/cards/search/route.ts
autonomous: true
requirements: [TEST-08, TEST-09]
tags: [testing, db-helpers, query-extraction, integration-tests]

must_haves:
  truths:
    - "test/setup.ts DATABASE_URL matches docker-compose credentials (mtgprice:mtgprice_password) so integration tests can connect"
    - "src/test/helpers/db.ts exists exporting seedTestUser, seedTestCard, seedTestWishlist, truncateTable with NO db parameter (db imported internally)"
    - "src/lib/cards/queries.ts exists exporting searchCards(query: string) that throws 'Query must be at least 2 characters long' when query.length < 2 and otherwise runs the same ilike+limit(10) query as the route handler"
    - "src/app/api/cards/search/route.ts no longer contains an inline db.select() call — it delegates to searchCards from @/lib/cards/queries and returns { cards: results } with HTTP 200, retains 400 guard for invalid query, retains 500 catch"
    - "The existing root-level test/helpers/db.ts (db-param signature) is NOT deleted or modified — left intact per D-03"
    - "pnpm biome check shows 0 errors across the four modified files"
    - "pnpm test:run still shows 161 passed | 182 skipped (no regressions; new tests are activated in plans 02/03)"
  artifacts:
    - path: "test/setup.ts"
      provides: "Test-suite environment variable bootstrap with corrected DATABASE_URL"
      contains: "mtgprice:mtgprice_password@localhost:5432/mtgprice"
    - path: "src/test/helpers/db.ts"
      provides: "No-param test data helpers: seedTestUser, seedTestCard, seedTestWishlist, truncateTable"
      exports: ["seedTestUser", "seedTestCard", "seedTestWishlist", "truncateTable"]
      min_lines: 60
    - path: "src/lib/cards/queries.ts"
      provides: "Standalone searchCards(query) extracted from route handler; reusable from tests and route"
      exports: ["searchCards", "CardSearchResult"]
      min_lines: 20
    - path: "src/app/api/cards/search/route.ts"
      provides: "HTTP route handler that delegates DB query to searchCards"
      contains: "from '@/lib/cards/queries'"
  key_links:
    - from: "src/app/api/cards/search/route.ts"
      to: "src/lib/cards/queries.ts"
      via: "searchCards import"
      pattern: "from '@/lib/cards/queries'"
    - from: "src/test/helpers/db.ts"
      to: "src/db/index.ts"
      via: "db singleton import at module level (no db parameter)"
      pattern: "import \\{ db \\} from '@/db'"
    - from: "src/lib/cards/queries.ts"
      to: "src/db/schema (cards table)"
      via: "drizzle ilike query against cards.name with limit(10)"
      pattern: "ilike\\(cards\\.name"
---

<objective>
Lay the Wave-1 foundation that the two test-activation plans (09-02, 09-03) depend on. Three new/modified files plus one credential fix unlock all 11 skipped integration tests.

Purpose: The skipped tests import `searchCards` from `@/lib/cards/queries` and helpers from `@/test/helpers/db` — neither path exists today (per D-01, D-02, D-04). Tests also cannot connect to PostgreSQL because `test/setup.ts` hardcodes `postgres:postgres` while docker-compose provisions `mtgprite:mtgprice_password`. Plan 01 fixes the credentials, creates the helper file (no-`db`-param signatures per D-01/D-02), extracts `searchCards` from the existing route handler into a reusable lib module (D-04/D-06), and refactors the route to delegate (D-06). After this plan, the route still passes existing HTTP behavior and the codebase is ready for direct-function integration testing.

Output: 1 modified config file (`test/setup.ts`), 2 new files (`src/test/helpers/db.ts`, `src/lib/cards/queries.ts`), 1 refactored route handler (`src/app/api/cards/search/route.ts`). Net test count change: 161 passed | 182 skipped (unchanged — new tests are activated in plans 02 and 03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-api-db-integration-tests/09-CONTEXT.md
@.planning/phases/09-api-db-integration-tests/09-RESEARCH.md
@.planning/phases/09-api-db-integration-tests/09-PATTERNS.md
@.planning/phases/09-api-db-integration-tests/09-VALIDATION.md

# Source-of-truth analog: existing test helpers with db-param signature (DO NOT MODIFY — D-03)
@test/helpers/db.ts

# Source-of-truth analog: route handler with inline ilike query — extract into queries.ts
@src/app/api/cards/search/route.ts

# Schema modules referenced by helpers and queries
@src/db/schema/cards.ts
@src/db/schema/users.ts
@src/db/schema/wishlists.ts
@src/db/schema/index.ts

# Test-time env bootstrap with the credential bug to fix
@test/setup.ts

# Vitest config — confirms `@` alias resolves to src/ (so @/test/helpers/db → src/test/helpers/db.ts)
@vitest.config.ts

# Biome rules — single quotes, no semicolons, organizeImports enabled
@biome.json

# Prior plan reference for format/style
@.planning/phases/08-circuit-breaker-tests/08-01-PLAN.md

<interfaces>
<!-- Contracts the executor must implement and consume. Extracted from the codebase. -->

From src/db/index.ts (singleton, already wired):
```typescript
export const db: PostgresJsDatabase<typeof schema>
```

From src/db/schema/users.ts (FK parent of wishlists.user_id):
```typescript
export const users = pgTable('users', {
  id: serial('id').primaryKey(),
  email: varchar('email', { length: 255 }).notNull().unique(),
  passwordHash: varchar('password_hash', { length: 255 }).notNull(),
  telegramChatId: varchar('telegram_chat_id', { length: 255 }),
  createdAt: timestamp('created_at').notNull().defaultNow(),
})
```

From src/db/schema/cards.ts (FK parent of wishlists.card_id via oracleId):
```typescript
// columns relevant to seeding and searchCards
oracleId: varchar('oracle_id', { length: 255 }).notNull().unique()
name:     varchar('name',     { length: 255 }).notNull()
set:      varchar('set',      { length: 10 })           // nullable
rarity:   varchar('rarity',   { length: 50 })           // nullable
color:    varchar('color',    { length: 50 })           // nullable
imageUrl: text('image_url')                              // nullable
```

From src/db/schema/wishlists.ts:
```typescript
// FK to users.id and cards.oracleId, both ON DELETE no action
userId:  integer('user_id').notNull().references(() => users.id)
cardId:  varchar('card_id', { length: 255 }).notNull().references(() => cards.oracleId)
addedAt: timestamp('added_at').notNull().defaultNow()
isAutoAdded: boolean('is_auto_added').notNull().default(false)
// unique('uniqueUserCard').on(table.userId, table.cardId)
```

Helper contracts to export from src/test/helpers/db.ts (no db parameter — D-01/D-02):
```typescript
export async function seedTestUser(userData?: Partial<typeof users.$inferInsert>): Promise<typeof users.$inferSelect>
export async function seedTestCard(cardData?: Partial<typeof cards.$inferInsert>): Promise<typeof cards.$inferSelect>
export async function seedTestWishlist(wishlistData?: Partial<typeof wishlists.$inferInsert>): Promise<typeof wishlists.$inferSelect>
export async function truncateTable(table: any): Promise<void>
```

searchCards contract to export from src/lib/cards/queries.ts (D-04, D-05):
```typescript
export type CardSearchResult = {
  oracleId: string
  name: string
  set: string | null
  imageUrl: string | null
}
export async function searchCards(query: string): Promise<CardSearchResult[]>
// Throws Error('Query must be at least 2 characters long') when query.length < 2 (per D-05 + planner's discretion to throw — consistent with route handler 400 message)
// Otherwise: ilike(cards.name, `%${query}%`).limit(10) returning the 4 columns above
```
</interfaces>

<critical_rules>
1. **DO NOT delete or modify** the existing `test/helpers/db.ts` at repo root. It is used by other test files (D-03). The new file lives at `src/test/helpers/db.ts` and is a separate file.
2. **DO NOT add a `db` parameter** to any function in the new `src/test/helpers/db.ts`. The new file imports `db` at module level and closes over it. (D-01, D-02)
3. **DO NOT remove** the route handler's own `query.length < 2` guard. Both the route AND `searchCards` validate — intentional double protection per D-05/D-06. The route returns HTTP 400; `searchCards` throws.
4. **DO NOT change** the route handler's response shape: `{ cards: results }` with status 200, `{ error: '...' }` with 400 on short query, `{ error: 'Failed to search cards' }` with 500 on unexpected error.
5. **Throw, do not return `[]`** for short queries inside `searchCards` — keeps error semantics aligned with the route's 400 message. Test 09-02 will assert `rejects.toThrow('Query must be at least 2 characters long')`.
6. **Truncation order matters** when both helpers are used together. The new file does NOT need to truncate internally — `truncateTable` only deletes the table it is passed. Test files are responsible for calling it in dependency order (child → parent). Helpers do not enforce order.
7. **Biome import order:** internal `@/` imports first, then external (`drizzle-orm`, `next/server`, `vitest`). Single quotes, no semicolons, trailing commas. Run `pnpm biome check --write <file>` after each edit.
8. **`truncateTable` uses Drizzle `db.delete(table)`** (no WHERE clause) — matches the existing root-level helper. Do NOT use raw SQL `TRUNCATE`. Apply `// biome-ignore lint/suspicious/noExplicitAny: Test helper accepts any table` above the `table: any` parameter.
9. **`seedTestUser` default email must be unique per call** — use `` `test-user-${Date.now()}-${Math.random().toString(36).slice(2,8)}@example.com` `` to avoid colliding with `users.email` unique constraint when seeding multiple users without override.
10. **DO NOT install new packages.** All required modules (`drizzle-orm`, `@/db`, `@/db/schema`) are present.
</critical_rules>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix DATABASE_URL credentials in test/setup.ts + create src/test/helpers/db.ts</name>

  <files>test/setup.ts, src/test/helpers/db.ts</files>

  <read_first>
    - test/setup.ts (current state — line 6 has wrong credentials)
    - test/helpers/db.ts (root-level analog with db-param signatures — logic model only; copy the shape, drop the db param)
    - src/db/schema/cards.ts (cards.$inferInsert / $inferSelect column shapes)
    - src/db/schema/users.ts (users.$inferInsert — for seedTestUser defaults: email unique, passwordHash required)
    - src/db/schema/wishlists.ts (wishlists.$inferInsert — userId, cardId, addedAt; unique('uniqueUserCard'))
    - src/db/schema/index.ts (confirms `cards`, `wishlists` are re-exported from `@/db/schema`; `users` lives at `@/db/schema/users`)
    - .env (confirms actual DB credentials are mtgprice:mtgprice_password)
    - docker-compose.yml (confirms POSTGRES_USER=mtgprice / POSTGRES_PASSWORD=mtgprice_password)
    - biome.json (single quotes, no semicolons, organizeImports rule)
  </read_first>

  <action>
Step 1 — `test/setup.ts`: change ONLY line 6. Replace `process.env.DATABASE_URL = 'postgresql://postgres:postgres@localhost:5432/mtgprice'` with `process.env.DATABASE_URL = 'postgresql://mtgprice:mtgprice_password@localhost:5432/mtgprice'`. Leave all other lines (JWT_SECRET, REDIS_URL, TELEGRAM_BOT_TOKEN, beforeAll/afterAll console mocking) untouched. This unblocks the Postgres connection — verified against `.env` (line 2) and `docker-compose.yml` (POSTGRES_USER/POSTGRES_PASSWORD).

Step 2 — create the new file `src/test/helpers/db.ts` with FOUR exported async functions plus a module-doc comment block at the top. The file MUST import `db` from `@/db` at module level (NOT pass it as a parameter — this is the central D-01/D-02 distinction from the analog `test/helpers/db.ts`).

Imports (Biome order: internal `@/` first, alphabetized):
- `db` from `@/db`
- `cards`, `wishlists` from `@/db/schema`
- `users` from `@/db/schema/users` (not re-exported from the schema barrel? — verify with read; if it IS re-exported via `@/db/schema`, import from there for consistency)

Function 1: `seedTestUser(userData: Partial<typeof users.$inferInsert> = {})` returns `Promise<typeof users.$inferSelect>`. Defaults: email = `` `test-user-${Date.now()}-${Math.random().toString(36).slice(2, 8)}@example.com` ``, passwordHash = `'hashed-password'`. Pattern: `const inserted = await db.insert(users).values({ ...defaults, ...userData }).returning(); return inserted[0]`. This function is REQUIRED because `wishlists.user_id` references `users.id` (Pitfall 2 in RESEARCH.md) and the FK has `ON DELETE no action`.

Function 2: `seedTestCard(cardData: Partial<typeof cards.$inferInsert> = {})` returns `Promise<typeof cards.$inferSelect>`. Defaults: oracleId = `` `test-oracle-${Date.now()}` ``, name = `'Test Card'`, set = `'TST'`, rarity = `'common'`, color = `'R'`, imageUrl = `'https://example.com/card.jpg'`, lastFetched = `new Date()`. Identical defaults to analog `test/helpers/db.ts` lines 52–60 — copy them as-is.

Function 3: `seedTestWishlist(wishlistData: Partial<typeof wishlists.$inferInsert> = {})` returns `Promise<typeof wishlists.$inferSelect>`. Defaults: userId = `1`, cardId = `` `test-oracle-${Date.now()}` ``, addedAt = `new Date()`. Identical defaults to analog lines 120–124. NOTE: this default will FK-fail unless the caller seeds a user with id=1 OR overrides userId — that is the caller's responsibility (documented in JSDoc).

Function 4: `truncateTable(table: any): Promise<void>` — body is exactly `await db.delete(table)`. Apply `// biome-ignore lint/suspicious/noExplicitAny: Test helper accepts any table` immediately above the `table: any` parameter. Do NOT add a `db` parameter — `db` is at module scope.

Add brief JSDoc on each function describing intent, the unique constraint behavior (uniqueUserCard for wishlists, email for users), and the FK chain note in `seedTestWishlist` (must seed user first).

DO NOT add `seedTestPrice`, `seedTestCards`, `seedTestPricesForAllSources` — those exist in the analog and serve other phases. Phase 9 only needs the four above.

Step 3 — run `pnpm biome check --write test/setup.ts src/test/helpers/db.ts` to enforce import order, single quotes, no semicolons, trailing commas.
  </action>

  <verify>
    <automated>pnpm biome check test/setup.ts src/test/helpers/db.ts && grep -q "mtgprice:mtgprice_password@localhost:5432/mtgprice" test/setup.ts && grep -q "export async function seedTestUser" src/test/helpers/db.ts && grep -q "export async function seedTestCard" src/test/helpers/db.ts && grep -q "export async function seedTestWishlist" src/test/helpers/db.ts && grep -q "export async function truncateTable" src/test/helpers/db.ts && ! grep -E "db: ?PostgresJsDatabase" src/test/helpers/db.ts</automated>
  </verify>

  <acceptance_criteria>
    - File `test/setup.ts` line 6 contains the exact string `postgresql://mtgprice:mtgprice_password@localhost:5432/mtgprice` (single quotes)
    - File `test/setup.ts` retains its `JWT_SECRET`, `REDIS_URL`, `TELEGRAM_BOT_TOKEN` lines and `beforeAll`/`afterAll` blocks unchanged
    - File `src/test/helpers/db.ts` exists and exports exactly four async functions: `seedTestUser`, `seedTestCard`, `seedTestWishlist`, `truncateTable`
    - None of the four exported functions accept a `db` parameter (verified by `grep -E "db: ?PostgresJsDatabase" src/test/helpers/db.ts` returning no matches)
    - `src/test/helpers/db.ts` imports `db` from `@/db` at module level
    - `truncateTable` body is exactly `await db.delete(table)` with a `// biome-ignore lint/suspicious/noExplicitAny:` comment above `table: any`
    - `seedTestUser` defaults produce a unique email each call (uses `Date.now()` plus random suffix)
    - `pnpm biome check test/setup.ts src/test/helpers/db.ts` exits 0
    - Root-level `test/helpers/db.ts` is byte-identical to its pre-task state (verified by `git diff test/helpers/db.ts` showing no changes)
  </acceptance_criteria>

  <done>Credentials fixed in test/setup.ts; new helper file with four no-`db`-param functions present at `src/test/helpers/db.ts`; Biome passes; existing root helper untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Extract searchCards into src/lib/cards/queries.ts and refactor the route handler to delegate</name>

  <files>src/lib/cards/queries.ts, src/app/api/cards/search/route.ts</files>

  <read_first>
    - src/app/api/cards/search/route.ts (current inline ilike query — lines 1–49, source of truth for column selection and query shape)
    - src/lib/wishlist/queries.ts (analog module structure — file-level doc comment, imports order, single-quote style)
    - src/db/schema/cards.ts (column types: set varchar nullable, imageUrl text nullable)
    - biome.json (import order, quote style)
    - .planning/phases/09-api-db-integration-tests/09-PATTERNS.md (Pattern 2: searchCards function; Pattern: route handler refactor)
    - .planning/phases/09-api-db-integration-tests/09-CONTEXT.md (D-04, D-05, D-06)
  </read_first>

  <action>
Step 1 — create `src/lib/cards/queries.ts` (NEW FILE). The module exports two symbols: a type `CardSearchResult` and an async function `searchCards`.

Imports (Biome order, internal `@/` first then external alphabetized):
- `db` from `@/db`
- `cards` from `@/db/schema`
- `ilike` from `drizzle-orm`

Add a file-level JSDoc comment block describing: "Card domain query functions. Extracted from src/app/api/cards/search/route.ts so the same DB query is reusable from API routes and from integration tests (per D-04)."

Export type `CardSearchResult` matching the route's existing select shape exactly:
- `oracleId: string`
- `name: string`
- `set: string | null`
- `imageUrl: string | null`
(Column nullability mirrors `cards.set` and `cards.imageUrl` schema definitions.)

Export `async function searchCards(query: string): Promise<CardSearchResult[]>` with this exact behavior:
- First line of body: if `query.length < 2`, `throw new Error('Query must be at least 2 characters long')` — verbatim string for downstream test assertion in plan 09-02. (D-05; planner's discretion of throw-vs-return: throwing wins because it keeps the error message aligned with the route handler's 400 response copy.)
- Otherwise: return `db.select({ oracleId: cards.oracleId, name: cards.name, set: cards.set, imageUrl: cards.imageUrl }).from(cards).where(ilike(cards.name, \`%${query}%\`)).limit(10)`. Column ordering, the `ilike` predicate, and the `.limit(10)` chain must match the existing route handler implementation verbatim (route handler lines 29–38).

Step 2 — modify `src/app/api/cards/search/route.ts`. Refactor to delegate to `searchCards` while keeping the route's HTTP-level contract identical.

Replace imports (current lines 1–4) with (Biome order — internal `@/` first then `next/server`):
- `searchCards` from `@/lib/cards/queries`
- `NextRequest`, `NextResponse` from `next/server`
Drop `db`, `cards`, `ilike` imports — they move to `queries.ts`.

Update the `GET` handler body:
- Keep the `try` block.
- Keep `const { searchParams } = new URL(request.url)` and `const query = searchParams.get('q')`.
- Keep the route-level 400 guard: `if (!query || query.length < 2) { return NextResponse.json({ error: 'Query must be at least 2 characters long' }, { status: 400 }) }` — both the route AND `searchCards` validate (intentional double protection per D-06; the route returns 400 before throwing inside the lib).
- Replace the inline DB select chain with: `const results = await searchCards(query)`.
- Return `NextResponse.json({ cards: results }, { status: 200 })` — response key remains `cards`, NOT `results`, to preserve the existing API contract.
- Keep the `catch (error)` block as-is: `console.error('Error searching cards:', error)` + `NextResponse.json({ error: 'Failed to search cards' }, { status: 500 })`.

Step 3 — run `pnpm biome check --write src/lib/cards/queries.ts src/app/api/cards/search/route.ts` to enforce style.

Step 4 — run `pnpm tsc --noEmit` to verify types resolve. The handler will infer the return type of `searchCards` automatically; no explicit annotations needed at the call site.
  </action>

  <verify>
    <automated>pnpm biome check src/lib/cards/queries.ts src/app/api/cards/search/route.ts && grep -q "export async function searchCards" src/lib/cards/queries.ts && grep -q "Query must be at least 2 characters long" src/lib/cards/queries.ts && grep -q "ilike(cards.name" src/lib/cards/queries.ts && grep -q ".limit(10)" src/lib/cards/queries.ts && grep -q "from '@/lib/cards/queries'" src/app/api/cards/search/route.ts && ! grep -E "import \{ ilike \}" src/app/api/cards/search/route.ts && ! grep -E "db\\.select" src/app/api/cards/search/route.ts && pnpm tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - File `src/lib/cards/queries.ts` exists and exports `searchCards` (async function) and `CardSearchResult` (type)
    - `searchCards('A')` throws an Error whose message is exactly `'Query must be at least 2 characters long'` (verified by source assertion: file contains the literal string)
    - `searchCards` body uses `ilike(cards.name, \`%${query}%\`)` and chains `.limit(10)` (verified by grep)
    - `searchCards` return type columns are exactly `oracleId`, `name`, `set`, `imageUrl` (no other columns) — verified by reading the select object
    - File `src/app/api/cards/search/route.ts` imports `searchCards` from `@/lib/cards/queries`
    - File `src/app/api/cards/search/route.ts` no longer imports `db`, `cards`, or `ilike` (verified by grep returning no matches)
    - File `src/app/api/cards/search/route.ts` no longer contains `db.select(` (verified by grep returning no matches)
    - File `src/app/api/cards/search/route.ts` still returns `NextResponse.json({ cards: results }, { status: 200 })` for valid queries
    - File `src/app/api/cards/search/route.ts` still returns 400 with message `'Query must be at least 2 characters long'` for missing/short query
    - File `src/app/api/cards/search/route.ts` still returns 500 with message `'Failed to search cards'` in the catch block
    - `pnpm biome check src/lib/cards/queries.ts src/app/api/cards/search/route.ts` exits 0
    - `pnpm tsc --noEmit` exits 0 (no type errors introduced)
  </acceptance_criteria>

  <done>`src/lib/cards/queries.ts` exposes a reusable `searchCards`; the route delegates and preserves its HTTP contract; type-check passes; Biome passes.</done>
</task>

<task type="auto">
  <name>Task 3: Verify foundation by running existing tests + biome + typecheck (no regressions)</name>

  <files>(verification-only — no file modifications)</files>

  <read_first>
    - package.json (confirm `pnpm test:run` script wires to `vitest --run`)
    - .planning/phases/09-api-db-integration-tests/09-VALIDATION.md (sampling rate + Wave 0 requirements)
  </read_first>

  <action>
Run the full test suite and verify NO regressions are introduced by the refactor and helper additions. The 11 skipped tests in `search.test.ts` and `actions.test.ts` MUST remain `test.skip` — they are activated in plans 09-02 and 09-03, not here. The count should remain exactly `161 passed | 182 skipped` (matching the pre-Phase-9 baseline measured during research on 2026-05-13).

Step 1 — confirm PostgreSQL is reachable. If `docker ps --filter name=mtgprice-db --format '{{.Status}}'` shows nothing or "Exited", run `docker compose up -d postgres` and wait ~10s for the healthcheck to pass (per RESEARCH.md Environment Availability). If Docker is unavailable, surface a clear blocker — integration tests in subsequent plans require the DB but Plan 01 itself does not actually exercise it.

Step 2 — run `pnpm biome check .` and confirm 0 errors.

Step 3 — run `pnpm tsc --noEmit` and confirm 0 errors. The refactored route delegates to `searchCards` — types must flow correctly without explicit annotations.

Step 4 — run `pnpm test:run` and confirm the summary line reads exactly `Tests  161 passed | 182 skipped | 38 todo (381)` (or equivalent counts if upstream tests have shifted; the critical invariant is that the 11 target tests are STILL skipped and no previously-passing test now fails).

Step 5 — confirm the route handler still works at the HTTP layer using a smoke-test command (does not require the dev server; this is a code-level assertion only): `grep -c "NextResponse.json" src/app/api/cards/search/route.ts` should report 3 (one 200, one 400, one 500). If 3, the response shape is intact.

If any regression appears (Biome errors, TS errors, previously-passing tests now failing, or the 11 target tests no longer skipped), stop and report — do not patch the test files in this plan.
  </action>

  <verify>
    <automated>pnpm biome check . && pnpm tsc --noEmit && pnpm test:run 2>&1 | tee /tmp/phase9-plan01-test.log | grep -E "Tests +161 passed \| 182 skipped" && [ "$(grep -c 'NextResponse.json' src/app/api/cards/search/route.ts)" = "3" ]</automated>
  </verify>

  <acceptance_criteria>
    - `pnpm biome check .` exits 0 (no errors across the repo after the refactor)
    - `pnpm tsc --noEmit` exits 0 (no new type errors)
    - `pnpm test:run` summary line contains `161 passed` AND `182 skipped` (the 11 target tests remain skipped — they are activated in 09-02 and 09-03)
    - `grep -c "test.skip" src/api/__tests__/cards/search.test.ts` returns `5` (all 5 tests still skipped)
    - `grep -c "test.skip" src/lib/wishlist/__tests__/actions.test.ts` returns `6` (all 6 tests still skipped)
    - `grep -c "NextResponse.json" src/app/api/cards/search/route.ts` returns `3`
    - No file outside `files_modified` list was changed (verified by `git diff --name-only` comparing only the four declared files plus any auto-formatted Biome side-effects in those four files)
  </acceptance_criteria>

  <done>Full repo Biome + TypeScript + Vitest are green at the pre-Phase-9 baseline; route still has 3 `NextResponse.json` returns; the 11 target tests are still skipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner → DB | Tests connect to local PostgreSQL via `DATABASE_URL`; credentials are now `mtgprice:mtgprice_password` (matches docker-compose). No external network. |
| Route handler → `searchCards` | HTTP-layer guards 400 before delegating; lib-layer guards again and throws. Defense in depth — no untrusted input bypasses validation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | test/setup.ts (DATABASE_URL in source) | accept | Credentials match local docker-compose dev DB only; not production; no PII in test data. RESEARCH.md V5 confirms no new attack surface. |
| T-09-02 | Tampering (SQL Injection) | `searchCards(query)` ilike with template literal | mitigate | Drizzle's `ilike(cards.name, \`%${query}%\`)` uses parameterised binds (postgres-js driver) — query is passed as a parameter, not concatenated into raw SQL. Verified in drizzle-orm source: `ilike` produces `name ILIKE $1` with `$1` bound separately. No SQLi surface. |
| T-09-03 | Denial of Service (large query) | `searchCards(query)` no upper bound on query length | accept | PostgreSQL `varchar` matching has linear cost in length; arbitrary-length query strings are constrained by Node's request body limit and HTTP layer. Minimum-length guard (≥2 chars) exists; maximum-length guard is out of scope per Phase 9 boundary (test-only phase). |
</threat_model>

<verification>
- `pnpm biome check .` exits 0 after all three tasks
- `pnpm tsc --noEmit` exits 0
- `pnpm test:run` shows `161 passed | 182 skipped` (baseline preserved)
- `src/test/helpers/db.ts` exposes the four no-`db`-param functions and is importable as `@/test/helpers/db`
- `src/lib/cards/queries.ts` exposes `searchCards` and is importable as `@/lib/cards/queries`
- `src/app/api/cards/search/route.ts` still has three `NextResponse.json` return sites (200, 400, 500)
- Root-level `test/helpers/db.ts` is unchanged (D-03)
</verification>

<success_criteria>
- TEST-08 (foundation): `searchCards` extracted and reusable from tests; route delegates to it
- TEST-09 (foundation): `src/test/helpers/db.ts` exists with seedTestUser, seedTestCard, seedTestWishlist, truncateTable
- No test regressions; baseline `161 passed | 182 skipped` preserved
- All four locked decisions implemented: D-01, D-02, D-04, D-05, D-06; existing root helper untouched (D-03)
</success_criteria>

<output>
After completion, create `.planning/phases/09-api-db-integration-tests/09-01-SUMMARY.md` using the project's summary template (artifacts list, decisions implemented with D-IDs, files created/modified, observable truths verified, and any follow-up notes for plans 09-02 and 09-03).
</output>
