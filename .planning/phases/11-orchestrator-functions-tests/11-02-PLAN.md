---
phase: 11
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/scraper/orchestrator.ts
  - src/scraper/__tests__/orchestrator.test.ts
autonomous: true
requirements: [TEST-13]
must_haves:
  truths:
    - "Type `PriceRecord = { oracleId: string; source: string; priceBrl: number }` is exported from src/scraper/orchestrator.ts (D-07)"
    - "Named export `aggregateResults(oracleId: string, results: AllSourcesResult): PriceRecord[]` exists as a pure function (D-06)"
    - "Named export `batchOrchestrateFetch` aliases `fetchAllPrices` in src/scraper/orchestrator.ts (D-08)"
    - "All 4 `aggregateResults` test.skip stubs (lines 202, 211, 219, 228) become active `test(...)` with real assertions and pass"
    - "All 3 `batchOrchestrateFetch` test.skip stubs (lines 239, 248, 257) become active `test(...)` with real assertions and pass"
    - "All 3 `Integration scenarios` test.skip stubs (lines 268, 277, 286) become active `test(...)` with real assertions and pass"
    - "`pnpm test:run src/scraper/__tests__/orchestrator.test.ts` shows 0 failures and 0 skipped tests across the file"
    - "`pnpm test:run` (full suite) exits 0 — no regressions in any other test file"
    - "fetchAllPrices and fetchCardPriceFromAllSources behavior is unchanged"
  artifacts:
    - path: "src/scraper/orchestrator.ts"
      provides: "PriceRecord interface + aggregateResults + batchOrchestrateFetch alias"
      contains: "export const batchOrchestrateFetch = fetchAllPrices"
    - path: "src/scraper/__tests__/orchestrator.test.ts"
      provides: "10 activated test stubs across 3 describe blocks (aggregateResults, batchOrchestrateFetch, Integration scenarios)"
      contains: "describe('Integration scenarios'"
  key_links:
    - from: "src/scraper/orchestrator.ts aggregateResults"
      to: "AllSourcesResult type (already exported)"
      via: "pure function input parameter"
      pattern: "aggregateResults\\([^,]+,\\s*[^)]+:\\s*AllSourcesResult"
    - from: "src/scraper/orchestrator.ts batchOrchestrateFetch"
      to: "fetchAllPrices"
      via: "literal alias re-export"
      pattern: "^export const batchOrchestrateFetch = fetchAllPrices$"
    - from: "src/scraper/__tests__/orchestrator.test.ts Integration scenarios"
      to: "src/scraper/orchestrator.ts"
      via: "dynamic import with full provider mock set"
      pattern: "vi\\.doMock\\(.@/scraper/providers"
---

<objective>
Add the remaining 2 named exports (`aggregateResults` and `batchOrchestrateFetch`) plus the `PriceRecord` interface to `src/scraper/orchestrator.ts` (per CONTEXT.md D-06/D-07/D-08), then activate the final 10 `test.skip` stubs in `src/scraper/__tests__/orchestrator.test.ts` across the `aggregateResults`, `batchOrchestrateFetch`, and `Integration scenarios` describe blocks.

Purpose: Satisfies TEST-13 — completes the orchestrator's public surface with a pure aggregation helper for converting per-source results into a flat `PriceRecord[]`, an alias for the existing batch entry point, and exercises the three integration scenarios that prove the wired-together system behaves correctly.

Output: Modified orchestrator.ts with 2 new exports + `PriceRecord` type; modified orchestrator.test.ts with the final 10 newly-active passing tests, leaving 0 `test.skip` in the entire file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-orchestrator-functions-tests/11-CONTEXT.md
@.planning/phases/11-orchestrator-functions-tests/11-RESEARCH.md
@.planning/phases/11-orchestrator-functions-tests/11-PATTERNS.md
@.planning/phases/11-orchestrator-functions-tests/11-01-SUMMARY.md

@src/scraper/orchestrator.ts
@src/scraper/__tests__/orchestrator.test.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from src/scraper/orchestrator.ts. Use directly — no codebase exploration needed. -->

From src/scraper/orchestrator.ts (already exported, do not modify):
- `export interface SourceFetchResult { success: boolean; price?: number; error?: string }`
- `export type AllSourcesResult = { [K in 'ligamagic' | 'tcgplayer' | 'cardmarket' | 'cardkingdom']: SourceFetchResult }`
- `export interface FetchAllPricesStats { total: number; fetched: number; skipped: number; failed: number; errors: string[] }`
- `export async function fetchAllPrices(oracleIds: string[]): Promise<FetchAllPricesStats>`

Added by Plan 11-01 (Wave 1) — already present when this plan executes:
- `export const orchestrateFetch = fetchCardPriceFromAllSources`
- `export function handleSourceFailure(source: string, oracleId: string, error: unknown): SourceFetchResult`
- `export async function applyRateLimiting(source: string): Promise<void>`

New exports to ADD in THIS plan (after Plan 11-01's additions at the bottom of orchestrator.ts):
- `export interface PriceRecord { oracleId: string; source: string; priceBrl: number }` (D-07)
- `export function aggregateResults(oracleId: string, results: AllSourcesResult): PriceRecord[]` (D-06)
- `export const batchOrchestrateFetch = fetchAllPrices` (D-08)

PATTERNS.md Pattern G (vi.resetModules + vi.doMock + dynamic import) — applies to batchOrchestrateFetch tests and all 3 Integration scenarios; required mock targets are the same 7 modules used by the existing-passing test at orchestrator.test.ts lines 32-66.

PATTERNS.md Pattern H (pure function tests, no mocks) — applies to all 4 aggregateResults tests; just static import + direct call + object/array assertions.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add PriceRecord type + aggregateResults pure function + activate its 4 test.skip stubs</name>
  <files>src/scraper/orchestrator.ts, src/scraper/__tests__/orchestrator.test.ts</files>
  <read_first>
    - src/scraper/orchestrator.ts (current state — verify Plan 11-01 additions are present: orchestrateFetch, handleSourceFailure, applyRateLimiting)
    - src/scraper/orchestrator.ts lines 75-83 (SourceFetchResult + AllSourcesResult types — already exported, reused as input type)
    - src/scraper/__tests__/orchestrator.test.ts lines 201-236 (aggregateResults describe block; 4 test.skip stubs at lines 202, 211, 219, 228)
    - .planning/phases/11-orchestrator-functions-tests/11-RESEARCH.md §"Pattern 4: aggregateResults — Pure Function" + §"Pitfall 4: allSourcesResult.price Undefined on Success"
  </read_first>
  <behavior>
    - Test 1 (line 202, "should collect prices from all successful sources"): Activate as `test(...)`. Build an AllSourcesResult fixture with 2 successful entries (e.g., ligamagic: { success: true, price: 10 }, tcgplayer: { success: true, price: 50 }) and 2 failed (cardmarket: { success: false, error: 'not found' }, cardkingdom: { success: false, error: 'timeout' }). Call `aggregateResults('oracle-abc', fixture)`. Assert: returned array has length 2; element at index 0 is `{ oracleId: 'oracle-abc', source: 'ligamagic', priceBrl: 10 }`; element at index 1 is `{ oracleId: 'oracle-abc', source: 'tcgplayer', priceBrl: 50 }`.
    - Test 2 (line 211, "should deduplicate prices from same source"): Activate as `test(...)`. The `AllSourcesResult` type structurally guarantees one entry per source key, so true dedup is N/A — instead assert the structural invariant: build a result with all 4 sources successful and assert that each `source` value in the output appears exactly once. Use a Set: `expect(new Set(records.map(r => r.source)).size).toBe(records.length)`.
    - Test 3 (line 219, "should handle partial results (some sources failed)"): Activate as `test(...)`. Build an AllSourcesResult where ligamagic and cardmarket succeed (with prices 15 and 25 respectively) and tcgplayer + cardkingdom fail. Call `aggregateResults('oracle-xyz', fixture)`. Assert: array length is 2; sources are exactly `['ligamagic', 'cardmarket']` (verify both presence and absence); function does NOT throw.
    - Test 4 (line 228, "should return empty array if all sources fail"): Activate as `test(...)`. Build an AllSourcesResult where all 4 sources have `success: false`. Call `aggregateResults('oracle-fail', fixture)`. Assert: returned value is `[]` (length 0), and the function does NOT throw. Also test the edge case where `success: true` but `price` is `undefined` — assert that entry is FILTERED OUT (per RESEARCH Pitfall 4: filter on both `result.success && result.price !== undefined`).
  </behavior>
  <action>
    Step A — Add `PriceRecord` interface to src/scraper/orchestrator.ts.

    1. Open src/scraper/orchestrator.ts. Locate a position near the existing type exports — ideally just after the `AllSourcesResult` type alias (currently around line 83), OR alongside the other new exports at the bottom of the file. Choice is Claude's discretion per CONTEXT.md (the existing type cluster at lines 75-83 is a natural home, but appending at the bottom near `aggregateResults` is equally valid). PATTERNS.md confirms there is no existing `PriceRecord` or `PriceEntry` type in the codebase, so defining it inline is safe.
    2. Add the export:
       - `export interface PriceRecord { oracleId: string; source: string; priceBrl: number }`
       - Property names are exact per D-07: `oracleId` (camelCase, matches existing AllSourcesResult conventions), `source` (string, matches the lowercase source keys in AllSourcesResult), `priceBrl` (number, BRL-denominated since the orchestrator already converts internationals to BRL via convertToBRL).

    Step B — Add `aggregateResults` to src/scraper/orchestrator.ts.

    1. Append after the existing new exports from Plan 11-01:
       - Signature: `export function aggregateResults(oracleId: string, results: AllSourcesResult): PriceRecord[]`. NOT async.
       - Body uses `Object.entries(results)` to iterate the source-keyed entries, filters with the predicate `([, result]) => result.success && result.price !== undefined` (this is critical per RESEARCH Pitfall 4 — `success: true` does NOT guarantee `price` is defined), then maps each entry to `{ oracleId, source, priceBrl: result.price as number }`. The type assertion `as number` is safe because the filter just proved `price !== undefined`.
       - Return type is `PriceRecord[]`. Empty input or all-failed input yields `[]` naturally from the filter.
       - Add a JSDoc block referencing D-06/D-07 for traceability. State explicitly that this is a pure function — no DB writes, no logging.

    Step C — Activate the 4 `aggregateResults` test.skip stubs.

    1. Per PATTERNS.md Pattern H, `aggregateResults` tests need NO mocks — direct import and call. Add a static import at the top of orchestrator.test.ts (merge with the existing/Plan 11-01 imports): `import { aggregateResults, type AllSourcesResult, type PriceRecord } from '@/scraper/orchestrator'` — adjust based on what's already imported.
    2. For each of the 4 stubs (lines 202, 211, 219, 228):
       - Change `test.skip(...)` to `test(...)` keeping the original title.
       - Replace the comment-only body + `expect(true).toBe(false)` with the real assertions per the `<behavior>` block above.
       - Build the AllSourcesResult fixture inline in each test as a typed object literal.
       - Do NOT use `vi.doMock` or `vi.resetModules` for these tests — they are pure-function unit tests. The top-level mocks added in Plan 11-01 (logger, rate-limiter) remain hoisted but have no effect on aggregateResults.
    3. Add a `beforeEach(() => { vi.clearAllMocks() })` to the `aggregateResults` describe block (defensive — keeps the file's pattern consistent even though aggregateResults tests don't touch mocks).
    4. Verify the existing 12 passing tests (5 originals + Plan 11-01's 7) still pass.
  </action>
  <verify>
    <automated>pnpm test:run src/scraper/__tests__/orchestrator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/scraper/orchestrator.ts contains `export interface PriceRecord {` (grep: `grep -nE "^export interface PriceRecord \\{" src/scraper/orchestrator.ts` returns exactly 1 match).
    - The PriceRecord interface has exactly 3 properties named `oracleId`, `source`, `priceBrl` (grep each property name on lines adjacent to the interface declaration).
    - src/scraper/orchestrator.ts contains `export function aggregateResults(oracleId: string, results: AllSourcesResult): PriceRecord[]` (grep: `grep -nE "^export function aggregateResults\\(" src/scraper/orchestrator.ts` returns exactly 1 match).
    - The aggregateResults body contains the dual-condition filter `result.success && result.price !== undefined` (or logically equivalent) — grep: `grep -E "result\\.success.*result\\.price.*undefined" src/scraper/orchestrator.ts` returns at least 1 match.
    - The aggregateResults body does NOT call `insertPrice`, `convertToBRL`, `logger`, or any async function (grep: `grep -E "insertPrice|convertToBRL|await|async" inside the function lines` returns 0 matches within the function body).
    - src/scraper/__tests__/orchestrator.test.ts: the `describe('aggregateResults', ...)` block contains 0 `test.skip(` occurrences.
    - The 4 stubs are now active `test(` calls with the original titles: `should collect prices from all successful sources`, `should deduplicate prices from same source`, `should handle partial results (some sources failed)`, `should return empty array if all sources fail`.
    - No `expect(true).toBe(false)` strings remain in the `aggregateResults` describe block.
    - `pnpm test:run src/scraper/__tests__/orchestrator.test.ts` exits 0 with all currently-active tests passing (5 originals + Plan 11-01's 10 + this task's 4 = 19 passing).
  </acceptance_criteria>
  <done>
    `PriceRecord` interface and `aggregateResults` function exported from orchestrator.ts; aggregateResults is pure (no async, no I/O, no mocks needed); 4 stubs active and passing; full test command green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add batchOrchestrateFetch alias + activate its 3 test.skip stubs</name>
  <files>src/scraper/orchestrator.ts, src/scraper/__tests__/orchestrator.test.ts</files>
  <read_first>
    - src/scraper/orchestrator.ts (current state — verify Plan 11-01 + Task 1 additions are present; locate `export default fetchAllPrices` line and the `aggregateResults` block)
    - src/scraper/orchestrator.ts lines 194-203 (FetchAllPricesStats interface — referenced by the alias)
    - src/scraper/__tests__/orchestrator.test.ts lines 238-265 (batchOrchestrateFetch describe block; 3 test.skip stubs at lines 239, 248, 257)
    - src/scraper/__tests__/orchestrator.test.ts lines 29-67 (existing-passing `preserves FetchAllPricesStats return shape` test — use it as the mock-setup template per PATTERNS.md Pattern G)
  </read_first>
  <behavior>
    - Test 1 (line 239, "should orchestrate fetch for multiple cards efficiently"): Activate as `test(...)`. Use `vi.resetModules() + vi.doMock(...)` to mock the 7 standard modules (`@/scraper/smart-refresh`, `@/scraper/providers/liga-magic`, `@/scraper/providers/tcgplayer`, `@/scraper/providers/cardmarket`, `@/scraper/providers/cardkingdom`, `@/db/queries/prices`, `@/lib/currency`). Mock all 4 providers to resolve to numbers. Dynamically import `batchOrchestrateFetch` and call it with an array of 3 oracle IDs (e.g., `['a', 'b', 'c']`). Assert: the returned stats object has `total === 3`; `fetched` is a number `>= 0`; `errors` is an Array. Wrap up with `vi.resetModules()`.
    - Test 2 (line 248, "should parallelize across cards within rate limits"): Activate as `test(...)`. Same mock structure as Test 1. Call `batchOrchestrateFetch(['a', 'b', 'c', 'd'])`. Assert: each of the 4 provider mocks (liga-magic, tcgplayer, cardmarket, cardkingdom) was called 4 times (once per card). This proves the batch fans out across all cards. Time-based parallelism is NOT asserted (the underlying `fetchAllPrices` already has a separate concurrency test at lines 5-27 — this test verifies the alias forwards all card IDs).
    - Test 3 (line 257, "should provide progress updates for large batches"): Activate as `test(...)`. Same mock structure as Test 1, but also assert against the mocked `logger.info` (from the top-level logger mock added in Plan 11-01 Task 2). Call `batchOrchestrateFetch(['a', 'b', 'c'])`. Assert: `logger.info` was called at least once with a string that includes `'price collection'` (case-insensitive — `fetchAllPrices` logs "Starting price collection..." and "Price collection complete:..."). This proves the alias forwards through the underlying progress logging.
  </behavior>
  <action>
    Step A — Add `batchOrchestrateFetch` alias to src/scraper/orchestrator.ts.

    1. Open src/scraper/orchestrator.ts. Locate the bottom of the file where Plan 11-01 added `orchestrateFetch`, `handleSourceFailure`, `applyRateLimiting` and Task 1 added `aggregateResults` (the `export default fetchAllPrices` line remains as-is, currently around the original line 271 but shifted down by prior additions).
    2. Append a new line: `export const batchOrchestrateFetch = fetchAllPrices`. This is the literal alias per D-08 — no body, no JSDoc beyond an optional one-line comment. Place it alongside the `orchestrateFetch` alias for visual consistency.
    3. Do NOT change `fetchAllPrices` itself. Do NOT remove `export default fetchAllPrices`.

    Step B — Activate the 3 `batchOrchestrateFetch` test stubs.

    1. For each of the 3 stubs in the `batchOrchestrateFetch` describe block (lines 239, 248, 257):
       - Change `test.skip(...)` to `test(...)` keeping the original title.
       - Replace the comment-only body with a body that follows the Pattern G template (PATTERNS.md): `vi.resetModules()` at the start, the 7 `vi.doMock(...)` calls for the provider+infrastructure modules, dynamic `const { batchOrchestrateFetch } = await import('@/scraper/orchestrator')`, then the assertions, then `vi.resetModules()` at the end.
       - Vary the mock return values per the `<behavior>` block. For Test 2 (parallelization), capture the provider mocks as named `vi.fn()` instances BEFORE the `vi.doMock` call so they can be referenced in assertions (e.g., `const tcgMock = vi.fn().mockResolvedValue(50); vi.doMock('@/scraper/providers/tcgplayer', () => ({ fetchCardPrice: tcgMock })); ...; expect(tcgMock).toHaveBeenCalledTimes(4)`).
       - For Test 3 (progress updates), import `logger` from `@/lib/logger` at the top of the test file (or merge with existing imports) and use `vi.mocked(logger.info)` to assert against the calls. Since the top-level `vi.mock('@/lib/logger', ...)` from Plan 11-01 hoists, `logger.info` is already a `vi.fn()`.
    2. Add a `beforeEach(() => { vi.clearAllMocks() })` to the `batchOrchestrateFetch` describe block.
    3. CRITICAL: The dynamic-import inside each test means the test will pick up whatever the top-level mocks specify for logger and rate-limiter. The rate-limiter mock from Plan 11-01 returns `{ allowed: true, ... }` by default, so `fetchAllPrices` → `fetchCardPriceFromAllSources` will not throw during these tests. Verify by running the test command.
    4. The provider modules in this file may use Node ESM resolution that differs from the `@/scraper/providers/*` alias used in the existing line-32-66 test. Mirror the existing test's import paths exactly — if it uses `'@/scraper/providers/liga-magic'`, use that. If a typecheck fails because of an alias mismatch, fall back to the relative path `'../providers/liga-magic'` (verify against the file's tsconfig path aliases).
  </action>
  <verify>
    <automated>pnpm test:run src/scraper/__tests__/orchestrator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/scraper/orchestrator.ts contains the literal line `export const batchOrchestrateFetch = fetchAllPrices` (grep: `grep -nE "^export const batchOrchestrateFetch = fetchAllPrices$" src/scraper/orchestrator.ts` returns exactly 1 match).
    - src/scraper/__tests__/orchestrator.test.ts: the `describe('batchOrchestrateFetch', ...)` block contains 0 `test.skip(` occurrences.
    - The 3 stubs are now active `test(` calls with the original titles: `should orchestrate fetch for multiple cards efficiently`, `should parallelize across cards within rate limits`, `should provide progress updates for large batches`.
    - Each of the 3 tests contains a `const { batchOrchestrateFetch } = await import('@/scraper/orchestrator')` dynamic-import line (or equivalent — they all need fresh module state per Pattern G).
    - No `expect(true).toBe(false)` strings remain in the `batchOrchestrateFetch` describe block.
    - `pnpm test:run src/scraper/__tests__/orchestrator.test.ts` exits 0 with all currently-active tests passing (5 originals + Plan 11-01's 10 + Task 1's 4 + this task's 3 = 22 passing).
  </acceptance_criteria>
  <done>
    `batchOrchestrateFetch` alias exported from orchestrator.ts; 3 stubs active and passing with mocked provider/infrastructure modules; full test command green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Activate 3 Integration scenarios test.skip stubs</name>
  <files>src/scraper/__tests__/orchestrator.test.ts</files>
  <read_first>
    - src/scraper/orchestrator.ts (current state — all 5 new exports plus existing functions should be present from Plan 11-01 + Task 1 + Task 2)
    - src/scraper/__tests__/orchestrator.test.ts lines 267-294 (Integration scenarios describe block; 3 test.skip stubs at lines 268, 277, 286)
    - src/scraper/__tests__/orchestrator.test.ts lines 29-67 (existing passing test — template for full integration mock setup)
    - .planning/phases/11-orchestrator-functions-tests/11-RESEARCH.md §"Open Questions" item 2 (resolves "complete large batch within time budget" by mocking providers to return immediately and asserting the stats shape — NOT enforcing literal 30-minute timing)
  </read_first>
  <behavior>
    - Test 1 (line 268, "should fetch single card from all sources"): Activate as `test(...)`. Use the Pattern G dynamic-mock setup with all 4 providers resolving to numbers (e.g., ligamagic=10, tcgplayer=20, cardmarket=30, cardkingdom=40). Dynamically import `orchestrateFetch` (the alias added in Plan 11-01) and call it with a single oracle ID. Assert: the result is an AllSourcesResult with all 4 keys present (`ligamagic`, `tcgplayer`, `cardmarket`, `cardkingdom`); each entry has `success: true` and a numeric `price`. This proves the wired-together orchestrator successfully composes smart-refresh + Liga Magic sequential + 3 international parallel + currency conversion + DB insert.
    - Test 2 (line 277, "should handle mixed success/failure across sources"): Activate as `test(...)`. Mock ligamagic and cardmarket to resolve to numbers; mock tcgplayer and cardkingdom to throw errors (e.g., `vi.fn().mockRejectedValue(new Error('network error'))`). Call `orchestrateFetch('oracle-mix')`. Assert: `result.ligamagic.success === true` AND `result.cardmarket.success === true` AND `result.tcgplayer.success === false` AND `result.cardkingdom.success === false`. Verify the 2 failed entries have non-empty `error` strings. This proves partial-failure tolerance (the original stub comment says "Returns 2 price objects" — interpret that semantically: 2 successful entries out of 4).
    - Test 3 (line 286, "should complete large batch within time budget"): Activate as `test(...)`. Per RESEARCH Open Question 2, do NOT literally process 1000 cards or assert a 30-minute wall-clock budget. Mock all 4 providers to resolve immediately. Call `batchOrchestrateFetch(['a', 'b', 'c'])` (3 cards is sufficient — the test verifies the FetchAllPricesStats shape and that no per-card overhead is artificially blocked). Assert: `stats.total === 3`; `stats.fetched` is a number; `stats.errors` is an Array; the function returns within a generous time bound (e.g., `expect(durationMs).toBeLessThan(5000)` — well under 5 seconds since everything is mocked and immediate).
  </behavior>
  <action>
    Step A — Activate the 3 Integration scenario test.skip stubs.

    1. For each of the 3 stubs in the `Integration scenarios` describe block (lines 268, 277, 286):
       - Change `test.skip(...)` to `test(...)` keeping the original title.
       - Replace the comment-only body with a real body following PATTERNS.md Pattern G (dynamic mock + dynamic import + assertions + `vi.resetModules()` at end).
       - Reuse the 7-module mock structure from the existing-passing test at lines 32-66.
       - Per CONTEXT.md Claude's Discretion item, integration tests use the `orchestrateFetch` alias (D-03) and `batchOrchestrateFetch` alias (D-08) to exercise the public API surface, not the underlying functions directly. This validates that the aliases work end-to-end.
       - For Test 2, configure 2 of the 4 provider mocks to throw via `.mockRejectedValue(new Error('...'))`. Verify the orchestrator's existing catch logic (lines 121-137 and 172-176 of orchestrator.ts) surfaces those as `{ success: false, error: ... }` entries.
       - For Test 3, wrap the call in a duration measurement using `performance.now()`: `const t0 = performance.now(); await batchOrchestrateFetch([...]); const durationMs = performance.now() - t0`. Assert `durationMs < 5000`.
    2. Add a `beforeEach(() => { vi.clearAllMocks() })` to the `Integration scenarios` describe block.
    3. Re-confirm dynamic imports use the same path style as the existing passing test (`@/scraper/orchestrator`).

    Step B — Full test file verification.

    1. Run `pnpm test:run src/scraper/__tests__/orchestrator.test.ts`. Confirm 0 failures and 0 skipped tests (the entire file is now active).
    2. Run `pnpm test:run` to confirm no regression in any other test file. The top-level mocks added in Plan 11-01 (logger, rate-limiter) are scoped to this test file and should not affect others.
    3. If the project has a separate typecheck command (`pnpm typecheck` or `pnpm exec tsc --noEmit`), run it and confirm exit 0.

    Step C — Final cleanup.

    1. Grep the entire orchestrator.test.ts file: `grep -c "test.skip(" src/scraper/__tests__/orchestrator.test.ts` MUST return 0. If non-zero, identify and activate any stragglers.
    2. Grep for the placeholder assertion: `grep -c "expect(true).toBe(false)" src/scraper/__tests__/orchestrator.test.ts` MUST return 0.
  </action>
  <verify>
    <automated>pnpm test:run src/scraper/__tests__/orchestrator.test.ts && pnpm test:run</automated>
  </verify>
  <acceptance_criteria>
    - src/scraper/__tests__/orchestrator.test.ts: the `describe('Integration scenarios', ...)` block contains 0 `test.skip(` occurrences.
    - The 3 stubs are now active `test(` calls with the original titles: `should fetch single card from all sources`, `should handle mixed success/failure across sources`, `should complete large batch within time budget`.
    - The entire orchestrator.test.ts file has 0 `test.skip(` occurrences (verify with `grep -c "test.skip(" src/scraper/__tests__/orchestrator.test.ts` returning 0).
    - The entire orchestrator.test.ts file has 0 `expect(true).toBe(false)` occurrences (verify with `grep -c "expect(true).toBe(false)" src/scraper/__tests__/orchestrator.test.ts` returning 0).
    - Each of the 3 integration tests contains a `const { (orchestrateFetch|batchOrchestrateFetch) } = await import('@/scraper/orchestrator')` dynamic-import line.
    - Test 3 measures duration with `performance.now()` and asserts `durationMs < 5000` (or equivalent — verify by reading the test body).
    - `pnpm test:run src/scraper/__tests__/orchestrator.test.ts` exits 0 with all 25 tests passing (5 originals + Plan 11-01's 10 + Task 1's 4 + Task 2's 3 + this task's 3 = 25).
    - `pnpm test:run` (full suite) exits 0 — no other test file regressed.
    - Project typecheck (if a script exists, e.g., `pnpm typecheck` or equivalent in package.json) exits 0.
  </acceptance_criteria>
  <done>
    All 3 Integration scenarios stubs active and passing; the entire orchestrator.test.ts file has 0 skipped tests; full project test suite green; TypeScript compiles clean.
  </done>
</task>

</tasks>

<verification>
After all 3 tasks complete:

1. **Required exports present in src/scraper/orchestrator.ts:**
   - `grep -nE "^export interface PriceRecord \\{" src/scraper/orchestrator.ts` → 1 match
   - `grep -nE "^export function aggregateResults\\(" src/scraper/orchestrator.ts` → 1 match
   - `grep -nE "^export const batchOrchestrateFetch = fetchAllPrices$" src/scraper/orchestrator.ts` → 1 match

2. **Combined with Plan 11-01, all 5 named exports exist:**
   - `grep -c "^export\\s\\+\\(const\\|function\\|async function\\|interface\\)\\s\\+\\(orchestrateFetch\\|handleSourceFailure\\|applyRateLimiting\\|aggregateResults\\|batchOrchestrateFetch\\|PriceRecord\\)" src/scraper/orchestrator.ts` → 6 matches (5 functions/aliases + 1 interface)

3. **No skipped stubs anywhere in the file:**
   - `grep -c "test.skip(" src/scraper/__tests__/orchestrator.test.ts` → 0
   - `grep -c "expect(true).toBe(false)" src/scraper/__tests__/orchestrator.test.ts` → 0

4. **Tests pass:**
   - `pnpm test:run src/scraper/__tests__/orchestrator.test.ts` exits 0
   - The 3 target describe blocks (aggregateResults, batchOrchestrateFetch, Integration scenarios) contribute 10 newly-passing tests
   - Total: 25 active tests, all passing

5. **No regressions:**
   - `pnpm test:run` (full suite) exits 0
   - The 5 originally-passing tests at orchestrator.test.ts lines 1-99 still pass
   - The 10 tests activated by Plan 11-01 still pass

6. **TypeScript clean:**
   - Project typecheck command exits 0
   - PriceRecord type is structurally compatible with future consumers (oracleId/source/priceBrl naming matches existing AllSourcesResult conventions)
</verification>

<success_criteria>
- TEST-13 requirement is satisfied: `aggregateResults`, `batchOrchestrateFetch`, and the `PriceRecord` interface exist as named exports in src/scraper/orchestrator.ts; their 7 unit-test stubs are active and passing; the 3 `Integration scenarios` stubs are active and passing.
- The entire src/scraper/__tests__/orchestrator.test.ts file has 0 `test.skip(` calls remaining — Phase 11 closes the file.
- The 5 pre-existing passing tests (orchestrator.test.ts lines 1-99) and the `fetchCardPriceFromAllSources` / `fetchAllPrices` function contracts are unchanged.
- Project test suite (`pnpm test:run`) is green; project typecheck is green.
- Phase 11 success criteria from ROADMAP.md are all satisfied:
  1. `orchestrateFetch`, `handleSourceFailure`, `applyRateLimiting` exist and are exported (Plan 11-01)
  2. `aggregateResults`, `batchOrchestrateFetch` exist and are exported (this plan)
  3. TEST-12 stubs all pass (Plan 11-01)
  4. TEST-13 stubs all pass (this plan)
  5. fetchCardPriceFromAllSources and fetchAllPrices contracts unchanged (verified in both plans)
</success_criteria>

<output>
After completion, create `.planning/phases/11-orchestrator-functions-tests/11-02-SUMMARY.md` per the standard summary template, documenting:
- The 2 new exports added (PriceRecord interface, aggregateResults function, batchOrchestrateFetch alias) with line ranges in orchestrator.ts
- The 10 newly-active tests across the aggregateResults (4), batchOrchestrateFetch (3), and Integration scenarios (3) describe blocks
- Confirmation that the entire orchestrator.test.ts file now has 0 `test.skip` occurrences
- Confirmation that fetchAllPrices and fetchCardPriceFromAllSources behavior is unchanged
- The RESEARCH Open Question 2 resolution: large-batch test asserts FetchAllPricesStats shape on a small mocked batch, not literal 1000-card or 30-min wall-clock budget
- Phase 11 ROADMAP success criteria checklist marked complete
</output>
