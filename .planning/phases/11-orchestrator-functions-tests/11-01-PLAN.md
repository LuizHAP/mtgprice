---
phase: 11
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scraper/orchestrator.ts
  - src/scraper/__tests__/orchestrator.test.ts
autonomous: true
requirements: [TEST-12]
must_haves:
  truths:
    - "Named export `orchestrateFetch` exists in src/scraper/orchestrator.ts and aliases fetchCardPriceFromAllSources (D-03)"
    - "Named export `handleSourceFailure(source, oracleId, error): SourceFetchResult` exists and logs via logger.error (D-04)"
    - "Named export `applyRateLimiting(source): Promise<void>` exists and calls checkRateLimitPreset(source, RATE_LIMITS.X) for known sources (D-05 + RESEARCH critical finding)"
    - "All 4 `orchestrateFetch` test.skip stubs (lines 103, 112, 121, 131) become active `test(...)` with real assertions and pass"
    - "All 3 `handleSourceFailure` test.skip stubs (lines 142, 152, 161) become active `test(...)` with real assertions and pass"
    - "All 3 `applyRateLimiting` test.skip stubs (lines 172, 182, 191) become active `test(...)` with real assertions and pass"
    - "`pnpm test:run src/scraper/__tests__/orchestrator.test.ts` shows 0 failures for the 3 describe blocks (orchestrateFetch, handleSourceFailure, applyRateLimiting)"
    - "The 5 existing passing tests at lines 1-99 are NOT modified (Pitfall #5)"
    - "fetchCardPriceFromAllSources signature and behavior unchanged"
  artifacts:
    - path: "src/scraper/orchestrator.ts"
      provides: "3 new named exports: orchestrateFetch (alias), handleSourceFailure (new), applyRateLimiting (new) + SOURCE_RATE_LIMIT_MAP"
      contains: "export const orchestrateFetch = fetchCardPriceFromAllSources"
    - path: "src/scraper/__tests__/orchestrator.test.ts"
      provides: "10 activated test stubs across 3 describe blocks"
      contains: "describe('applyRateLimiting'"
  key_links:
    - from: "src/scraper/orchestrator.ts applyRateLimiting"
      to: "src/lib/ratelimit/rate-limiter.ts checkRateLimitPreset"
      via: "two-argument call with RATE_LIMITS preset"
      pattern: "checkRateLimitPreset\\([^,]+,\\s*[A-Z_]+\\."
    - from: "src/scraper/orchestrator.ts handleSourceFailure"
      to: "src/lib/logger.ts logger.error"
      via: "error normalization + log call"
      pattern: "logger\\.error\\(`✗ \\${source}: \\${oracleId}"
    - from: "src/scraper/__tests__/orchestrator.test.ts"
      to: "src/scraper/orchestrator.ts"
      via: "named imports of orchestrateFetch, handleSourceFailure, applyRateLimiting"
      pattern: "(orchestrateFetch|handleSourceFailure|applyRateLimiting)"
---

<objective>
Add three new named exports to `src/scraper/orchestrator.ts` (per CONTEXT.md D-03/D-04/D-05) and activate the 10 corresponding `test.skip` stubs in `src/scraper/__tests__/orchestrator.test.ts` so they run as real tests and pass.

Purpose: Satisfies TEST-12 — provides `orchestrateFetch`, `handleSourceFailure`, and `applyRateLimiting` as first-class named exports of the orchestrator module, with active unit tests covering each. The orchestrator's public API expands without changing the existing `fetchCardPriceFromAllSources` or `fetchAllPrices` contracts.

Output: Modified orchestrator.ts with 3 new exports + SOURCE_RATE_LIMIT_MAP helper; modified orchestrator.test.ts with 10 newly-active passing tests across the `orchestrateFetch`, `handleSourceFailure`, and `applyRateLimiting` describe blocks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-orchestrator-functions-tests/11-CONTEXT.md
@.planning/phases/11-orchestrator-functions-tests/11-RESEARCH.md
@.planning/phases/11-orchestrator-functions-tests/11-PATTERNS.md

@src/scraper/orchestrator.ts
@src/scraper/__tests__/orchestrator.test.ts
@src/lib/ratelimit/rate-limiter.ts
@src/lib/logger.ts
@src/scheduler/__tests__/jobs.test.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from src/scraper/orchestrator.ts and src/lib/ratelimit/rate-limiter.ts. Use directly — no codebase exploration needed. -->

From src/scraper/orchestrator.ts (already exported, do not modify):
- `export interface SourceFetchResult { success: boolean; price?: number; error?: string }`
- `export type AllSourcesResult = { [K in 'ligamagic' | 'tcgplayer' | 'cardmarket' | 'cardkingdom']: SourceFetchResult }`
- `export async function fetchCardPriceFromAllSources(oracleId: string): Promise<AllSourcesResult>`

From src/lib/ratelimit/rate-limiter.ts (already exported, do not modify):
- `export interface RateLimitConfig { limit: number; interval: number }`
- `export interface RateLimitResult { allowed: boolean; remaining: number; resetAt: number }` (verify shape via Read)
- `export const RATE_LIMITS = { SCRYFALL, SCRYFALL_HEAVY, TELEGRAM, TCGPLAYER, CARDMARKET, CARDKINGDOM, LIGAMAGIC } as const`
- `export async function checkRateLimitPreset(key: string, preset: RateLimitConfig, tokens?: number): Promise<RateLimitResult>` — TWO REQUIRED ARGS

From src/lib/logger.ts (already exported, do not modify):
- `export const logger = winston.createLogger(...)` — `logger.error(message: string)` available

New exports to ADD at the bottom of src/scraper/orchestrator.ts (after existing `export default fetchAllPrices` on line 271):
- `export const orchestrateFetch = fetchCardPriceFromAllSources` (alias, D-03)
- `export function handleSourceFailure(source: string, oracleId: string, error: unknown): SourceFetchResult` (D-04)
- `export async function applyRateLimiting(source: string): Promise<void>` (D-05)
- internal const `SOURCE_RATE_LIMIT_MAP: Record<string, RateLimitConfig>` mapping `ligamagic|tcgplayer|cardmarket|cardkingdom` to the corresponding `RATE_LIMITS.*` preset (case-insensitive lookup)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add orchestrateFetch alias + activate its 4 test.skip stubs</name>
  <files>src/scraper/orchestrator.ts, src/scraper/__tests__/orchestrator.test.ts</files>
  <read_first>
    - src/scraper/orchestrator.ts (current state; new export appended at end-of-file after line 271)
    - src/scraper/__tests__/orchestrator.test.ts (lines 101-139 contain the `orchestrateFetch` describe block with 4 test.skip stubs at lines 103, 112, 121, 131; lines 1-99 are UNTOUCHABLE per Pitfall #5)
    - src/lib/logger.ts (logger.error signature to verify mock shape)
    - src/scraper/providers/liga-magic.ts, tcgplayer.ts, cardmarket.ts, cardkingdom.ts (only to verify `fetchCardPrice` export name — already verified in PATTERNS.md, no edits)
    - src/scheduler/__tests__/jobs.test.ts (Pattern E reference for mocking pattern)
  </read_first>
  <behavior>
    - Test 1 (line 103, "should fetch Liga Magic first (BR data priority)"): Activate as `test(...)`. Use the existing-passing pattern from orchestrator.test.ts lines 32-66 — `vi.resetModules()` + `vi.doMock` for `@/scraper/smart-refresh`, all 4 providers, `@/db/queries/prices`, `@/lib/currency`. Import `orchestrateFetch` dynamically. Provide Liga Magic mock that resolves to a number, others to `null`. Assert: `results.ligamagic.success === true`, `results.ligamagic.price === 10` (or BRL value matching the Liga Magic mock since Liga Magic returns BRL directly without conversion).
    - Test 2 (line 112, "should fetch international sources in parallel"): Activate as `test(...)`. Mock all 4 providers to resolve to numbers; spy on at least 2 international `fetchCardPrice` mocks. Assert: all 3 international entries in result have `success: true`. Time-based parallelism assertions are NOT required — sufficient to assert that all 3 mocks were called within the same orchestrateFetch invocation (use `vi.fn` call records, e.g., `expect(tcgPlayerMock).toHaveBeenCalledTimes(1)` and same for cardmarket/cardkingdom).
    - Test 3 (line 121, "should continue if one international source fails"): Activate as `test(...)`. Mock Liga Magic + 2 of the 3 international providers to resolve to numbers; mock the third (e.g., tcgplayer) to throw. Assert: `results.ligamagic.success === true`, `results.cardmarket.success === true`, `results.cardkingdom.success === true`, `results.tcgplayer.success === false` and `results.tcgplayer.error` is a non-empty string.
    - Test 4 (line 131, "should handle complete Liga Magic failure gracefully"): Activate as `test(...)`. Mock Liga Magic provider to throw; mock the 3 international providers to resolve to numbers. Assert: `results.ligamagic.success === false` with a non-empty `error`, AND the 3 international results all have `success: true`. This verifies international sources continue even when Liga Magic blows up.
  </behavior>
  <action>
    Step A — Add `orchestrateFetch` alias to src/scraper/orchestrator.ts.

    1. Open src/scraper/orchestrator.ts. Locate the existing final line `export default fetchAllPrices` (currently line 271).
    2. Append a new line immediately after it: `export const orchestrateFetch = fetchCardPriceFromAllSources`. This is the literal alias per D-03 — no body, no JSDoc beyond an optional one-line comment.
    3. Do NOT change `fetchCardPriceFromAllSources` itself. Do NOT remove `export default fetchAllPrices`. Do NOT reorder existing code.

    Step B — Activate the 4 `orchestrateFetch` test stubs (lines 103, 112, 121, 131 of orchestrator.test.ts).

    1. The 5 tests at lines 1-99 are UNTOUCHABLE. Start editing at line 101 (`describe('Fetch orchestration'`).
    2. For each of the 4 stubs in the `orchestrateFetch` describe block:
       - Change `test.skip(...)` to `test(...)` keeping the same title.
       - Replace the comment-only body (and the `expect(true).toBe(false)` line) with a real test body using the `vi.resetModules() + vi.doMock(...)` + dynamic-import pattern documented in the file's existing test at lines 32-66.
       - Mock targets: `@/scraper/smart-refresh` → `shouldFetchPrice: vi.fn().mockResolvedValue(true)`; `@/scraper/providers/liga-magic` → `fetchCardPrice`; `@/scraper/providers/tcgplayer` → `fetchCardPrice`; `@/scraper/providers/cardmarket` → `fetchCardPrice`; `@/scraper/providers/cardkingdom` → `fetchCardPrice`; `@/db/queries/prices` → `insertPrice: vi.fn().mockResolvedValue(undefined)`; `@/lib/currency` → `convertToBRL: vi.fn().mockResolvedValue(50)`.
       - Vary the provider mock return values per the `<behavior>` block above (number for success, `null` for "price not found", or `.mockRejectedValue(new Error('...'))` for throw cases).
       - At the end of each test, call `vi.resetModules()` to prevent mock bleeding (Pitfall #3).
       - Import the function under test via `const { orchestrateFetch } = await import('@/scraper/orchestrator')` INSIDE the test, after the `vi.doMock` calls.
    3. Add a `beforeEach(() => { vi.clearAllMocks() })` to the `orchestrateFetch` describe block if not already present (it currently is not — confirm in the file).
    4. Do not delete or rename the surrounding `describe('orchestrateFetch', () => { ... })` block.
  </action>
  <verify>
    <automated>pnpm test:run src/scraper/__tests__/orchestrator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/scraper/orchestrator.ts contains the literal line `export const orchestrateFetch = fetchCardPriceFromAllSources` (grep: `grep -n "^export const orchestrateFetch = fetchCardPriceFromAllSources$" src/scraper/orchestrator.ts` returns exactly 1 match).
    - src/scraper/__tests__/orchestrator.test.ts: the `describe('orchestrateFetch', ...)` block on or after line 102 contains 0 `test.skip(` occurrences (grep within the block).
    - The 4 stubs are now active `test(` calls with the original titles: `should fetch Liga Magic first (BR data priority)`, `should fetch international sources in parallel`, `should continue if one international source fails`, `should handle complete Liga Magic failure gracefully`.
    - Lines 1-99 of orchestrator.test.ts are byte-identical to before this task (`git diff src/scraper/__tests__/orchestrator.test.ts` must not show any change above line 100; verify with `git diff -U0 src/scraper/__tests__/orchestrator.test.ts | grep -E "^@@.*\\+[0-9]+" | awk -F'+' '{print $2}'` showing only line ranges >= 100).
    - `pnpm test:run src/scraper/__tests__/orchestrator.test.ts` exits 0 with the 4 new `orchestrateFetch` tests passing AND the original 5 tests at lines 1-99 still passing.
    - No `expect(true).toBe(false)` strings remain in the `orchestrateFetch` describe block.
  </acceptance_criteria>
  <done>
    `orchestrateFetch` alias exported from orchestrator.ts; 4 test.skip stubs in the `orchestrateFetch` describe block are active `test(...)` calls with real assertions; `pnpm test:run src/scraper/__tests__/orchestrator.test.ts` passes; lines 1-99 of the test file are unchanged.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add handleSourceFailure + activate its 3 test.skip stubs</name>
  <files>src/scraper/orchestrator.ts, src/scraper/__tests__/orchestrator.test.ts</files>
  <read_first>
    - src/scraper/orchestrator.ts lines 133-136 (existing Liga Magic catch block — error message format to mirror)
    - src/scraper/orchestrator.ts lines 75-79 (SourceFetchResult type definition)
    - src/scraper/__tests__/orchestrator.test.ts lines 141-169 (handleSourceFailure describe block; 3 test.skip stubs at lines 142, 152, 161)
    - src/lib/logger.ts (logger.error API)
    - src/scheduler/__tests__/jobs.test.ts lines 38-45 (logger mock pattern reference per PATTERNS.md Pattern E)
  </read_first>
  <behavior>
    - Test 1 (line 142, "should log error with source context"): Activate as `test(...)`. Call `handleSourceFailure('tcgplayer', 'oracle-abc', new Error('boom'))`. Assert: returned value equals `{ success: false, error: 'boom' }`. Assert: `logger.error` (the top-of-file mock) was called exactly once with a string that contains both `'tcgplayer'` and `'oracle-abc'` and `'boom'`. Stack-trace inclusion is NOT required (D-04 keeps it simple — error.message is sufficient).
    - Test 2 (line 152, "should continue with remaining sources"): Activate as `test(...)`. Call `handleSourceFailure('cardmarket', 'oracle-xyz', new Error('network down'))` and assert it does NOT throw (use `expect(() => handleSourceFailure(...)).not.toThrow()` or call it directly inside the test body and let absence of throw be the implicit pass). Additionally assert the returned object has `success: false` and `error: 'network down'`. The "continues orchestration" semantics are inherent — the function returns a SourceFetchResult instead of throwing, allowing callers to keep going.
    - Test 3 (line 161, "should track failure count per source"): Activate as `test(...)`. Per D-04 + RESEARCH (Don't Hand-Roll table row 4), `handleSourceFailure` does NOT maintain its own counter — Opossum handles circuit breaking. Satisfy the stub by calling `handleSourceFailure` twice with the same source ('tcgplayer') and different oracleIds, then asserting that `logger.error` was called twice with messages that each include `'tcgplayer'`. This proves per-source failure events are logged (which is what monitoring would consume).
  </behavior>
  <action>
    Step A — Add `handleSourceFailure` to src/scraper/orchestrator.ts.

    1. Open src/scraper/orchestrator.ts. After the `orchestrateFetch` alias line added in Task 1 (or alongside it if Task 1's alias goes first), append the new function. Both new symbols sit at the bottom of the file after `export default fetchAllPrices`.
    2. Add the implementation:
       - Name: `handleSourceFailure`. Signature: `(source: string, oracleId: string, error: unknown): SourceFetchResult`. NOT async.
       - Body normalizes the error as `const errorMsg = error instanceof Error ? error.message : String(error)` — identical to the existing pattern at lines 134 and 173 of orchestrator.ts.
       - Then calls `logger.error(\`✗ \${source}: \${oracleId} - \${errorMsg}\`)` — same template-literal shape as line 136.
       - Returns `{ success: false, error: errorMsg }`.
    3. The function MUST be a named `export function` (not `export const`) so it can be re-mocked cleanly.
    4. Do NOT add a counter, file-scoped state, or any persistence. D-04 + "Don't Hand-Roll" row 4 prohibit it.

    Step B — Activate the 3 `handleSourceFailure` test stubs (lines 142, 152, 161 of orchestrator.test.ts).

    1. At the top of orchestrator.test.ts (above line 4 where `describe(...)` starts), add a top-level `vi.mock('@/lib/logger', () => ({ logger: { info: vi.fn(), warn: vi.fn(), error: vi.fn(), debug: vi.fn() } }))` IF AND ONLY IF the existing-passing tests in lines 1-99 do not already mock the logger (they currently do not — verify in Read). Place the new mock at the very top after the existing imports. This is the Pattern E (PATTERNS.md) top-level mock.
    2. For each of the 3 stubs in the `handleSourceFailure` describe block (lines 142, 152, 161):
       - Change `test.skip(...)` to `test(...)` keeping the same title.
       - Replace the comment-only body with a real test body per the `<behavior>` block above.
       - Use a direct named import at the top of the test file: `import { handleSourceFailure } from '@/scraper/orchestrator'` (or merge into existing imports). Note: this is a normal static import — `handleSourceFailure` is a synchronous pure-ish function so no dynamic import or `vi.resetModules` is needed.
       - To access the mocked `logger.error` for assertions, import it: `import { logger } from '@/lib/logger'` and assert `expect(vi.mocked(logger.error)).toHaveBeenCalled...`.
    3. Add a `beforeEach(() => { vi.clearAllMocks() })` to the `handleSourceFailure` describe block.
    4. CRITICAL: The top-level `vi.mock('@/lib/logger', ...)` must not break the existing 5 passing tests in lines 1-99. The orchestrator.ts already imports `logger` directly, so mocking the module hoists before any import. Verify post-edit by running the test command. If the existing tests break (they should not, since they don't assert anything about logger calls), wrap the mock such that the logger surface still exposes all 4 methods (info/warn/error/debug) as `vi.fn()`.

    Step C — Confirm acceptance.

    Run `pnpm test:run src/scraper/__tests__/orchestrator.test.ts` and verify 0 failures across all the active tests in the file (the 5 originals + Task 1's 4 + this task's 3 = 12 passing tests).
  </action>
  <verify>
    <automated>pnpm test:run src/scraper/__tests__/orchestrator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/scraper/orchestrator.ts contains `export function handleSourceFailure(` (grep: `grep -nE "^export function handleSourceFailure\\(" src/scraper/orchestrator.ts` returns exactly 1 match).
    - The function body contains both `error instanceof Error ? error.message : String(error)` AND a `logger.error(` call (grep both patterns within the function's lines).
    - The function returns an object literal containing `success: false` (grep: `grep -nE "success:\\s*false" src/scraper/orchestrator.ts` returns at least one match inside the new function — verify by reading the file after edit).
    - src/scraper/__tests__/orchestrator.test.ts: the `describe('handleSourceFailure', ...)` block contains 0 `test.skip(` occurrences.
    - The 3 stubs are now active `test(` calls with the original titles: `should log error with source context`, `should continue with remaining sources`, `should track failure count per source`.
    - A top-level `vi.mock('@/lib/logger', ...)` exists in orchestrator.test.ts that mocks all 4 logger methods (info, warn, error, debug) as `vi.fn()`.
    - No `expect(true).toBe(false)` strings remain in the `handleSourceFailure` describe block.
    - `pnpm test:run src/scraper/__tests__/orchestrator.test.ts` exits 0 with all currently-active tests passing, including the 5 originals at lines 1-99.
    - Lines 1-99 of the test file (excluding the new top-level imports/mocks added at the very top BEFORE existing test code) are functionally unchanged: the 3 existing test bodies are byte-identical.
  </acceptance_criteria>
  <done>
    `handleSourceFailure` exported from orchestrator.ts as a named `export function` returning SourceFetchResult; logger is mocked at top of test file; 3 stubs active and passing; full test command green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add applyRateLimiting + SOURCE_RATE_LIMIT_MAP + activate its 3 test.skip stubs</name>
  <files>src/scraper/orchestrator.ts, src/scraper/__tests__/orchestrator.test.ts</files>
  <read_first>
    - src/scraper/orchestrator.ts (current state; verify Tasks 1 & 2 additions are present)
    - src/scraper/__tests__/orchestrator.test.ts lines 171-199 (applyRateLimiting describe block; 3 test.skip stubs at lines 172, 182, 191)
    - src/lib/ratelimit/rate-limiter.ts lines 39-47 (RATE_LIMITS constants), lines 179-185 (checkRateLimitPreset two-arg signature), lines 17-24 (RateLimitResult and RateLimitConfig types)
    - .planning/phases/11-orchestrator-functions-tests/11-RESEARCH.md §"Pattern 3: applyRateLimiting — CRITICAL API MISMATCH" (resolves CONTEXT.md D-05 1-arg vs actual 2-arg API)
    - .planning/phases/11-orchestrator-functions-tests/11-RESEARCH.md §"Open Questions" item 1 (resolves "rate limit errors (429)" test interpretation: throw on `allowed: false`)
  </read_first>
  <behavior>
    - Test 1 (line 172, "should respect rate limits per source"): Activate as `test(...)`. With the rate-limiter module mocked at the top of the file, call `await applyRateLimiting('ligamagic')`. Assert: `checkRateLimitPreset` (the mocked vi.fn) was called exactly once with first arg `'ligamagic'` and second arg matching `{ limit: 30, interval: 60 }` (the LIGAMAGIC preset). Repeat for the other 3 sources by varying the input — at minimum cover ligamagic + one international source (e.g., tcgplayer with `{ limit: 40, interval: 60 }`). For an unknown source like `'unknown'`, assert `checkRateLimitPreset` was NOT called (since SOURCE_RATE_LIMIT_MAP has no entry).
    - Test 2 (line 182, "should wait between requests to same source"): Activate as `test(...)`. Mock `checkRateLimitPreset` to resolve. Call `await applyRateLimiting('tcgplayer')` twice in sequence. Assert `checkRateLimitPreset` was called exactly 2 times, both with first arg `'tcgplayer'` and the TCGPLAYER preset. The "wait" semantics are delegated to the rate limiter — this test verifies the orchestrator hits the rate limiter for each call, which is the unit-level contract `applyRateLimiting` owns (the spacing logic itself lives in checkRateLimit).
    - Test 3 (line 191, "should handle rate limit errors (429)"): Activate as `test(...)`. Per RESEARCH §Open Questions item 1, the resolution is: when `checkRateLimitPreset` resolves with `{ allowed: false, ... }`, `applyRateLimiting` MUST throw an Error (so callers surface the denial). Mock `checkRateLimitPreset` to resolve with `{ allowed: false, remaining: 0, resetAt: Date.now() + 60000 }`. Assert `await expect(applyRateLimiting('ligamagic')).rejects.toThrow()`. The error message should include the source name (e.g., `'Rate limit exceeded for ligamagic'`).
  </behavior>
  <action>
    Step A — Add the import line to src/scraper/orchestrator.ts.

    1. Open src/scraper/orchestrator.ts. Locate the existing imports near the top (lines 14-27).
    2. Add a new import line: `import { RATE_LIMITS, checkRateLimitPreset, type RateLimitConfig } from '@/lib/ratelimit/rate-limiter'`. Place it alphabetically with other `@/lib/*` imports (just before or after the `@/lib/retry` import on line 14, or grouped with the `../lib/*` imports — match the project's existing import style by inspecting the file).
    3. The path alias `@/lib/...` resolves identically to `../lib/...` for this file (it lives in `src/scraper/`). Use whichever alias is consistent with existing imports — verify by reading the file.

    Step B — Add SOURCE_RATE_LIMIT_MAP + applyRateLimiting at the bottom of the file.

    1. After the `handleSourceFailure` function added in Task 2, add the source-to-preset map:
       - Name: `SOURCE_RATE_LIMIT_MAP`. Type: `Record<string, RateLimitConfig>`. Not exported (internal helper).
       - Entries (lowercase keys, matching the source names used elsewhere in orchestrator.ts): `ligamagic: RATE_LIMITS.LIGAMAGIC`, `tcgplayer: RATE_LIMITS.TCGPLAYER`, `cardmarket: RATE_LIMITS.CARDMARKET`, `cardkingdom: RATE_LIMITS.CARDKINGDOM`.
    2. Add the `applyRateLimiting` function:
       - Signature: `export async function applyRateLimiting(source: string): Promise<void>`.
       - Body looks up `SOURCE_RATE_LIMIT_MAP[source.toLowerCase()]`. If undefined, returns silently (unknown source — no rate limit configured).
       - Otherwise calls `const result = await checkRateLimitPreset(source, preset)`.
       - If `result.allowed === false`, throws `new Error(\`Rate limit exceeded for \${source}\`)` (per RESEARCH Open Question 1 resolution).
       - Otherwise returns void.
    3. JSDoc one-liner explaining the function references CONTEXT D-05 + RESEARCH critical finding for traceability.

    Step C — Activate the 3 `applyRateLimiting` test stubs (lines 172, 182, 191 of orchestrator.test.ts).

    1. At the top of orchestrator.test.ts (alongside the `@/lib/logger` mock added in Task 2), add a top-level `vi.mock('@/lib/ratelimit/rate-limiter', () => ({ checkRateLimitPreset: vi.fn().mockResolvedValue({ allowed: true, remaining: 10, resetAt: Date.now() + 60000 }), RATE_LIMITS: { LIGAMAGIC: { limit: 30, interval: 60 }, TCGPLAYER: { limit: 40, interval: 60 }, CARDMARKET: { limit: 40, interval: 60 }, CARDKINGDOM: { limit: 40, interval: 60 }, SCRYFALL: { limit: 10, interval: 1 }, SCRYFALL_HEAVY: { limit: 2, interval: 1 }, TELEGRAM: { limit: 100, interval: 60 } } }))`. The mock must include all 7 RATE_LIMITS keys so that any code in orchestrator.ts that consumes them works in tests.
    2. For each of the 3 stubs in the `applyRateLimiting` describe block:
       - Change `test.skip(...)` to `test(...)` keeping the same title.
       - Replace the comment-only body with the real assertions per the `<behavior>` block.
       - Import `applyRateLimiting` statically (`import { applyRateLimiting } from '@/scraper/orchestrator'` — merge with existing imports).
       - To assert against the mocked `checkRateLimitPreset`, import it as well (or use `vi.mocked(...)`): `import { checkRateLimitPreset } from '@/lib/ratelimit/rate-limiter'` then `expect(vi.mocked(checkRateLimitPreset)).toHaveBeenCalledWith('ligamagic', { limit: 30, interval: 60 })`.
       - For Test 3 specifically, use `vi.mocked(checkRateLimitPreset).mockResolvedValueOnce({ allowed: false, remaining: 0, resetAt: Date.now() + 60000 })` BEFORE calling `applyRateLimiting`.
    3. Add a `beforeEach(() => { vi.clearAllMocks() })` to the `applyRateLimiting` describe block. After clearing, RE-PRIME the default mock implementation: `vi.mocked(checkRateLimitPreset).mockResolvedValue({ allowed: true, remaining: 10, resetAt: Date.now() + 60000 })`. Otherwise `mockClear` resets but the default would still be in place from the top-level vi.mock — verify with a quick test run.
    4. The existing passing tests at lines 1-99 already do `vi.doMock(...)` for various modules but NOT for `@/lib/ratelimit/rate-limiter`. The new top-level mock might affect any test in lines 1-99 that indirectly hits the rate limiter — none of those tests do, but verify by running `pnpm test:run src/scraper/__tests__/orchestrator.test.ts` and confirming all 5 original tests still pass.

    Step D — Final verification.

    Run `pnpm test:run src/scraper/__tests__/orchestrator.test.ts`. All 15 tests (5 originals + Task 1's 4 + Task 2's 3 + this task's 3) must pass with 0 failures. Then run full suite `pnpm test:run` to ensure no other test file regressed (the new top-level mocks are scoped to this file only, but a tsc-strict project may surface type errors elsewhere).
  </action>
  <verify>
    <automated>pnpm test:run src/scraper/__tests__/orchestrator.test.ts && pnpm test:run</automated>
  </verify>
  <acceptance_criteria>
    - src/scraper/orchestrator.ts contains the import `import { RATE_LIMITS, checkRateLimitPreset` from `@/lib/ratelimit/rate-limiter` (or the `../lib/...` equivalent) — grep: `grep -n "RATE_LIMITS.*checkRateLimitPreset\\|checkRateLimitPreset.*RATE_LIMITS" src/scraper/orchestrator.ts` returns at least 1 match.
    - src/scraper/orchestrator.ts contains a `SOURCE_RATE_LIMIT_MAP` const with all 4 source keys (ligamagic, tcgplayer, cardmarket, cardkingdom) — verify by reading the file.
    - src/scraper/orchestrator.ts contains `export async function applyRateLimiting(source: string)` (grep: `grep -nE "^export async function applyRateLimiting\\(source: string\\)" src/scraper/orchestrator.ts` returns exactly 1 match).
    - The applyRateLimiting body calls `checkRateLimitPreset(source, preset)` with TWO arguments (grep: `grep -nE "checkRateLimitPreset\\(source\\b" src/scraper/orchestrator.ts` returns a match; verify the call passes the preset as the second argument by reading the function body).
    - The applyRateLimiting body throws when `result.allowed === false` — grep finds both `result.allowed` and `throw new Error` patterns within the function.
    - src/scraper/__tests__/orchestrator.test.ts: the `describe('applyRateLimiting', ...)` block contains 0 `test.skip(` occurrences.
    - The 3 stubs are now active `test(` calls with the original titles: `should respect rate limits per source`, `should wait between requests to same source`, `should handle rate limit errors (429)`.
    - A top-level `vi.mock('@/lib/ratelimit/rate-limiter', ...)` exists in orchestrator.test.ts that mocks `checkRateLimitPreset` and exports `RATE_LIMITS` with all 7 standard keys.
    - No `expect(true).toBe(false)` strings remain in the `applyRateLimiting` describe block.
    - `pnpm test:run src/scraper/__tests__/orchestrator.test.ts` exits 0 with all 15 currently-active tests passing.
    - `pnpm test:run` (full suite) exits 0 — no other test file regressed.
    - The TypeScript compile is clean: `pnpm exec tsc --noEmit` exits 0 (or whatever the project's typecheck command is — verify by inspecting package.json scripts).
  </acceptance_criteria>
  <done>
    `applyRateLimiting` exported with correct 2-arg call to checkRateLimitPreset and throw-on-denied semantics; rate-limiter mocked at top of test file; 3 stubs active and passing; full test suite green; TypeScript compiles clean.
  </done>
</task>

</tasks>

<verification>
After all 3 tasks complete:

1. **Required exports present:**
   - `grep -nE "^export const orchestrateFetch = fetchCardPriceFromAllSources$" src/scraper/orchestrator.ts` → 1 match
   - `grep -nE "^export function handleSourceFailure\\(" src/scraper/orchestrator.ts` → 1 match
   - `grep -nE "^export async function applyRateLimiting\\(" src/scraper/orchestrator.ts` → 1 match

2. **No skipped stubs in the 3 target describe blocks:**
   - `awk '/describe..orchestrateFetch/,/describe..handleSourceFailure/' src/scraper/__tests__/orchestrator.test.ts | grep -c "test.skip("` → 0
   - `awk '/describe..handleSourceFailure/,/describe..applyRateLimiting/' src/scraper/__tests__/orchestrator.test.ts | grep -c "test.skip("` → 0
   - `awk '/describe..applyRateLimiting/,/describe..aggregateResults/' src/scraper/__tests__/orchestrator.test.ts | grep -c "test.skip("` → 0

3. **Tests pass:**
   - `pnpm test:run src/scraper/__tests__/orchestrator.test.ts` exits 0
   - The 3 target describe blocks contribute 10 newly-passing tests

4. **No regressions:**
   - `pnpm test:run` (full suite) exits 0
   - The 5 originally-passing tests at orchestrator.test.ts lines 1-99 still pass

5. **TypeScript clean:**
   - Project typecheck command (likely `pnpm exec tsc --noEmit` or `pnpm typecheck`) exits 0
</verification>

<success_criteria>
- TEST-12 requirement is satisfied: `orchestrateFetch`, `handleSourceFailure`, `applyRateLimiting` exist as named exports of src/scraper/orchestrator.ts and their 10 unit-test stubs are active and passing.
- The 5 pre-existing passing tests (orchestrator.test.ts lines 1-99) and the `fetchCardPriceFromAllSources` / `fetchAllPrices` function contracts are unchanged.
- Project test suite (`pnpm test:run`) is green.
- Plan 11-02 can begin without prerequisite work — its tasks edit a non-overlapping describe block region (lines 201-265) and add an independent `aggregateResults` + `batchOrchestrateFetch` set of exports.
</success_criteria>

<output>
After completion, create `.planning/phases/11-orchestrator-functions-tests/11-01-SUMMARY.md` per the standard summary template, documenting:
- The 3 new exports added (with line ranges in orchestrator.ts)
- The 10 newly-active tests (with the 3 describe blocks and their original titles)
- Confirmation that lines 1-99 of orchestrator.test.ts are unchanged
- Confirmation that fetchCardPriceFromAllSources behavior is unchanged
- The CONTEXT.md D-05 → RESEARCH correction (2-arg checkRateLimitPreset + SOURCE_RATE_LIMIT_MAP) and that RESEARCH Open Question 1 was resolved by throwing on `allowed: false`
</output>
