---
phase: 02-core-data-collection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scraper/providers/scryfall.ts
  - src/scraper/providers/liga-magic.ts
  - package.json
autonomous: true
requirements:
  - PRICE-08
must_haves:
  truths:
    - "Scryfall bulk data imports card metadata to database"
    - "Cards table populated with oracle_ids, names, sets, images"
    - "Liga Magic scraper can fetch prices given oracle_id"
    - "Rate limiting prevents Scryfall API violations"
  artifacts:
    - path: "src/scraper/providers/scryfall.ts"
      provides: "Scryfall bulk data import and card metadata refresh"
      exports: ["downloadBulkData", "parseBulkData", "upsertCards", "refreshCardMetadata"]
    - path: "src/scraper/providers/liga-magic.ts"
      provides: "Liga Magic price scraping with cheerio/Playwright"
      exports: ["fetchCardPrice", "checkRobotsTxt"]
    - path: "package.json"
      provides: "Dependencies for HTTP client, HTML parsing, browser automation"
      contains: "axios, cheerio, playwright, date-fns"
  key_links:
    - from: "src/scraper/providers/scryfall.ts"
      to: "src/db/schema/cards.ts"
      via: "upsertCards() inserts to cards table"
      pattern: "db\\.insert\\(cards\\)|db\\.update\\(cards\\)"
    - from: "src/scraper/providers/liga-magic.ts"
      to: "https://api.scryfall.com"
      via: "axios.get() for bulk data download"
      pattern: "axios\\.get.*scryfall"
    - from: "src/scraper/providers/liga-magic.ts"
      to: "src/lib/ratelimit/rate-limiter.ts"
      via: "checkRateLimitPreset() before scraping"
      pattern: "checkRateLimitPreset.*RATE_LIMITS"
---

# Phase 2 Plan 01: Scryfall Card Metadata & Liga Magic Foundation

## Objective

Bootstrap the card database with Scryfall bulk data and implement Liga Magic price scraping as the first price source. This plan establishes the foundation for all price collection by populating the cards table and creating the scraping pattern that other sources will follow.

Purpose: Enable price collection by having card metadata available and first working price source
Output: Cards table populated with 100-500 seed cards, Liga Magic scraper functional with rate limiting

## Execution Context

@/Users/luizpansarini/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luizpansarini/.claude/get-shit-done/templates/summary.md

@.planning/phases/02-core-data-collection/02-CONTEXT.md
@.planning/phases/02-core-data-collection/02-RESEARCH.md
@.planning/phases/01-foundation-infrastructure/01-02-SUMMARY.md

## Context

### From Phase 1 Database Schema (01-02-SUMMARY.md)

From `src/db/schema/cards.ts`:
```typescript
export const cards = pgTable('cards', {
  id: serial('id').primaryKey(),
  oracleId: varchar('oracle_id', { length: 255 }).notNull().unique(),
  name: varchar('name', { length: 255 }).notNull(),
  set: varchar('set', { length: 10 }),
  rarity: varchar('rarity', { length: 50 }),
  color: varchar('color', { length: 50 }),
  imageUrl: text('image_url'),
  lastFetched: timestamp('last_fetched'),
})
```

Key insight: `oracleId` is the unique identifier from Scryfall that aggregates all printings of a card.

### From Phase 1 Rate Limiting (01-03-SUMMARY.md)

From `src/lib/ratelimit/rate-limiter.ts`:
```typescript
export const RATE_LIMITS = {
  SCRYFALL: { limit: 10, interval: 1 },
  // TCGPLAYER: { limit: 50, interval: 60 },
} as const

export async function checkRateLimitPreset(
  key: string,
  preset: RateLimitConfig,
  tokens = 1,
): Promise<RateLimitResult>
```

Key insight: Use `checkRateLimitPreset('scryfall:bulk', RATE_LIMITS.SCRYFALL)` before API calls.

### From Phase 2 Decisions (02-CONTEXT.md)

**Locked Decisions:**
- Scryfall bulk data for card discovery: "Unique Card Artwork" or "Default Cards" dataset
- Manual seed 100-500 cards initially (not full import)
- 30-day metadata refresh cycle
- Upsert All strategy (ON CONFLICT DO UPDATE)
- Hotlink Scryfall image URLs (store in cards.image_url)

**Claude's Discretion:**
- Card deduplication logic within bulk data import
- Bulk data re-sync cadence (beyond 30-day refresh)

### From Phase 2 Research (02-RESEARCH.md)

**Scryfall Bulk Data Download:**
```typescript
// 1. Fetch bulk data metadata
const response = await axios.get('https://api.scryfall.com/bulk-data')
const bulkItems = response.data.data

// 2. Find "Unique Artwork" or "Default Cards" file
const uniqueArtwork = bulkItems.find(item => item.type === 'unique_artwork')
const downloadUri = uniqueArtwork?.download_uri

// 3. Download and parse gzip
const { data } = await axios.get(downloadUri, { responseType: 'arraybuffer' })
const gunzip = zlib.createGunzip()
const jsonString = await gunzipPromise(data, gunzip)

// 4. Upsert to database
await upsertCards(JSON.parse(jsonString))
```

**Critical note:** "prices should be considered dangerously stale after 24 hours" - do NOT use bulk data prices.

**Liga Magic Approach:**
- No documented public API (per CONTEXT.md)
- Scraping-only approach with cheerio/Playwright
- Check robots.txt compliance first
- Conservative rate limiting (30 req/min as fallback)

## Tasks

<task type="auto" tdd="true">
  <name>Task 1: Install dependencies for HTTP, scraping, and date manipulation</name>
  <files>package.json</files>
  <behavior>
    - Package install succeeds with axios, cheerio, playwright, date-fns
    - Playwright browsers installed (chromium binary)
    - TypeScript types resolved without errors
  </behavior>
  <action>
Install required packages per RESEARCH.md standard stack:
- axios@^1.6.0 - HTTP client for API calls
- cheerio@^1.0.0 - Fast HTML parsing
- playwright@^1.40.0 - Browser automation
- date-fns@^3.0.0 - Date manipulation for smart refresh
- zlib - Built-in Node.js module for gzip decompression

Run: `pnpm add axios cheerio playwright date-fns`
Run: `npx playwright install chromium` (for browser automation)

Verify: TypeScript compiles without errors, all imports resolve
  </action>
  <verify>
    <automated>pnpm test:run --reporter=verbose 2>&1 | grep -i "error" | wc -l</automated>
  </verify>
  <done>All packages installed, Playwright chromium binary downloaded, no TypeScript import errors</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Scryfall bulk data download and parsing</name>
  <files>src/scraper/providers/scryfall.ts</files>
  <behavior>
    - downloadBulkData() fetches bulk data metadata from Scryfall API
    - Finds "Unique Artwork" or "Default Cards" download URI
    - Downloads gzip file and decompresses with zlib
    - Returns parsed JSON array of card objects
    - Rate limits to 10 req/sec using existing rate limiter
  </behavior>
  <action>
Create src/scraper/providers/scryfall.ts with:

1. **downloadBulkData()** function:
   - Fetch https://api.scryfall.com/bulk-data
   - Find item.type === 'unique_artwork' or 'default_cards'
   - GET the download_uri with responseType: 'arraybuffer'
   - Decompress gzip using zlib.createGunzip()
   - Parse JSON and return array

2. **Rate limiting**:
   - Import checkRateLimitPreset from src/lib/ratelimit/rate-limiter
   - Call before each axios.get: `await checkRateLimitPreset('scryfall:bulk', RATE_LIMITS.SCRYFALL)`
   - Add SCRYFALL to RATE_LIMITS if not present (10 req/sec, 1 sec interval)

3. **Error handling**:
   - Log errors with winston logger
   - Throw on HTTP errors (axios response codes)
   - Retry logic: 3 retries with exponential backoff

Do NOT use bulk data prices - only metadata (oracle_id, name, set, rarity, image_uris)
  </action>
  <verify>
    <automated>pnpm test:run src/scraper/__tests__/scryfall.test.ts 2>&1 | grep -E "(PASS|FAIL)"</automated>
  </verify>
  <done>downloadBulkData() successfully fetches and parses Scryfall bulk data JSON</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement Scryfall card upsert to database</name>
  <files>src/scraper/providers/scryfall.ts</files>
  <behavior>
    - upsertCards() performs batch upsert to cards table
    - Inserts new cards, updates existing cards (ON CONFLICT DO UPDATE)
    - Maps Scryfall fields to database columns (oracle_id, name, set, rarity, image_uris.normal)
    - Handles pagination for large bulk data files (100-500 cards for initial seed)
  </behavior>
  <action>
Extend src/scraper/providers/scryfall.ts with:

1. **upsertCards()** function:
   - Import db, cards from src/db
   - Use Drizzle ORM insert().values() .onConflictDoUpdate()
   - Map Scryfall card object:
     - oracle_id → oracleId
     - name → name
     - set → set
     - rarity → rarity
     - colors → color (join array)
     - image_uris.normal → imageUrl (if single face)
     - card_faces[0].image_uris.normal → imageUrl (if transform card)
   - Batch insert in chunks of 100 (PostgreSQL parameter limit)

2. **Initial seed logic**:
   - Filter bulk data to 100-500 cards of interest
   - Filter criteria: set_type in ['core', 'expansion'] from last 2 years
   - This is temporary manual seed until Phase 5 metagame auto-discovery

3. **Type definitions**:
   - Create ScryfallCard interface matching API response
   - Use Zod for runtime validation (optional but recommended)

4. **Error handling**:
   - Log duplicate oracle_ids (shouldn't happen with unique constraint)
   - Continue on individual card failures (don't abort entire batch)
  </action>
  <verify>
    <automated>pnpm test:run src/scraper/__tests__/scryfall.test.ts 2>&1 | grep -E "(PASS|FAIL)"</automated>
  </verify>
  <done>upsertCards() successfully inserts/updates cards in database, no duplicate oracle_id errors</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Implement Scryfall 30-day metadata refresh</name>
  <files>src/scraper/providers/scryfall.ts</files>
  <behavior>
    - refreshCardMetadata() checks if cards.lastFetched > 30 days ago
    - Re-fetches card data from Scryfall API (not bulk data)
    - Updates existing cards with fresh metadata
    - Uses date-fns differenceInDays for calculation
  </behavior>
  <action>
Extend src/scraper/providers/scryfall.ts with:

1. **refreshCardMetadata()** function:
   - Import differenceInDays from date-fns
   - Query cards table for cards where lastFetched is NULL or > 30 days ago
   - For each stale card:
     - GET https://api.scryfall.com/cards/{oracle_id}
     - Update cards table with fresh data
     - Update lastFetched to now
   - Rate limit to 10 req/sec

2. **Smart refresh integration**:
   - This will be called by orchestrator in Plan 04
   - Separate from price collection (metadata only, no prices)

3. **Error handling**:
   - Log 404 errors (card removed from Scryfall)
   - Continue on individual card failures
   - Track refresh statistics (success/failure counts)

Note: Scryfall API endpoint for single card returns TCGPlayer prices - DO NOT use those prices (per RESEARCH.md)
  </action>
  <verify>
    <automated>pnpm test:run src/scraper/__tests__/scryfall.test.ts 2>&1 | grep -E "(PASS|FAIL)"</automated>
  </verify>
  <done>refreshCardMetadata() updates cards older than 30 days, respects Scryfall rate limits</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Implement Liga Magic robots.txt checker</name>
  <files>src/scraper/providers/liga-magic.ts</files>
  <behavior>
    - checkRobotsTxt() fetches https://ligamagic.com.br/robots.txt
    - Parses User-agent and Disallow rules
    - Returns boolean: true if scraping allowed, false if prohibited
    - Logs warnings if scraping is discouraged
  </behavior>
  <action>
Create src/scraper/providers/liga-magic.ts with:

1. **checkRobotsTxt()** function:
   - Fetch https://ligamagic.com.br/robots.txt
   - Parse text for 'User-agent: *' section
   - Check for 'Disallow: /' or specific path disallows
   - Return false if scraping prohibited
   - Log warning if 'Crawl-delay:' specifies delay > 1 second

2. **Error handling**:
   - If robots.txt unreachable (404/500), log warning and allow scraping (fail-open)
   - Cache robots.txt content for 24 hours (don't fetch on every call)

3. **Legal compliance**:
   - This is a safety check, not legal advice
   - Users should still review Liga Magic Terms of Service
   - Brazilian law: robots.txt not legally binding but considered as evidence (per RESEARCH.md)

Note: CONTEXT.md assumes Liga Magic has no API, scraping-only approach
  </action>
  <verify>
    <automated>pnpm test:run src/scraper/__tests__/liga-magic.test.ts 2>&1 | grep -E "(PASS|FAIL)"</automated>
  </verify>
  <done>checkRobotsTxt() returns correct boolean based on robots.txt rules, logs warnings</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Implement Liga Magic price scraper</name>
  <files>src/scraper/providers/liga-magic.ts</files>
  <behavior>
    - fetchCardPrice() fetches price from ligamagic.com.br given oracle_id
    - Searches for card by name (Scryfall name mapped to Liga Magic search)
    - Extracts BRL price from page HTML using cheerio or Playwright
    - Returns price_brl (number) or null if not found
    - Respects robots.txt and rate limits (30 req/min conservative)
  </behavior>
  <action>
Extend src/scraper/providers/liga-magic.ts with:

1. **fetchCardPrice()** function:
   - Input: oracle_id (string)
   - Query cards table for card.name and card.set
   - Build Liga Magic search URL:
     - Base: https://ligamagic.com.br/?view=cards/search
     - Query params: card={name}, {set}
   - Fetch page HTML with axios
   - Parse with cheerio (try cheerio first for speed)
   - Extract price from selector (need to inspect actual HTML - use common selectors like '.price', '[data-price]', '.card-price')
   - Parse BRL string (e.g., "R$ 45,90") to number (45.90)
   - Return price_brl or null

2. **Fallback to Playwright**:
   - If cheerio parsing fails (empty/invalid price)
   - Use Playwright with headless chromium
   - Wait for networkidle (dynamic content)
   - Extract price with page.$eval()
   - Close browser

3. **Rate limiting**:
   - Add LIGAMAGIC to RATE_LIMITS: { limit: 30, interval: 60 }
   - Call checkRateLimitPreset before each fetch

4. **Error handling**:
   - Return null if card not found (404)
   - Return null if price cannot be parsed
   - Log errors with card oracle_id for debugging
   - Don't throw - allow orchestrator to continue with other sources

Note: Claude's discretion on exact price selector - will need inspection during implementation
  </action>
  <verify>
    <automated>pnpm test:run src/scraper/__tests__/liga-magic.test.ts 2>&1 | grep -E "(PASS|FAIL)"</automated>
  </verify>
  <done>fetchCardPrice() returns numeric BRL price or null, respects rate limits, logs errors</done>
</task>

## Verification

After all tasks complete:
1. Run `pnpm test:run src/scraper/__tests__/scryfall.test.ts` - all Scryfall tests pass
2. Run `pnpm test:run src/scraper/__tests__/liga-magic.test.ts` - all Liga Magic tests pass
3. Manual verification: Run seed script to populate cards table with 100-500 cards
4. Manual verification: Test Liga Magic scraper with 2-3 known cards (e.g., "Lightning Bolt", "Black Lotus")

## Success Criteria

- [ ] Scryfall bulk data downloads and parses correctly
- [ ] Cards table populated with 100-500 cards from recent sets
- [ ] refreshCardMetadata() updates cards older than 30 days
- [ ] Liga Magic scraper fetches prices and returns numeric BRL values
- [ ] Rate limiting prevents Scryfall API violations (10 req/sec)
- [ ] Rate limiting prevents Liga Magic blocking (30 req/min)
- [ ] All tests pass with green checkmarks
- [ ] No hardcoded values (rate limits, URLs, selectors)

## Output

After completion, create `.planning/phases/02-core-data-collection/02-01-SUMMARY.md`
