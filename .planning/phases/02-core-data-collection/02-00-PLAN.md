---
phase: 02-core-data-collection
plan: 00
type: execute
wave: 0
depends_on: []
files_modified: []
autonomous: true
requirements: []
must_haves:
  truths:
    - "All test stub files exist for Wave 1-4 tasks"
    - "Vitest configuration is complete and functional"
    - "Test mocks for external dependencies are configured"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Vitest configuration for test framework"
      contains: "test, exclude, coverage configuration"
    - path: "src/scraper/__tests__/scryfall.test.ts"
      provides: "Test stubs for Scryfall bulk data import"
      min_lines: 20
    - path: "src/scraper/__tests__/liga-magic.test.ts"
      provides: "Test stubs for Liga Magic scraping"
      min_lines: 20
    - path: "src/scraper/__tests__/tcgplayer.test.ts"
      provides: "Test stubs for TCGPlayer API/scraping"
      min_lines: 20
    - path: "src/scraper/__tests__/cardmarket.test.ts"
      provides: "Test stubs for CardMarket API/scraping"
      min_lines: 20
    - path: "src/scraper/__tests__/cardkingdom.test.ts"
      provides: "Test stubs for CardKingdom API/scraping"
      min_lines: 20
    - path: "src/scraper/__tests__/circuit-breaker.test.ts"
      provides: "Test stubs for Opossum circuit breaker"
      min_lines: 20
    - path: "src/scraper/__tests__/smart-refresh.test.ts"
      provides: "Test stubs for 8-hour smart refresh logic"
      min_lines: 20
    - path: "src/scraper/__tests__/orchestrator.test.ts"
      provides: "Test stubs for fetch orchestration"
      min_lines: 20
    - path: "src/lib/__tests__/currency.test.ts"
      provides: "Test stubs for currency conversion with IOF"
      min_lines: 20
    - path: "src/scheduler/__tests__/jobs.test.ts"
      provides: "Test stubs for node-cron scheduling"
      min_lines: 20
  key_links: []
---

# Phase 2 Plan 00: Test Infrastructure Setup

## Objective

Create test stubs and configuration for all Phase 2 functionality, ensuring Nyquist-compliant automated verification. This Wave 0 plan creates the test foundation that enables rapid feedback during Waves 1-4 implementation.

Purpose: Enable continuous test-driven development with <30 second feedback cycles per task
Output: Complete Vitest configuration and test stub files for all scrapers, circuit breaker, currency conversion, and scheduling

## Execution Context

@/Users/luizpansarini/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luizpansarini/.claude/get-shit-done/templates/summary.md

@.planning/phases/02-core-data-collection/02-CONTEXT.md
@.planning/phases/02-core-data-collection/02-RESEARCH.md
@.planning/phases/02-core-data-collection/02-VALIDATION.md

## Context

### From Phase 1 Infrastructure

From `package.json`:
```json
{
  "scripts": {
    "test": "vitest",
    "test:run": "vitest --run",
    "test:coverage": "vitest --coverage"
  },
  "devDependencies": {
    "vitest": "^3.0.9",
    "@vitest/ui": "^3.0.9"
  }
}
```

From `src/lib/ratelimit/rate-limiter.ts`:
- Existing rate limit presets: SCRYFALL (10/sec), TELEGRAM (100/min), TCGPLAYER (50/min)
- Token bucket algorithm with Lua scripts
- `checkRateLimit` and `checkRateLimitPreset` functions

### From Phase 2 Research (02-RESEARCH.md)

**Standard Stack to Test:**
- **axios** ^1.6.0 - HTTP client for API calls
- **cheerio** ^1.0.0 - Fast HTML parsing
- **playwright** ^1.40.0 - Browser automation
- **opossum** ^8.0.0 - Circuit breaker pattern
- **date-fns** ^3.0.0 - Date manipulation
- **zod** ^3.22.0 - Runtime type validation

**Test Requirements from 02-VALIDATION.md:**
- Quick run: `pnpm test:run` (<30 seconds)
- Full suite: `pnpm test:coverage`
- Sampling: After every task commit, run subset of relevant tests

## Tasks

<task type="auto">
  <name>Task 1: Create Vitest configuration file</name>
  <files>vitest.config.ts</files>
  <action>
Create `vitest.config.ts` with:
- Test environment: node (for backend testing)
- Coverage provider: v8 (built-in to Vitest)
- Include patterns: `src/**/__tests__/**/*.test.ts`
- Exclude patterns: node_modules, dist, .next
- Coverage thresholds: 80% global (adjustable per phase)
- Test timeout: 10000ms (10 seconds for API/scraping tests)

Configuration should match Vitest 3.x API (already installed per package.json)
  </action>
  <verify>
    <automated>pnpm test:run --reporter=verbose</automated>
  </verify>
  <done>vitest.config.ts exists with valid configuration, `pnpm test:run` executes without errors</done>
</task>

<task type="auto">
  <name>Task 2: Create test stubs for Scryfall bulk data import</name>
  <files>src/scraper/__tests__/scryfall.test.ts</files>
  <action>
Create test stub file with describe blocks for:
- downloadBulkData(): Fetches bulk data metadata from Scryfall API
- parseBulkData(): Parses gzipped JSON file
- upsertCards(): Batch inserts/updates cards in database
- refreshCardMetadata(): Updates cards older than 30 days

Use test.each() for parameterized test cases (multiple card scenarios)
  </action>
  <verify>
    <automated>pnpm test:run src/scraper/__tests__/scryfall.test.ts</automated>
  </verify>
  <done>Test file exists with 4+ describe blocks, stub tests marked as test.skip</done>
</task>

<task type="auto">
  <name>Task 3: Create test stubs for Liga Magic scraper</name>
  <files>src/scraper/__tests__/liga-magic.test.ts</files>
  <action>
Create test stub file with describe blocks for:
- fetchCardPrice(): Scrapes price from ligamagic.com.br given oracle_id
- checkRobotsTxt(): Validates robots.txt compliance
- parsePriceFromHTML(): Extracts price from page HTML using cheerio/Playwright

Note: Liga Magic has no documented API (per CONTEXT.md), scraping-only approach
  </action>
  <verify>
    <automated>pnpm test:run src/scraper/__tests__/liga-magic.test.ts</automated>
  </verify>
  <done>Test file exists with 3+ describe blocks, stub tests marked as test.skip</done>
</task>

<task type="auto">
  <name>Task 4: Create test stubs for TCGPlayer fetcher</name>
  <files>src/scraper/__tests__/tcgplayer.test.ts</files>
  <action>
Create test stub file with describe blocks for:
- fetchViaAPI(): Uses TCGPlayer API with bearer token auth
- fetchViaScraping(): Fallback scraping if API fails
- fetchCardPrice(): Hybrid approach with circuit breaker wrapper

Note: TCGPlayer requires API key application (per RESEARCH.md open questions)
  </action>
  <verify>
    <automated>pnpm test:run src/scraper/__tests__/tcgplayer.test.ts</automated>
  </verify>
  <done>Test file exists with 3+ describe blocks, stub tests marked as test.skip</done>
</task>

<task type="auto">
  <name>Task 5: Create test stubs for CardMarket fetcher</name>
  <files>src/scraper/__tests__/cardmarket.test.ts</files>
  <action>
Create test stub file with describe blocks for:
- fetchViaAPI(): Uses CardMarket MKM API
- fetchViaScraping(): Fallback scraping for European prices
- fetchCardPrice(): Hybrid approach with circuit breaker

Note: CardMarket API rate limits unknown (per RESEARCH.md), assume 50 req/min
  </action>
  <verify>
    <automated>pnpm test:run src/scraper/__tests__/cardmarket.test.ts</automated>
  </verify>
  <done>Test file exists with 3+ describe blocks, stub tests marked as test.skip</done>
</task>

<task type="auto">
  <name>Task 6: Create test stubs for CardKingdom fetcher</name>
  <files>src/scraper/__tests__/cardkingdom.test.ts</files>
  <action>
Create test stub file with describe blocks for:
- fetchViaAPI(): Attempts CardKingdom API (if exists)
- fetchViaScraping(): Scrapes cardkingdom.com as primary method
- fetchCardPrice(): Hybrid approach with circuit breaker

Note: CardKingdom likely scraping-only (per RESEARCH.md open questions)
  </action>
  <verify>
    <automated>pnpm test:run src/scraper/__tests__/cardkingdom.test.ts</automated>
  </verify>
  <done>Test file exists with 3+ describe blocks, stub tests marked as test.skip</done>
</task>

<task type="auto">
  <name>Task 7: Create test stubs for circuit breaker behavior</name>
  <files>src/scraper/__tests__/circuit-breaker.test.ts</files>
  <action>
Create test stub file with describe blocks for Opossum circuit breaker:
- Circuit opens when 50% of requests fail (errorThresholdPercentage: 50)
- Circuit closes after resetTimeout when service recovers
- Circuit half-open state allows test requests
- Fallback function returns cached data or null
- Event emission (open, close, halfOpen, fallback)

Per CONTEXT.md: Circuit breaker prevents cascading failures from bad sources
  </action>
  <verify>
    <automated>pnpm test:run src/scraper/__tests__/circuit-breaker.test.ts</automated>
  </verify>
  <done>Test file exists with 5+ describe blocks, stub tests marked as test.skip</done>
</task>

<task type="auto">
  <name>Task 8: Create test stubs for smart refresh logic</name>
  <files>src/scraper/__tests__/smart-refresh.test.ts</files>
  <action>
Create test stub file with describe blocks for:
- shouldFetchPrice(): Returns true if >8 hours since last fetch
- shouldFetchPrice(): Returns false if <=8 hours since last fetch
- shouldFetchPrice(): Returns true if never fetched before

Per CONTEXT.md: Smart refresh reduces API calls by ~66% vs fetching all cards every run
  </action>
  <verify>
    <automated>pnpm test:run src/scraper/__tests__/smart-refresh.test.ts</automated>
  </verify>
  <done>Test file exists with 3+ describe blocks, stub tests marked as test.skip</done>
</task>

<task type="auto">
  <name>Task 9: Create test stubs for fetch orchestration</name>
  <files>src/scraper/__tests__/orchestrator.test.ts</files>
  <action>
Create test stub file with describe blocks for:
- orchestrateFetch(): Sequential Liga Magic first, then parallel intl sources
- handleSourceFailure(): Continues with other sources when one fails
- applyRateLimiting(): Respects rate limits per source (80% of documented)

Per CONTEXT.md: Liga Magic priority (BR data), parallel TCGPlayer/CardMarket/CardKingdom
  </action>
  <verify>
    <automated>pnpm test:run src/scraper/__tests__/orchestrator.test.ts</automated>
  </verify>
  <done>Test file exists with 3+ describe blocks, stub tests marked as test.skip</done>
</task>

<task type="auto">
  <name>Task 10: Create test stubs for currency conversion with IOF</name>
  <files>src/lib/__tests__/currency.test.ts</files>
  <action>
Create test stub file with describe blocks for:
- convertToBRL(): Converts USD to BRL with 6.38% IOF
- convertToBRL(): Converts EUR to BRL with 6.38% IOF
- getExchangeRate(): Fetches rate from Brazilian Central Bank API
- applyIOF(): Applies 6.38% IOF tax to converted amount

Per REQUIREMENTS.md PRICE-05: IOF of 6.38% for credit card purchases
  </action>
  <verify>
    <automated>pnpm test:run src/lib/__tests__/currency.test.ts</automated>
  </verify>
  <done>Test file exists with 4+ describe blocks, stub tests marked as test.skip</done>
</task>

<task type="auto">
  <name>Task 11: Create test stubs for cron job scheduling</name>
  <files>src/scheduler/__tests__/jobs.test.ts</files>
  <action>
Create test stub file with describe blocks for:
- schedulePriceCollection(): Configures node-cron for 2-3x daily execution
- executePriceCollection(): Runs full fetch orchestration
- validateScheduleTimes(): Verifies jobs run at correct times (e.g., 9AM, 3PM, 9PM)

Per CONTEXT.md: 2-3x daily checks, NOT real-time (NOTIF-03 compliance)
  </action>
  <verify>
    <automated>pnpm test:run src/scheduler/__tests__/jobs.test.ts</automated>
  </verify>
  <done>Test file exists with 3+ describe blocks, stub tests marked as test.skip</done>
</task>

## Verification

After all tasks complete:
1. Run `pnpm test:run` - all stub tests should pass (test.skip marks them as pending)
2. Verify test files exist in correct locations
3. Confirm vitest.config.ts is valid

## Success Criteria

- [ ] vitest.config.ts created with proper Vitest 3.x configuration
- [ ] 10 test stub files created with describe blocks
- [ ] `pnpm test:run` executes without configuration errors
- [ ] All stub tests use test.skip() to indicate pending implementation
- [ ] Test files follow naming convention: `**/__tests__/*.test.ts`
- [ ] Coverage configuration enabled in vitest.config.ts

## Output

After completion, create `.planning/phases/02-core-data-collection/02-00-SUMMARY.md`
