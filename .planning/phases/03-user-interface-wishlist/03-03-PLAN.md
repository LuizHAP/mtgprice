---
phase: 03-user-interface-wishlist
plan: 03
type: execute
wave: 2
depends_on: ["03-00", "03-01", "03-02"]
files_modified: [
  "src/app/wishlist/page.tsx",
  "src/app/page.tsx",
  "src/app/layout.tsx",
  "src/components/wishlist/CardGrid.tsx",
  "src/components/wishlist/SearchBar.tsx",
  "src/components/wishlist/PriceTable.tsx",
  "src/components/wishlist/EmptyState.tsx",
  "src/components/layout/Header.tsx",
  "src/components/ui/button",
  "src/components/ui/card",
  "src/components/ui/input",
  "src/components/ui/table",
  "src/components/ui/toast",
  "src/components/ui/dialog",
  "src/components/ui/checkbox"
]
autonomous: false
requirements: ["WISH-01", "WISH-02", "WISH-03", "WISH-05", "DASH-01", "DASH-02"]
user_setup:
  - service: shadcn
    why: "UI component library for web dashboard"
    setup_commands:
      - "npx shadcn@latest init"
      - "npx shadcn@latest add button card input table toast dialog checkbox dropdown-menu"
must_haves:
  truths:
    - "User can view wishlist as card grid with images on /wishlist page"
    - "User can search for cards using autocomplete search bar"
    - "User can add cards to wishlist from search results"
    - "User can remove individual cards via X button on each card"
    - "User can bulk remove cards via checkboxes + Remove Selected button"
    - "User can see price comparison table with all 4 sources"
    - "User can see best price highlighted in green/bold"
    - "User can see price trend indicators (↑↓) with % change"
    - "Empty state shows when wishlist is empty"
    - "Pages work on mobile (responsive grid, horizontal table scroll)"
  artifacts:
    - path: "src/app/wishlist/page.tsx"
      provides: "Main wishlist page with Server Component"
      min_lines: 80
    - path: "src/app/page.tsx"
      provides: "Home/overview page"
      min_lines: 40
    - path: "src/components/wishlist/CardGrid.tsx"
      provides: "Client Component for card grid display"
      min_lines: 100
    - path: "src/components/wishlist/SearchBar.tsx"
      provides: "Client Component with autocomplete dropdown"
      min_lines: 120
    - path: "src/components/wishlist/PriceTable.tsx"
      provides: "Client Component for price comparison table"
      min_lines: 80
    - path: "src/components/wishlist/EmptyState.tsx"
      provides: "Empty state component with suggestions"
      min_lines: 40
    - path: "src/components/layout/Header.tsx"
      provides: "Navigation header"
      min_lines: 40
  key_links:
    - from: "src/app/wishlist/page.tsx"
      to: "/api/wishlist"
      via: "Server Component fetches wishlist on server"
      pattern: "getUserWishlist.*userId"
    - from: "src/components/wishlist/SearchBar.tsx"
      to: "/api/cards/search"
      via: "Debounced fetch on input change"
      pattern: "fetch.*api/cards/search.*debounce"
    - from: "src/components/wishlist/CardGrid.tsx"
      to: "/api/wishlist"
      via: "POST (add) and DELETE (remove) fetch calls"
      pattern: "fetch.*api/wishlist.*POST|DELETE"
    - from: "src/components/wishlist/PriceTable.tsx"
      to: "/api/prices/[oracle_id]"
      via: "Fetch price comparison data"
      pattern: "fetch.*api/prices/"
---

<objective>
Build web dashboard UI for wishlist management with card grid, search autocomplete, and price comparison table. Install Shadcn/ui components and create responsive layout implementing WISH-01 through WISH-05 and DASH-01, DASH-02 requirements.

Purpose: Deliver user-facing web interface for wishlist CRUD operations and price comparison (WISH-05, DASH-02 requirements).
Output: 2 Next.js pages (wishlist, home), 6 React components, 8 Shadcn/ui components, fully functional dashboard
</objective>

<execution_context>
@/Users/luizpansarini/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luizpansarini/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-user-interface-wishlist/03-CONTEXT.md
@.planning/phases/03-user-interface-wishlist/03-RESEARCH.md

# API endpoints from previous plans
@src/app/api/wishlist/route.ts (plan 03-01)
@src/app/api/cards/search/route.ts (plan 03-02)
@src/app/api/prices/[oracle_id]/route.ts (plan 03-02)

# Type definitions
@src/types/wishlist.ts (plan 03-01)

# Existing Next.js configuration
@src/app/layout.tsx (root layout exists, may need updates)
@next.config.js
@tailwind.config.ts

# Existing patterns from Phase 1
@src/app/api/auth/login/route.ts
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>Install Shadcn/ui components needed for wishlist dashboard UI</what-built>
  <how-to-verify>
    Shadcn/ui is a CLI tool that copies component source code into your project. This is a manual setup step that requires user interaction.

    **Run these commands in your terminal:**

    1. Initialize Shadcn/ui (first time only):
       ```bash
       npx shadcn@latest init
       ```
       Accept defaults for all prompts (Tailwind CSS, src/components/ui, etc.)

    2. Install required components:
       ```bash
       npx shadcn@latest add button card input table toast dialog checkbox dropdown-menu
       ```

    **Expected output:**
    - Components will be copied to `src/components/ui/` directory
    - Each component will have a .tsx file (e.g., button.tsx, card.tsx)
    - lib/utils.ts file will be created for className merging utility

    **Verify installation:**
    ```bash
    ls src/components/ui/
    ```
    Should show: button.tsx, card.tsx, checkbox.tsx, dialog.tsx, dropdown-menu.tsx, input.tsx, table.tsx, toast.tsx

    Type "approved" when all 8 components are installed.
  </how-to-verify>
  <resume-signal>Type "approved" when Shadcn/ui components are installed</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create layout components (Header, Footer)</name>
  <files>src/components/layout/Header.tsx, src/components/layout/Footer.tsx</files>
  <behavior>
    - Header component shows navigation links (Logo | Wishlist | About)
    - Header is sticky at top of page
    - Footer component shows copyright and project info
    - Both use Shadcn/ui components for styling
  </behavior>
  <action>
    Create layout components for the dashboard.

    **src/components/layout/Header.tsx:**
    - Create Client Component ('use client')
    - Import Link from 'next/link', Button from '@/components/ui/button'
    - Render navigation header with:
      - Logo/brand name on left ("MTG Price Monitor")
      - Navigation links on right (Wishlist, About)
      - Use flexbox for layout, sticky positioning
      - Style with Tailwind classes (bg-white, shadow-md, py-4, px-6)
    - Export as Header component

    **src/components/layout/Footer.tsx:**
    - Create Server Component (default)
    - Render footer with copyright text and simple links
    - Style with Tailwind (bg-gray-100, py-6, text-center)

    Use Shadcn/ui Button component for links. Follow Shadcn/ui patterns from RESEARCH.md.

    Files should be ~40 lines each, export default components.

    Reference CONTEXT.md decision: "Navigation: Simple header nav (Logo | Wishlist | About)"
  </action>
  <verify>
    <automated>grep -c "export" src/components/layout/Header.tsx src/components/layout/Footer.tsx</automated>
  </verify>
  <done>Header and Footer components created, use Shadcn/ui Button, styled with Tailwind</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create SearchBar component with autocomplete</name>
  <files>src/components/wishlist/SearchBar.tsx</files>
  <behavior>
    - SearchBar is Client Component with controlled input state
    - Input debounces searches by 300ms
    - Fetches /api/cards/search?q=query on debounced input
    - Shows dropdown with matching cards (max 10 results)
    - Clicking a result adds card to wishlist via POST /api/wishlist
    - Shows loading spinner during search
    - Shows error toast on failure
    - Clears input after successful add
  </behavior>
  <action>
    Create src/components/wishlist/SearchBar.tsx with autocomplete functionality.

    Component structure:
    - 'use client' directive (Client Component for interactivity)
    - State: query (string), results (array), loading (boolean), error (string | null)
    - Import: useState, useEffect from 'react', Input from '@/components/ui/input', Button from '@/components/ui/button', toast from '@/components/ui/toast'
    - Implement debounce using custom hook or setTimeout pattern (300ms delay)
    - useEffect on query changes: if query.length >= 2, fetch /api/cards/search?q=query
    - Render dropdown when results.length > 0 (absolute positioned, z-10, white bg, border, shadow)
    - On result click: POST to /api/wishlist with { cardId: result.oracleId }, refresh page on success
    - Handle errors with toast notification

    Debounce implementation (from RESEARCH.md):
    ```typescript
    const [debouncedQuery, setDebouncedQuery] = useState(query)

    useEffect(() => {
      const timer = setTimeout(() => setDebouncedQuery(query), 300)
      return () => clearTimeout(timer)
    }, [query])

    useEffect(() => {
      if (debouncedQuery.length >= 2) {
        // perform search
      }
    }, [debouncedQuery])
    ```

    Use Shadcn/ui Input and Button components. Follow autocomplete pattern from RESEARCH.md Pattern 1 (Client Component example).

    File should be ~120 lines, handle edge cases (empty results, network errors), include loading states.

    Reference CONTEXT.md decision: "Search bar with autocomplete as user types"
  </action>
  <verify>
    <automated>npm run test:run src/web/__tests__/components/SearchBar.test.ts (if created) or manual testing in browser</automated>
  </verify>
  <done>SearchBar debounces input, shows autocomplete dropdown, adds cards on click, handles errors</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create CardGrid component with bulk removal</name>
  <files>src/components/wishlist/CardGrid.tsx</files>
  <behavior>
    - CardGrid is Client Component receiving wishlist items as props
    - Displays cards in responsive grid (1 col mobile, 2 col sm, 3 col md, 4 col lg)
    - Each card shows: image (aspect ratio preserved), name, set, best price, trend indicator
    - Each card has remove button (X icon)
    - Each card has checkbox for bulk selection
    - "Remove Selected" button appears when cards are checked
    - Confirmation dialog before bulk removal
    - Images have fallback placeholder on error
  </behavior>
  <action>
    Create src/components/wishlist/CardGrid.tsx for displaying wishlist cards in grid layout.

    Component structure:
    - 'use client' directive
    - Props: cards (array of WishlistWithPrices from plan 03-01)
    - State: selectedCards (Set of oracleIds), showConfirmDialog (boolean)
    - Import: Card from '@/components/ui/card', Button from '@/components/ui/button', Checkbox from '@/components/ui/checkbox', Dialog from '@/components/ui/dialog', Image from 'next/image'
    - Render responsive grid: `div.grid.grid-cols-1.sm:grid-cols-2.md:grid-cols-3.lg:grid-cols-4.gap-6`
    - For each card, render Card component with:
      - Image (use next/image for optimization, aspect-ratio CSS, fallback to placeholder on error)
      - Card name (bold, truncate if too long)
      - Set name (smaller, gray text)
      - Best price (green/bold if lowest price, format as R$ X.XX)
      - Trend indicator (↑ red for up, ↓ green for down, ± gray for stable, with % change)
      - Remove button (X icon, top right of card)
      - Checkbox (top left of card, for bulk selection)
    - "Remove Selected" button fixed at bottom (appears when selectedCards.size > 0)
    - Dialog for bulk removal confirmation ("Remove X selected cards?")
    - On remove: DELETE /api/wishlist/[card_id] for each selected card, refresh page on success

    Image aspect ratio: Use Tailwind `aspect-[488/680]` class (from RESEARCH.md Open Question 5).
    Fallback image: Create placeholder at /placeholder-card.png or use CSS background.

    Use Shadcn/ui Card, Button, Checkbox, Dialog components. Follow grid pattern from RESEARCH.md Pattern 3 (Tailwind Responsive Grid).

    File should be ~100 lines, handle empty state (show EmptyState component), include error handling.

    Reference CONTEXT.md decision: "Grid layout showing card images + names + current best price"
  </action>
  <verify>
    <automated>npm run test:run src/web/__tests__/components/CardGrid.test.ts (if created) or manual testing in browser</automated>
  </verify>
  <done>CardGrid displays responsive grid, images with fallback, checkboxes work, bulk removal functional</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Create PriceTable component</name>
  <files>src/components/wishlist/PriceTable.tsx</files>
  <behavior>
    - PriceTable is Client Component receiving prices as props
    - Displays table with columns: Card | Liga Magic | TCGPlayer | CardMarket | CardKingdom | Best
    - Each row shows one card with all 4 source prices
    - Best price highlighted in green/bold
    - Sortable columns (by price, by name)
    - Horizontal scroll on mobile (overflow-x-auto)
    - Shows "-" for missing prices
  </behavior>
  <action>
    Create src/components/wishlist/PriceTable.tsx for comparing prices across all 4 sources.

    Component structure:
    - 'use client' directive
    - Props: cards (array of WishlistWithPrices)
    - State: sortColumn (string), sortDirection ('asc' | 'desc')
    - Import: Table components from '@/components/ui/table' (Table, TableBody, TableCell, TableHead, TableHeader, TableRow)
    - Render table with sticky header
    - Columns: Card Name, Liga Magic, TCGPlayer, CardMarket, CardKingdom, Best
    - For each card row:
      - Show card name (link to /cards/[oracle_id] page)
      - Show prices from all 4 sources (format as R$ X.XX)
      - Highlight best price in green/bold (use text-green-600 font-bold)
      - Show "-" if price is null
    - Sortable headers: click to sort by column
    - Mobile: wrap table in div with overflow-x-auto for horizontal scroll

    Use Shadcn/ui Table component. Follow table pattern from RESEARCH.md Pattern 3.

    File should be ~80 lines, handle missing prices gracefully, include sort logic.

    Reference CONTEXT.md decision: "Table with columns: Card | Liga Magic | TCGPlayer | CardMarket | CardKingdom | Best"
  </action>
  <verify>
    <automated>npm run test:run src/web/__tests__/components/PriceTable.test.ts (if created) or manual testing in browser</automated>
  </verify>
  <done>PriceTable displays 4 sources, best price highlighted, sortable columns, mobile scrollable</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Create EmptyState component</name>
  <files>src/components/wishlist/EmptyState.tsx</files>
  <behavior>
    - EmptyState is Server Component
    - Shows friendly message when wishlist is empty
    - Displays suggested popular cards to add
    - Quick-add buttons for suggested cards (pre-filled search)
  </behavior>
  <action>
    Create src/components/wishlist/EmptyState.tsx for empty wishlist state.

    Component structure:
    - Server Component (no 'use client' needed)
    - Import: Button from '@/components/ui/button', Card from '@/components/ui/card'
    - Render centered layout with:
      - Icon/illustration (use emoji or simple SVG)
      - Heading: "Your wishlist is empty"
      - Subtext: "Start tracking cards by searching above or try these popular cards:"
      - Suggested cards list (hardcoded popular Standard/Modern cards):
        - "Sheoldred, the Apocalypse" (Standard staple)
        - "Thoughtseize" (Modern staple)
        - "Orcish Bowmasters" (Modern staple)
        - "Hullbreaker Horror" (Commander staple)
      - Quick-add buttons for each suggestion (clicks SearchBar with pre-filled query)
    - Style with centered layout, gray text, card container

    Use Shadcn/ui Card and Button components. Follow empty state pattern from CONTEXT.md decision.

    File should be ~40 lines, visually appealing, include call-to-action.

    Reference CONTEXT.md decision: "Friendly message: 'Your wishlist is empty. Start tracking cards!'"
  </action>
  <verify>
    <automated>grep -c "export" src/components/wishlist/EmptyState.tsx</automated>
  </verify>
  <done>EmptyState shows friendly message, suggested cards, quick-add buttons</done>
</task>

<task type="auto" tdd="true">
  <name>Task 7: Create wishlist page (Server Component)</name>
  <files>src/app/wishlist/page.tsx</files>
  <behavior>
    - Wishlist page is Server Component
    - Fetches wishlist on server using getUserWishlist
    - Fetches prices for each card using getLatestPricesForCard
    - Shows SearchBar component at top
    - Shows CardGrid if wishlist has cards
    - Shows EmptyState if wishlist is empty
    - Shows PriceTable below grid for comparison view
    - Page refreshes after add/remove operations
  </behavior>
  <action>
    Create src/app/wishlist/page.tsx as main wishlist management page.

    Page structure:
    - Server Component (async function)
    - Import: getServerUser from '@/lib/auth.ts', getUserWishlist from '@/lib/wishlist/queries.ts, SearchBar from '@/components/wishlist/SearchBar', CardGrid from '@/components/wishlist/CardGrid', EmptyState from '@/components/wishlist/EmptyState', PriceTable from '@/components/wishlist/PriceTable'
    - Call getServerUser() to get authenticated user
    - Call getUserWishlist(user.id) to fetch wishlist items
    - For each item, fetch prices using getLatestPricesForCard(item.oracleId)
    - Calculate best price and trend (use query helpers from plan 03-01)
    - Render layout:
      - Page title "My Wishlist"
      - SearchBar component at top
      - Conditional: if wishlist.length === 0, show EmptyState
      - Conditional: if wishlist.length > 0, show CardGrid and PriceTable
    - Handle errors (show toast notification via Server Actions or client-side error boundary)

    Use Next.js 15 App Router pattern. Follow Server Component example from RESEARCH.md Pattern 1.

    File should be ~80 lines, use TypeScript types from src/types/wishlist.ts.

    Reference CONTEXT.md decision: "/wishlist - Full wishlist management page"
  </action>
  <verify>
    <automated>npm run build && grep -c "export default" src/app/wishlist/page.tsx</automated>
  </verify>
  <done>Wishlist page fetches data server-side, renders SearchBar + CardGrid + PriceTable, handles empty state</done>
</task>

<task type="auto" tdd="true">
  <name>Task 8: Update root layout and create home page</name>
  <files>src/app/layout.tsx, src/app/page.tsx</files>
  <behavior>
    - Root layout includes Header and Footer components
    - Root layout includes Toast provider for notifications
    - Home page shows overview (recent cards, quick stats)
    - Navigation links work (Logo, Wishlist, About)
  </behavior>
  <action>
    Update root layout and create home page.

    **src/app/layout.tsx:**
    - Import Header and Footer from @/components/layout/
    - Import Toaster from @/components/ui/toast
    - Update layout to include Header at top, Footer at bottom
    - Include Toaster component for toast notifications
    - Keep existing metadata and fonts

    **src/app/page.tsx:**
    - Create Server Component for home page
    - Import: getServerUser, getUserWishlist, Card from '@/components/ui/card'
    - Fetch user's wishlist (if authenticated)
    - Render overview:
      - Hero section with project description
      - Quick stats (total cards, best deals, price drops)
      - Call-to-action to visit /wishlist
      - Recent additions (last 5 cards added)
    - Style with Tailwind (centered layout, hero section)

    Use Shadcn/ui Card and Toast components. Follow dashboard layout from CONTEXT.md decision.

    Files should be ~40 lines each (layout), ~60 lines (home page).

    Reference CONTEXT.md decision: "/ - Home/Overview (wishlist + recent opportunities)"
  </action>
  <verify>
    <automated>npm run build && curl -I http://localhost:3000/</automated>
  </verify>
  <done>Layout includes Header/Footer, Toast notifications work, home page shows overview</done>
</task>

</tasks>

<verification>
- [ ] Shadcn/ui components installed (8 components in src/components/ui/)
- [ ] Header component with navigation links
- [ ] SearchBar with autocomplete functionality (300ms debounce)
- [ ] CardGrid displays wishlist in responsive grid (1-4 columns)
- [ ] PriceTable shows all 4 sources with best price highlighted
- [ ] EmptyState shows when wishlist is empty
- [ ] Wishlist page fetches data server-side (Server Component)
- [ ] Add/remove cards works via API calls
- [ ] Bulk removal with checkboxes and confirmation dialog
- [ ] Mobile responsive (grid collapses, table scrolls horizontally)
- [ ] Toast notifications for errors/success
</verification>

<success_criteria>
- User can manage wishlist via web interface (WISH-05 requirement complete)
- User can search cards by name (WISH-03 requirement complete)
- User can compare prices across 4 sources (DASH-01 requirement complete)
- User can view monitored cards list (DASH-02 requirement complete)
- UI works on desktop and mobile (responsive design)
- Ready for plan 03-04 (Telegram bot commands for wishlist management)
</success_criteria>

<output>
After completion, create `.planning/phases/03-user-interface-wishlist/03-03-SUMMARY.md`
</output>
