---
phase: 03-user-interface-wishlist
plan: 00
type: execute
wave: 0
depends_on: []
files_modified: [
  "vitest.config.ts",
  "package.json",
  "test/helpers/bot.ts",
  "test/helpers/db.ts",
  "test/helpers/auth.ts"
]
autonomous: true
requirements: []
must_haves:
  truths:
    - "Test stub files exist for all Phase 3 components"
    - "Vitest configuration supports both backend and frontend testing"
    - "Test helpers provide mock utilities for bot, database, and auth"
  artifacts:
    - path: "src/api/__tests__/wishlist.test.ts"
      provides: "Wishlist API endpoint test stubs"
      min_lines: 50
    - path: "src/api/__tests__/cards/search.test.ts"
      provides: "Card search API test stubs"
      min_lines: 40
    - path: "src/bot/__tests__/commands/add.test.ts"
      provides: "/add command test stubs"
      min_lines: 60
    - path: "src/bot/__tests__/commands/remove.test.ts"
      provides: "/remove command test stubs"
      min_lines: 50
    - path: "src/bot/__tests__/commands/list.test.ts"
      provides: "/list command test stubs"
      min_lines: 50
    - path: "src/bot/__tests__/commands/price.test.ts"
      provides: "/price command test stubs"
      min_lines: 40
    - path: "src/lib/wishlist/__tests__/queries.test.ts"
      provides: "Wishlist database query test stubs"
      min_lines: 60
    - path: "test/helpers/bot.ts"
      provides: "Mock grammY context factory"
      min_lines: 40
    - path: "test/helpers/db.ts"
      provides: "Database test helpers (seed, truncate)"
      min_lines: 50
    - path: "test/helpers/auth.ts"
      provides: "Auth helpers for API tests"
      min_lines: 30
  key_links:
    - from: "vitest.config.ts"
      to: "test environment"
      via: "environment: 'node' for backend, need jsdom for frontend components"
      pattern: "environment.*node"
---

<objective>
Set up test infrastructure for Phase 3 user interface components, including test stubs for all wishlist management features (web + bot), card search API, and price comparison display.

Purpose: Provide TDD foundation with RED tests ready for implementation. Follow Phase 2 pattern of creating test stubs first.
Output: 9 test stub files + 3 test helper files, Vitest configured for hybrid backend/frontend testing
</objective>

<execution_context>
@/Users/luizpansarini/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luizpansarini/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-user-interface-wishlist/03-CONTEXT.md
@.planning/phases/03-user-interface-wishlist/03-RESEARCH.md

# From Phase 2 (test infrastructure pattern)
@.planning/phases/02-core-data-collection/02-00-SUMMARY.md

# Existing test patterns
@vitest.config.ts
@test/setup.ts

# Database schema (for test data structure)
@src/db/schema/wishlists.ts
@src/db/schema/cards.ts
@src/db/schema/prices.ts

# Existing bot pattern (for mock helpers)
@src/bot/commands/start.ts
@src/bot/middleware/whitelist.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install missing test dependencies</name>
  <files>package.json</files>
  <action>
    Install @testing-library/user-event for user interaction simulation in component tests. This library is needed for simulating user input in search autocomplete and button clicks.

    Run: `pnpm add -D @testing-library/user-event`

    Verify package.json includes "@testing-library/user-event" in devDependencies.
  </action>
  <verify>
    <automated>grep "@testing-library/user-event" package.json</automated>
  </verify>
  <done>@testing-library/user-event installed in devDependencies</done>
</task>

<task type="auto">
  <name>Task 2: Create test helpers for bot, database, and auth mocking</name>
  <files>test/helpers/bot.ts, test/helpers/db.ts, test/helpers/auth.ts</files>
  <action>
    Create three test helper files to provide consistent mock utilities across Phase 3 tests.

    **test/helpers/bot.ts** - Mock grammY context factory:
    - `createMockContext(options)` - Creates a mock grammY Context with chat, message, from, and match properties
    - `createMockMessage(text, chatId)` - Helper to create mock message objects
    - Use same patterns as existing start.test.ts (if exists) or grammY testing patterns
    - Export types: MockContextOptions, MockMessageOptions

    **test/helpers/db.ts** - Database test helpers:
    - `truncateTable(db, table)` - Clears all rows from a table (use for test isolation)
    - `seedTestCard(db, cardData)` - Inserts a test card into cards table
    - `seedTestPrice(db, priceData)` - Inserts a test price into prices table
    - `seedTestWishlist(db, wishlistData)` - Inserts a test wishlist entry
    - Use Drizzle ORM queries, follow Phase 2 schema patterns

    **test/helpers/auth.ts** - Auth helpers for API tests:
    - `createTestToken(userId)` - Creates a JWT token for test user (use existing JWT_SECRET from test setup)
    - `createMockHeaders(token?)` - Creates mock Headers object with Authorization header
    - Reuse createToken from src/lib/auth.ts (don't duplicate JWT logic)

    Each helper should be ~30-50 lines, export 3-5 functions, include JSDoc comments.
  </action>
  <verify>
    <automated>npm run test:run test/helpers/bot.test.ts (if exists) or ls test/helpers/</automated>
  </verify>
  <done>Helper files created with exports, TypeScript types included</done>
</task>

<task type="auto">
  <name>Task 3: Create wishlist API test stubs</name>
  <files>src/api/__tests__/wishlist.test.ts</files>
  <action>
    Create test stub file for wishlist CRUD API endpoints (GET /api/wishlist, POST /api/wishlist, DELETE /api/wishlist/[card_id]).

    Use `test.skip()` for all stubs (TDD RED phase preparation pattern from Phase 2).

    **Test cases to stub:**
    1. "GET /api/wishlist returns user's wishlist with cards and prices" - integration
    2. "GET /api/wishlist returns empty array if no cards" - integration
    3. "POST /api/wishlist adds card to wishlist" - integration
    4. "POST /api/wishlist returns 409 if card already in wishlist" - integration
    5. "POST /api/wishlist returns 401 if not authenticated" - integration
    6. "DELETE /api/wishlist/[card_id] removes card from wishlist" - integration
    7. "DELETE /api/wishlist/[card_id] returns 404 if card not in wishlist" - integration

    Use test helpers from Task 2 (createTestToken, seedTestCard, seedTestWishlist).
    Include TODO comments referencing implementation plans (03-01, 03-03).

    Target: ~60 lines, 7 test stubs with descriptive names.
  </action>
  <verify>
    <automated>ls src/api/__tests__/wishlist.test.ts && grep "test.skip" src/api/__tests__/wishlist.test.ts | wc -l</automated>
  </verify>
  <done>File exists with 7 skipped test stubs, uses helpers, includes TODOs</done>
</task>

<task type="auto">
  <name>Task 4: Create card search API test stubs</name>
  <files>src/api/__tests__/cards/search.test.ts</files>
  <action>
    Create test stub file for card autocomplete search API endpoint (GET /api/cards/search?q=query).

    Use `test.skip()` for all stubs.

    **Test cases to stub:**
    1. "GET /api/cards/search returns matching cards by name" - unit
    2. "GET /api/cards/search returns empty array if no matches" - unit
    3. "GET /api/cards/search is case-insensitive" - unit
    4. "GET /api/cards/search limits results to 10 cards" - unit
    5. "GET /api/cards/search requires at least 2 characters" - unit

    Use test helpers (seedTestCard for creating test data).
    Include TODO comments referencing plan 03-02.

    Target: ~40 lines, 5 test stubs with descriptive names.
  </action>
  <verify>
    <automated>ls src/api/__tests__/cards/search.test.ts && grep "test.skip" src/api/__tests__/cards/search.test.ts | wc -l</automated>
  </verify>
  <done>File exists with 5 skipped test stubs, uses seedTestCard helper</done>
</task>

<task type="auto">
  <name>Task 5: Create bot command test stubs (add, remove, list, price)</name>
  <files>src/bot/__tests__/commands/add.test.ts, src/bot/__tests__/commands/remove.test.ts, src/bot/__tests__/commands/list.test.ts, src/bot/__tests__/commands/price.test.ts</files>
  <action>
    Create 4 test stub files for Telegram bot command handlers.

    **src/bot/__tests__/commands/add.test.ts** (/add command):
    Use `test.skip()` for all stubs.
    - "adds exact match card to wishlist" - integration
    - "shows selection list for multiple matches" - integration
    - "handles selection from numbered list" - integration
    - "returns error if no cards found" - unit
    - "returns error if card already in wishlist" - integration

    **src/bot/__tests__/commands/remove.test.ts** (/remove command):
    - "removes card by exact name" - integration
    - "removes card by index number" - integration
    - "returns error if card not in wishlist" - unit

    **src/bot/__tests__/commands/list.test.ts** (/list command):
    - "displays wishlist as numbered list" - integration
    - "shows empty message if wishlist is empty" - unit
    - "includes best price and trend for each card" - integration

    **src/bot/__tests__/commands/price.test.ts** (/price command):
    - "shows best price for queried card" - integration
    - "aggregates prices by oracle_id" - unit
    - "returns error if card not found" - unit

    Use test helpers from Task 2 (createMockContext, seedTestCard, seedTestPrice, seedTestWishlist).
    Include TODO comments referencing plan 03-04.

    Target: ~40-60 lines per file, 3-5 test stubs each.
  </action>
  <verify>
    <automated>ls src/bot/__tests__/commands/*.test.ts && grep -c "test.skip" src/bot/__tests__/commands/*.test.ts</automated>
  </verify>
  <done>4 files created with 3-5 skipped test stubs each, use bot helper</done>
</task>

<task type="auto">
  <name>Task 6: Create wishlist query and action test stubs</name>
  <files>src/lib/wishlist/__tests__/queries.test.ts, src/lib/wishlist/__tests__/actions.test.ts</files>
  <action>
    Create 2 test stub files for wishlist business logic.

    **src/lib/wishlist/__tests__/queries.test.ts** (Database queries):
    Use `test.skip()` for all stubs.
    - "getUserWishlist returns cards with metadata" - integration
    - "getUserWishlist returns empty array if user has no cards" - unit
    - "getLatestPricesForCard returns prices from all 4 sources" - integration
    - "getLatestPricesForCard returns null for sources without data" - unit
    - "findBestPrice returns lowest price across sources" - unit
    - "calculatePriceTrend returns % change vs 7 days ago" - unit

    **src/lib/wishlist/__tests__/actions.test.ts** (Server actions):
    - "addCardToWishlist inserts into wishlist table" - integration
    - "addCardToWishlist throws if card_id invalid" - unit
    - "removeCardFromWishlist deletes from wishlist table" - integration
    - "searchCards queries cards table by name" - unit

    Use test helpers (seedTestCard, seedTestPrice, seedTestWishlist, truncateTable).
    Include TODO comments referencing plans 03-01, 03-02.

    Target: ~50-60 lines per file, 4-6 test stubs each.
  </action>
  <verify>
    <automated>ls src/lib/wishlist/__tests__/*.test.ts && grep -c "test.skip" src/lib/wishlist/__tests__/*.test.ts</automated>
  </verify>
  <done>2 files created with 4-6 skipped test stubs each, use db helpers</done>
</task>

</tasks>

<verification>
- [ ] All 9 test stub files created in correct directories
- [ ] All 3 helper files created with exported functions
- [ ] @testing-library/user-event installed in package.json
- [ ] All test stubs use `test.skip()` (not `test()` or `it.todo()`)
- [ ] Test stubs include TODO comments referencing implementation plans
- [ ] Helper functions are typed with TypeScript
- [ ] Vitest configuration supports both node (backend) and jsdom (frontend) environments
</verification>

<success_criteria>
- Test infrastructure matches Phase 2 pattern (stub files first, implementation later)
- All Phase 3 requirements (WISH-01 through WISH-05, DASH-01, DASH-02) have corresponding test stubs
- Test helpers reduce duplication across test files (mock context factory, db seed helpers, auth helpers)
- Ready for TDD implementation in plans 03-01 through 03-04
</success_criteria>

<output>
After completion, create `.planning/phases/03-user-interface-wishlist/03-00-SUMMARY.md`
</output>
