---
phase: 03-user-interface-wishlist
plan: 02
type: execute
wave: 1
depends_on: ["03-00"]
files_modified: [
  "src/api/cards/search/route.ts",
  "src/api/cards/[oracle_id]/route.ts",
  "src/api/prices/[oracle_id]/route.ts"
]
autonomous: true
requirements: ["WISH-03", "DASH-01"]
must_haves:
  truths:
    - "User can search for cards by name via GET /api/cards/search?q=query"
    - "Search returns matching cards from database (case-insensitive)"
    - "Search requires at least 2 characters"
    - "Search limits results to 10 cards"
    - "User can get card details via GET /api/cards/[oracle_id]"
    - "User can compare prices across 4 sources via GET /api/prices/[oracle_id]"
    - "Price comparison returns latest price from each source"
    - "Price comparison highlights best price"
  artifacts:
    - path: "src/api/cards/search/route.ts"
      provides: "Card autocomplete search endpoint"
      exports: ["GET handler with query parameter"]
      min_lines: 50
    - path: "src/api/cards/[oracle_id]/route.ts"
      provides: "Card details endpoint"
      exports: ["GET handler with oracle_id parameter"]
      min_lines: 40
    - path: "src/api/prices/[oracle_id]/route.ts"
      provides: "Price comparison endpoint"
      exports: ["GET handler returning all 4 sources"]
      min_lines: 60
  key_links:
    - from: "src/api/cards/search/route.ts"
      to: "src/db/schema"
      via: "Drizzle ORM query on cards table with ILIKE"
      pattern: "ilike\\(cards\\.name.*query\\)"
    - from: "src/api/prices/[oracle_id]/route.ts"
      to: "src/lib/wishlist/queries.ts"
      via: "getLatestPricesForCard function"
      pattern: "getLatestPricesForCard"
    - from: "src/api/cards/search/route.ts"
      to: "src/components/wishlist/SearchBar.tsx"
      via: "fetch call for autocomplete dropdown"
      pattern: "fetch.*api/cards/search"
---

<objective>
Build card search and price comparison API endpoints. Card search provides autocomplete functionality for wishlist management (WISH-03), while price comparison enables users to view prices from all 4 sources side-by-side (DASH-01).

Purpose: Implement WISH-03 (search cards by name) and DASH-01 (compare prices across sources) requirements with efficient database queries.
Output: 3 API endpoints (search, card details, price comparison) optimized for frontend consumption
</objective>

<execution_context>
@/Users/luizpansarini/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luizpansarini/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-user-interface-wishlist/03-CONTEXT.md
@.planning/phases/03-user-interface-wishlist/03-RESEARCH.md

# Database schema
@src/db/schema/cards.ts
@src/db/schema/prices.ts

# Existing query patterns from Phase 2
@src/scraper/scryfall.ts

# Wishlist queries from plan 03-01
@src/lib/wishlist/queries.ts

# Test infrastructure
@test/helpers/db.ts
@test/helpers/auth.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement card search API endpoint</name>
  <files>src/api/cards/search/route.ts</files>
  <behavior>
    - GET /api/cards/search?q=query searches cards by name
    - Search is case-insensitive (ILIKE query)
    - Search requires at least 2 characters (returns 400 if less)
    - Search limits results to 10 cards
    - Returns array of cards with oracleId, name, set, imageUrl
    - Returns empty array if no matches found
    - No authentication required (public endpoint for autocomplete)
  </behavior>
  <action>
    Create src/app/api/cards/search/route.ts with GET handler for card autocomplete search.

    **GET handler:**
    - Extract query parameter: `const { searchParams } = new URL(request.url); const q = searchParams.get('q')`
    - Validate query: if `!q || q.length < 2`, return 400 with error message
    - Import cards schema from src/db/schema/, db from src/db/index.ts
    - Use Drizzle ORM: `db.select({ oracleId: cards.oracleId, name: cards.name, set: cards.set, imageUrl: cards.imageUrl }).from(cards).where(ilike(cards.name, `%${q}%`)).limit(10)`
    - Return NextResponse.json({ cards: results }, { status: 200 })
    - Handle errors: return NextResponse.json({ error: string }, { status: 500 })

    Use PostgreSQL ILIKE for case-insensitive search. Follow Scryfall search pattern from Phase 2 (src/scraper/scryfall.ts bulk data import).

    File should be ~50 lines, 1 exported handler (GET).

    Note: This endpoint doesn't require authentication since it's used for autocomplete in the search bar (per CONTEXT.md decision).
  </action>
  <verify>
    <automated>npm run test:run src/api/__tests__/cards/search.test.ts</automated>
  </verify>
  <done>Search endpoint returns matching cards, case-insensitive, 10 result limit, 2-char minimum</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement card details API endpoint</name>
  <files>src/app/api/cards/[oracle_id]/route.ts</files>
  <behavior>
    - GET /api/cards/[oracle_id] returns full card metadata
    - Returns card with oracleId, name, set, rarity, color, imageUrl
    - Returns 404 if card not found
    - No authentication required (public endpoint)
  </behavior>
  <action>
    Create src/app/api/cards/[oracle_id]/route.ts with GET handler for individual card details.

    **GET handler:**
    - Extract oracle_id from route params: `const { oracle_id } = await params` (Next.js 15 App Router pattern)
    - Import cards schema from src/db/schema/, db from src/db/index.ts
    - Use Drizzle ORM: `db.select().from(cards).where(eq(cards.oracleId, oracle_id)).limit(1)`
    - If no results, return NextResponse.json({ error: 'Card not found' }, { status: 404 })
    - Return NextResponse.json({ card: result[0] }, { status: 200 })
    - Handle errors: return NextResponse.json({ error: string }, { status: 500 })

    Use Next.js App Router dynamic route pattern. Directory name must be `[oracle_id]` exactly.

    File should be ~40 lines, 1 exported handler (GET).

    Note: This endpoint provides metadata for the card details page (per CONTEXT.md decision - separate page for full details).
  </action>
  <verify>
    <automated>npm run test:run src/api/__tests__/cards/details.test.ts (if created) or curl -I http://localhost:3000/api/cards/test-oracle-id</automated>
  </verify>
  <done>Card details endpoint returns full metadata, 404 for invalid oracle_id</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement price comparison API endpoint</name>
  <files>src/app/api/prices/[oracle_id]/route.ts</files>
  <behavior>
    - GET /api/prices/[oracle_id] returns latest prices from all 4 sources
    - Returns prices object with ligaMagic, tcgplayer, cardmarket, cardkingdom
    - Returns best price (lowest of 4 sources)
    - Returns price trend vs 7 days ago
    - Returns 404 if card not found or no price data
    - No authentication required (public endpoint)
  </behavior>
  <action>
    Create src/app/api/prices/[oracle_id]/route.ts with GET handler for price comparison across all sources.

    **GET handler:**
    - Extract oracle_id from route params: `const { oracle_id } = await params`
    - Import `getLatestPricesForCard`, `getBestPrice`, `calculatePriceTrend` from src/lib/wishlist/queries.ts
    - Import price history query (or query prices table directly for trend calculation)
    - Call `getLatestPricesForCard(oracle_id)` to fetch latest prices from all 4 sources
    - Call `getBestPrice(prices)` to find lowest price
    - Query price history for trend: `db.select().from(prices).where(eq(prices.cardId, oracle_id)).orderBy(desc(prices.timestamp)).limit(100)` (get ~25 days of data for 4 sources)
    - Call `calculatePriceTrend(bestPrice.priceBrl, priceHistory)` to get trend
    - If all prices are null, return 404 with error "No price data found for card"
    - Return NextResponse.json({ oracleId, prices: { ligaMagic, tcgplayer, cardmarket, cardkingdom }, bestPrice, trend }, { status: 200 })
    - Handle errors: return NextResponse.json({ error: string }, { status: 500 })

    Use query functions from plan 03-01. Follow price comparison pattern from RESEARCH.md (show all 4 sources, highlight best).

    File should be ~60 lines, 1 exported handler (GET).

    Note: This endpoint enables DASH-01 requirement (compare prices across 4 sources) and will be used by the price comparison table in plan 03-03.
  </action>
  <verify>
    <automated>npm run test:run src/api/__tests__/prices/comparison.test.ts (if created) or curl http://localhost:3000/api/prices/test-oracle-id</automated>
  </verify>
  <done>Price comparison endpoint returns all 4 sources, best price highlighted, trend calculated</done>
</task>

</tasks>

<verification>
- [ ] Card search endpoint returns autocomplete results (case-insensitive)
- [ ] Search requires minimum 2 characters, limits to 10 results
- [ ] Card details endpoint returns full metadata for oracle_id
- [ ] Price comparison endpoint returns all 4 sources with latest prices
- [ ] Best price correctly identified as minimum across sources
- [ ] Price trend calculated vs 7 days ago (up/down/stable with % change)
- [ ] All endpoints return 404 for invalid oracle_id
- [ ] All tests from plan 03-00 pass
</verification>

<success_criteria>
- Card search API works for autocomplete dropdown in SearchBar component
- Card details API provides metadata for individual card page
- Price comparison API enables DASH-01 requirement (view all 4 sources)
- Query performance is acceptable (use database indexes from Phase 1)
- Ready for plan 03-03 (web UI components consume these endpoints)
</success_criteria>

<output>
After completion, create `.planning/phases/03-user-interface-wishlist/03-02-SUMMARY.md`
</output>
