---
phase: 03-user-interface-wishlist
plan: 04
type: execute
wave: 2
depends_on: ["03-00", "03-01"]
files_modified: [
  "src/bot/commands/add.ts",
  "src/bot/commands/remove.ts",
  "src/bot/commands/list.ts",
  "src/bot/commands/price.ts",
  "src/bot/middleware/rate-limit.ts",
  "src/bot/utils/format.ts",
  "src/bot/utils/search.ts"
]
autonomous: true
requirements: ["WISH-01", "WISH-02", "WISH-03", "WISH-04"]
must_haves:
  truths:
    - "/add command searches cards and adds to wishlist"
    - "/add shows selection list for multiple matches"
    - "/add auto-adds exact matches without selection"
    - "/remove command removes card by name or index"
    - "/list command displays wishlist as numbered list"
    - "/list shows best price and trend for each card"
    - "/price command shows best price for queried card"
    - "/price aggregates prices by oracle_id (multiple printings)"
    - "Bot commands work with same database as web UI"
    - "Bot rate limiting prevents spam (10 requests/minute)"
  artifacts:
    - path: "src/bot/commands/add.ts"
      provides: "/add command handler with search + select flow"
      min_lines: 80
    - path: "src/bot/commands/remove.ts"
      provides: "/remove command handler"
      min_lines: 50
    - path: "src/bot/commands/list.ts"
      provides: "/list command handler"
      min_lines: 60
    - path: "src/bot/commands/price.ts"
      provides: "/price command handler"
      min_lines: 50
    - path: "src/bot/middleware/rate-limit.ts"
      provides: "Bot-specific rate limiting middleware"
      min_lines: 40
    - path: "src/bot/utils/format.ts"
      provides: "Message formatting helpers"
      min_lines: 40
    - path: "src/bot/utils/search.ts"
      provides: "Shared card search logic"
      min_lines: 40
  key_links:
    - from: "src/bot/commands/add.ts"
      to: "src/lib/wishlist/queries.ts"
      via: "addCardToWishlist function"
      pattern: "addCardToWishlist"
    - from: "src/bot/commands/list.ts"
      to: "src/lib/wishlist/queries.ts"
      via: "getUserWishlist, getLatestPricesForCard functions"
      pattern: "getUserWishlist|getLatestPricesForCard"
    - from: "src/bot/commands/add.ts"
      to: "src/db/schema"
      via: "Drizzle ORM query on cards table"
      pattern: "db\\.select.*from.*cards"
    - from: "src/bot/commands/price.ts"
      to: "src/lib/wishlist/queries.ts"
      via: "getLatestPricesForCard, getBestPrice functions"
      pattern: "getLatestPricesForCard|getBestPrice"
---

<objective>
Implement Telegram bot commands for wishlist management (/add, /remove, /list, /price) with search, selection, and formatting utilities. Complete WISH-04 requirement (manage wishlist via bot) using same database queries as web UI.

Purpose: Enable wishlist management through Telegram bot interface (WISH-04 requirement) with conversational flows for search and selection.
Output: 4 command handlers, 2 utility files, 1 rate limiting middleware, registered in bot menu
</objective>

<execution_context>
@/Users/luizpansarini/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luizpansarini/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-user-interface-wishlist/03-CONTEXT.md
@.planning/phases/03-user-interface-wishlist/03-RESEARCH.md

# Existing bot patterns from Phase 1
@src/bot/index.ts (grammY bot instance)
@src/bot/commands/start.ts (/start command pattern)
@src/bot/middleware/whitelist.ts (middleware pattern)
@src/lib/telegram.ts (bot instance)

# Database queries from plan 03-01
@src/lib/wishlist/queries.ts

# Test infrastructure from plan 03-00
@test/helpers/bot.ts (mock context factory)
@test/helpers/db.ts (database helpers)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create bot utility functions (format and search)</name>
  <files>src/bot/utils/format.ts, src/bot/utils/search.ts</files>
  <behavior>
    - format.ts provides message formatting helpers (price, trend, card list)
    - search.ts provides shared card search logic (query cards table)
    - Both functions are used across multiple bot commands
    - Format functions handle edge cases (null prices, missing data)
  </behavior>
  <action>
    Create utility files for bot message formatting and card search.

    **src/bot/utils/format.ts** - Message formatting helpers:
    - `formatPrice(priceBrl: number | null)` - Formats price as "R$ X.XX" or "N/A" if null
    - `formatTrend(trend: PriceTrend)` - Formats trend as "↑X%" (red), "↓X%" (green), "±" (gray)
    - `formatCardWithPrice(card: WishlistWithPrices)` - Formats card entry for /list command: "📈 [N] Card Name - R$ X (Source) - ↑10%"
    - `formatPriceComparison(prices: object)` - Formats all 4 sources for /price command: "Liga Magic: R$ X\nTCGPlayer: R$ Y\n..."
    - Export all functions, use emoji for visual appeal (📈, 💰, ✅, ❌)

    **src/bot/utils/search.ts** - Shared card search logic:
    - `searchCardsByName(query: string)` - Queries cards table with ILIKE, limits to 10 results
    - `findExactMatch(cards: Card[], query: string)` - Returns card if exact name match exists
    - `formatCardList(cards: Card[])` - Formats numbered list for selection: "1. Card Name (Set)\n2. ..."
    - Import db from src/db/index.ts, cards schema from src/db/schema/
    - Use Drizzle ORM: `db.select().from(cards).where(ilike(cards.name, `%${query}%`)).limit(10)`

    Use query functions from plan 03-01. Follow formatting pattern from CONTEXT.md decision.

    Files should be ~40 lines each, export 3-4 functions, handle nulls gracefully.

    Reference CONTEXT.md decisions:
    - "/list format: '📈 [N] Card Name - R$ X (Source) - ↑10%'"
    - "/price format: 'Black Lotus - R$ 5000 (Liga Magic) - ↑10%'"
  </action>
  <verify>
    <automated>npm run test:run src/bot/__tests__/utils/format.test.ts (if created) or grep -c "export" src/bot/utils/*.ts</automated>
  </verify>
  <done>Utility functions created, format helpers handle edge cases, search queries database correctly</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create bot rate limiting middleware</name>
  <files>src/bot/middleware/rate-limit.ts</files>
  <behavior>
    - Rate limit middleware checks user's request count
    - Uses Redis-backed rate limiter from Phase 1
    - Limits to 10 requests per minute per user
    - Returns "Too many requests" message if limit exceeded
    - Applied to /list and /price commands (not /add which has conversational flow)
  </behavior>
  <action>
    Create src/bot/middleware/rate-limit.ts for bot-specific rate limiting.

    Middleware structure:
    - Import: rateLimit from src/lib/ratelimit/rate-limiter.ts
    - Create grammY middleware function: `rateLimitMiddleware = async (ctx, next) => { ... }`
    - Extract chatId: `const chatId = ctx.chat?.id`
    - Check rate limit: `const allowed = await rateLimit(\`bot:${chatId}\`, 10, 60)` (10 requests per 60 seconds)
    - If not allowed: `await ctx.reply('Too many requests. Please wait a minute.')` and return early
    - If allowed: call `await next()` to continue to command handler
    - Export middleware as `rateLimitMiddleware`

    Use existing Redis rate limiter from Phase 1. Follow middleware pattern from src/bot/middleware/whitelist.ts.

    File should be ~40 lines, export middleware function, handle missing chatId gracefully.

    Reference RESEARCH.md Pitfall 5: "Telegram rate limits (30 msgs/sec, 20 msgs/min) - bot needs rate limiting"

    Note: This is per-user rate limiting to prevent spam, different from Phase 1's IP-based rate limiting for API routes.
  </action>
  <verify>
    <automated>npm run test:run src/bot/__tests__/middleware/rate-limit.test.ts (if created) or grep -c "rateLimit" src/bot/middleware/rate-limit.ts</automated>
  </verify>
  <done>Rate limiting middleware created, uses Redis, limits 10 req/min per user</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement /add command with search + select flow</name>
  <files>src/bot/commands/add.ts</files>
  <behavior>
    - /add command accepts card name as argument
    - Searches database for matching cards
    - If exact match found: auto-add to wishlist
    - If multiple matches: show numbered list, wait for user reply
    - User replies with number to select card
    - Adds selected card to wishlist
    - Confirms with success message or shows error
    - Handles duplicate cards (already in wishlist)
    - Handles not found (no cards match query)
  </behavior>
  <action>
    Create src/bot/commands/add.ts with conversational search + select flow.

    Command structure:
    - Import: bot from src/lib/telegram.ts, Context from 'grammy', db from src/db/index.ts, cards and wishlists schema from src/db/schema/, addCardToWishlist from src/lib/wishlist/queries.ts, searchCardsByName, findExactMatch, formatCardList from src/bot/utils/search.ts
    - Create conversation state storage: `const pendingSearches = new Map<number, typeof cards.$inferSelect[]>()`
    - Register command: `bot.command('add', async (ctx: Context) => { ... })`
    - Extract query: `const query = ctx.match` (text after /add)
    - If no query: reply "Usage: /add <card name>" and return
    - Call `searchCardsByName(query)` to get matching cards
    - If no results: reply "No cards found. Try a different search term." and return
    - Call `findExactMatch(results, query)` to check for exact match
    - If exact match exists: call `addCardToWishlist(userId, card.oracleId)`, handle duplicate error, reply success/error
    - If multiple matches: format numbered list with `formatCardList(results)`, reply with list, store results in pendingSearches Map keyed by chatId
    - Register message handler for number replies: `bot.on('msg:text', async (ctx: Context) => { ... })`
    - In message handler: check if chatId has pending search, parse number, validate selection, call addCardToWishlist, clear pending search
    - Add TTL to pending searches (10 minutes) to prevent memory leaks

    Use grammY conversation pattern from RESEARCH.md Pattern 2. Follow /add command specification from CONTEXT.md.

    File should be ~80 lines, handle all edge cases (no query, no results, exact match, multiple matches, duplicate cards).

    Reference CONTEXT.md decision: "/add Command: Search + Select Pattern"
  </action>
  <verify>
    <automated>npm run test:run src/bot/__tests__/commands/add.test.ts</automated>
  </verify>
  <done>/add command searches, shows selection list, handles replies, adds cards to wishlist</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Implement /remove command</name>
  <files>src/bot/commands/remove.ts</files>
  <behavior>
    - /remove command accepts card name OR index number
    - If name provided: searches for exact match, removes if found
    - If number provided: removes card at that index from /list
    - Returns error if card not in wishlist
    - Returns error if index out of range
    - Confirms removal with card name
  </behavior>
  <action>
    Create src/bot/commands/remove.ts for removing cards from wishlist.

    Command structure:
    - Import: bot from src/lib/telegram.ts, Context from 'grammy', db from src/db/index.ts, wishlists schema from src/db/schema/, removeCardFromWishlist from src/lib/wishlist/queries.ts, getUserWishlist from src/lib/wishlist/queries.ts
    - Register command: `bot.command('remove', async (ctx: Context) => { ... })`
    - Extract argument: `const arg = ctx.match` (text after /remove)
    - If no argument: reply "Usage: /remove <card name or index>" and return
    - Check if arg is number: `const isNumber = /^\d+$/.test(arg)`
    - If number:
      - Parse as index: `const index = parseInt(arg)`
      - Call `getUserWishlist(userId)` to get wishlist
      - Check if index valid (1 <= index <= wishlist.length)
      - Get card at index: `const card = wishlist[index - 1]`
      - Call `removeCardFromWishlist(userId, card.oracleId)`
      - Reply success or error
    - If not number (card name):
      - Call `getUserWishlist(userId)` to get wishlist
      - Search for exact name match (case-insensitive)
      - If found: call `removeCardFromWishlist(userId, card.oracleId)`, reply success
      - If not found: reply "Card not in wishlist. Use /list to see your wishlist."

    Use query functions from plan 03-01. Follow /remove command specification from CONTEXT.md.

    File should be ~50 lines, handle both name and index removal, validate inputs.

    Reference CONTEXT.md decision: "/remove Command: Exact Name Match or index from /list"
  </action>
  <verify>
    <automated>npm run test:run src/bot/__tests__/commands/remove.test.ts</automated>
  </verify>
  <done>/remove command works with both card names and index numbers, validates inputs</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Implement /list command with rate limiting</name>
  <files>src/bot/commands/list.ts</files>
  <behavior>
    - /list command displays user's wishlist as numbered list
    - Shows card name, best price, source, trend for each card
    - Uses rate limiting middleware (10 req/min)
    - Shows empty message if wishlist is empty
    - Formats prices with emoji and colors (↑↓)
  </behavior>
  <action>
    Create src/bot/commands/list.ts for displaying wishlist in Telegram.

    Command structure:
    - Import: bot from src/lib/telegram.ts, Context from 'grammy', getUserWishlist from src/lib/wishlist/queries.ts, getLatestPricesForCard and getBestPrice from src/lib/wishlist/queries.ts, formatCardWithPrice from src/bot/utils/format.ts, rateLimitMiddleware from src/bot/middleware/rate-limit.ts
    - Register command with middleware: `bot.command('list', rateLimitMiddleware, async (ctx: Context) => { ... })`
    - Get userId from context (single-user mode: userId = 1)
    - Call `getUserWishlist(userId)` to get wishlist
    - If wishlist empty: reply "Your wishlist is empty. Use /add to start tracking cards!" and return
    - For each wishlist item:
      - Call `getLatestPricesForCard(item.oracleId)` to get prices
      - Call `getBestPrice(prices)` to find best price
      - Format card entry with `formatCardWithPrice(item, bestPrice, trend)`
      - Add to message string with index number
    - Reply with formatted list (Telegram message)
    - Handle long messages (split into multiple messages if > 4096 characters)

    Use query functions from plan 03-01 and format helpers from Task 1. Apply rate limiting middleware.

    File should be ~60 lines, handle empty wishlist, format messages with emoji, split long messages if needed.

    Reference CONTEXT.md decision: "/list Command: Text Format - '📈 [N] Card Name - R$ X (Source) - ↑10%'"
  </action>
  <verify>
    <automated>npm run test:run src/bot/__tests__/commands/list.test.ts</automated>
  </verify>
  <done>/list command shows numbered wishlist with prices/trends, rate limited, handles empty state</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Implement /price command</name>
  <files>src/bot/commands/price.ts</files>
  <behavior>
    - /price command accepts card name as argument
    - Shows current best price across all 4 sources
    - Aggregates prices by oracle_id (multiple printings)
    - Returns error if card not found
    - Shows price trend vs last week
  </behavior>
  <action>
    Create src/bot/commands/price.ts for checking card prices.

    Command structure:
    - Import: bot from src/lib/telegram.ts, Context from 'grammy', db from src/db/index.ts, cards schema from src/db/schema/, getLatestPricesForCard and getBestPrice from src/lib/wishlist/queries.ts, calculatePriceTrend from src/lib/wishlist/queries.ts, formatPrice, formatTrend from src/bot/utils/format.ts
    - Register command: `bot.command('price', async (ctx: Context) => { ... })`
    - Extract query: `const query = ctx.match` (text after /price)
    - If no query: reply "Usage: /price <card name>" and return
    - Search for card: `db.select().from(cards).where(ilike(cards.name, `%${query}%`)).limit(1)`
    - If no results: reply "Card not found. Try the exact card name." and return
    - Get card oracleId: `const oracleId = result[0].oracleId`
    - Call `getLatestPricesForCard(oracleId)` to get prices from all 4 sources
    - Call `getBestPrice(prices)` to find lowest price
    - Call `calculatePriceTrend(bestPrice.priceBrl, priceHistory)` to get trend
    - Format message: "Card Name - R$ X (Source) - ↑10%" using format helpers
    - Reply with formatted message

    Use query functions from plan 03-01 and format helpers from Task 1. Follow /price command specification from CONTEXT.md.

    File should be ~50 lines, handle not found case, format price message with trend.

    Reference CONTEXT.md decision: "/price Command: Best Price Only - 'Black Lotus - R$ 5000 (Liga Magic) - ↑10%'"
  </action>
  <verify>
    <automated>npm run test:run src/bot/__tests__/commands/price.test.ts</automated>
  </verify>
  <done>/price command shows best price, source, trend, handles not found case</done>
</task>

<task type="auto" tdd="true">
  <name>Task 7: Register bot commands and update bot menu</name>
  <files>src/bot/index.ts</files>
  <behavior>
    - All 4 commands imported and registered in bot
    - Bot menu updated with /add, /remove, /list, /price commands
    - Bot startup logs command registration
  </behavior>
  <action>
    Update src/bot/index.ts to register new bot commands.

    Changes to make:
    - Import command handlers: `import './commands/add'`, `import './commands/remove'`, `import './commands/list'`, `import './commands/price'`
    - Update bot.api.setMyCommands call to include all commands:
      ```typescript
      bot.api.setMyCommands([
        { command: 'start', description: 'Authenticate with bot' },
        { command: 'add', description: 'Add card to wishlist' },
        { command: 'remove', description: 'Remove card from wishlist' },
        { command: 'list', description: 'View your wishlist' },
        { command: 'price', description: 'Check card price' }
      ])
      ```
    - Add startup log to confirm all commands registered

    Commands are auto-registered when their modules are imported (side effect of calling bot.command()).

    File should be ~20 lines modified, keep existing whitelist middleware and startup logic.

    Reference CONTEXT.md decision: "Bot Command Registration - Commands: /start (auth), /add, /remove, /list, /price"
  </action>
  <verify>
    <automated>grep -c "import.*commands" src/bot/index.ts && npm run bot:dev (check startup logs for command registration)</automated>
  </verify>
  <done>All 4 commands imported, bot menu updated, startup confirms registration</done>
</task>

</tasks>

<verification>
- [ ] All 4 command handlers created (/add, /remove, /list, /price)
- [ ] Utility functions for formatting and search work correctly
- [ ] Rate limiting middleware applied to /list and /price commands
- [ ] /add command handles conversational flow (search → select → add)
- [ ] /remove command works with both names and index numbers
- [ ] /list command displays wishlist with prices and trends
- [ ] /price command shows best price across all 4 sources
- [ ] All commands use same database queries as web UI
- [ ] Bot menu updated with all commands
- [ ] All tests from plan 03-00 pass
</verification>

<success_criteria>
- User can manage wishlist via Telegram bot (WISH-04 requirement complete)
- Bot commands use same data as web UI (shared queries from plan 03-01)
- Conversational flows work (search + select for /add)
- Rate limiting prevents spam (10 req/min per user)
- Messages formatted with emoji and trends (↑↓)
- Phase 3 complete: web UI (plan 03-03) + bot UI (plan 03-04) both functional
</success_criteria>

<output>
After completion, create `.planning/phases/03-user-interface-wishlist/03-04-SUMMARY.md`
</output>
