---
phase: 10
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scheduler/jobs.ts
  - src/scheduler/__tests__/jobs.test.ts
autonomous: true
requirements: [TEST-10, TEST-11]
tags: [testing, scheduler, node-cron, vitest, test-activation]

must_haves:
  truths:
    - "src/scheduler/jobs.ts schedulePriceCollection throws Error('Invalid cron expression: ...') when cron.validate returns false for any of morning/afternoon/evening expressions"
    - "src/scheduler/jobs.ts executePriceCollection return type includes durationMs: number and every return path (early isRunning, no-cards, success, error) sets it"
    - "All 3 test.skip stubs in describe('schedulePriceCollection') are replaced with active it(...) bodies and pass"
    - "All 4 test.skip stubs in describe('executePriceCollection') are replaced with active it(...) bodies and pass"
    - "The 9 remaining stubs (validateScheduleTimes ×3, stopScheduler ×3, Integration scenarios ×3) stay as test.skip and are NOT activated"
    - "The 5 pre-existing scheduleMetagameRefresh tests still pass unchanged"
    - "pnpm test:run src/scheduler/__tests__/jobs.test.ts reports 12 passed / 9 skipped (was 5 passed / 16 skipped)"
    - "No real network, no real DB writes, no real Telegram calls during scheduler tests — all side effects mocked"
  artifacts:
    - path: "src/scheduler/jobs.ts"
      provides: "schedulePriceCollection with cron.validate guard + executePriceCollection with durationMs tracking"
      contains: "Invalid cron expression"
    - path: "src/scheduler/__tests__/jobs.test.ts"
      provides: "7 newly active tests + node-cron mock extended with validate + @/lib/opportunities mock"
      contains: "should handle invalid cron expressions"
      min_lines: 280
  key_links:
    - from: "src/scheduler/jobs.ts"
      to: "node-cron"
      via: "cron.validate(expr) guard before cron.schedule calls"
      pattern: "cron\\.validate\\("
    - from: "src/scheduler/__tests__/jobs.test.ts"
      to: "src/scheduler/jobs.ts"
      via: "dynamic await import('../jobs') inside each it() body"
      pattern: "await import\\('\\.\\./jobs'\\)"
    - from: "src/scheduler/__tests__/jobs.test.ts"
      to: "@/lib/opportunities"
      via: "vi.mock factory exposing detectOpportunitiesForWishlist, sendDigestAndPersist, loadDetectionConfig"
      pattern: "vi\\.mock\\('@/lib/opportunities'"
---

<objective>
Activate the 7 `test.skip` stubs in `src/scheduler/__tests__/jobs.test.ts` that cover `schedulePriceCollection` (3 stubs) and `executePriceCollection` (4 stubs), delivering TEST-10 and TEST-11 from milestone v1.1.

Two minimal implementation additions to `src/scheduler/jobs.ts` make the tests verifiable:
1. `cron.validate()` guard at the top of `schedulePriceCollection` — throws `Error("Invalid cron expression: ${expr}")` before any `cron.schedule` call when any of the three schedule expressions is invalid.
2. `durationMs` tracking in `executePriceCollection` — capture `Date.now()` at function entry, compute elapsed at each return path, add `durationMs: number` to the return type.

Purpose: Existing scheduler production code (~165 LOC) is currently unverified for the price-collection function family. These 7 active tests verify cron registration, custom schedule override, invalid expression rejection, orchestration execution, error handling, duration metrics, and the concurrency guard — closing the v1.1 test-coverage gap.

Output:
- `src/scheduler/jobs.ts`: 2 small additions (validate guard + durationMs), no behavioral change to happy path
- `src/scheduler/__tests__/jobs.test.ts`: 7 newly active tests, extended `node-cron` mock, new `@/lib/opportunities` mock
- Net test count change: 5 passed → 12 passed; 16 skipped → 9 skipped in this file
- The 9 remaining stubs (`validateScheduleTimes`, `stopScheduler`, `Integration scenarios`) stay skipped per D-01
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-scheduler-tests/10-CONTEXT.md
@.planning/phases/10-scheduler-tests/10-RESEARCH.md
@.planning/phases/10-scheduler-tests/10-PATTERNS.md
@.planning/phases/10-scheduler-tests/10-VALIDATION.md

# Prior test-activation phase patterns (test.skip → active conversion + biome import order)
@.planning/phases/08-circuit-breaker-tests/08-01-PLAN.md

# Source under modification — executor MUST read full file before editing
@src/scheduler/jobs.ts

# Test file with the 7 stubs to activate (and the 5 pre-existing scheduleMetagameRefresh tests that MUST remain green)
@src/scheduler/__tests__/jobs.test.ts

# Re-export entry — verify TypeScript return type propagates
@src/scheduler/index.ts

# Canonical vi.resetModules() + vi.doMock() precedent in this project
@src/scraper/__tests__/orchestrator.test.ts

# Test setup (TELEGRAM_BOT_TOKEN, JWT_SECRET globals)
@test/setup.ts

# Vitest config (timeout, environment, globals)
@vitest.config.ts

<interfaces>
<!-- Key contracts the executor must respect. Extracted from src/scheduler/jobs.ts and node_modules/node-cron. -->

From src/scheduler/jobs.ts (CURRENT — pre-change):
```typescript
export async function executePriceCollection(): Promise<{
  total: number
  fetched: number
  skipped: number
  failed: number
}>

export function schedulePriceCollection(): {
  start: () => void
  stop: () => void
}

export function scheduleMetagameRefresh(): {
  start: () => void
  stop: () => void
}

export { default } from './jobs'  // via index.ts; default = executePriceCollection
```

From src/scheduler/jobs.ts (POST-CHANGE — required by D-06):
```typescript
export async function executePriceCollection(): Promise<{
  total: number
  fetched: number
  skipped: number
  failed: number
  durationMs: number   // NEW — required field on all return paths
}>
```

From node-cron@4.2.1 (VERIFIED via live exec):
- `cron.validate(expr: string): boolean` — returns `false` for invalid expressions (does NOT throw)
- `cron.schedule(expr, fn, options)` — already used; unchanged
- Default import: `import cron from 'node-cron'` (synthetic default); mock must place `validate` on the `default` object

From @/lib/opportunities (VERIFIED — called inside executePriceCollection lines 130–133):
- `loadDetectionConfig(): DetectionConfig` — synchronous
- `detectOpportunitiesForWishlist(userId: number, config): Promise<Opportunity[]>`
- `sendDigestAndPersist(opportunities): Promise<{ persisted: number; sent: boolean; error?: string }>`

</interfaces>

</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add cron.validate guard and durationMs tracking to jobs.ts</name>
  <files>src/scheduler/jobs.ts</files>
  <read_first>
    - src/scheduler/jobs.ts (full file — must see all existing return paths and the isRunning guard before modifying)
    - .planning/phases/10-scheduler-tests/10-CONTEXT.md (D-03, D-05, D-06 locked decisions)
    - .planning/phases/10-scheduler-tests/10-PATTERNS.md ("jobs.ts — cron.validate() guard" + "jobs.ts — durationMs tracking" sections)
    - .planning/phases/10-scheduler-tests/10-RESEARCH.md ("Pitfall 1: node-cron validate returns false, not throws" + "Pitfall 5: durationMs computed in finally vs at each return path")
    - src/scheduler/index.ts (verify named re-export propagates return type without manual edit)
  </read_first>
  <behavior>
    - schedulePriceCollection iterates morningSchedule, afternoonSchedule, eveningSchedule and calls cron.validate(expr) on each (per D-03)
    - First expression where cron.validate returns false causes schedulePriceCollection to throw new Error(`Invalid cron expression: ${expr}`) BEFORE any cron.schedule call
    - When all three expressions are valid (cron.validate returns true), schedulePriceCollection proceeds to call cron.schedule three times exactly as today — no behavioral regression
    - executePriceCollection captures const startTime = Date.now() as its first statement, before the isRunning guard (per D-05)
    - Return type of executePriceCollection includes durationMs: number alongside total, fetched, skipped, failed (per D-06)
    - Early "already running" return path returns durationMs: 0 (Claude's discretion per CONTEXT.md — must be a literal 0, not Date.now() - startTime, so the concurrency test's exact equality assertion holds)
    - No-cards early return path computes const durationMs = Date.now() - startTime, logs logger.info(`Price collection took ${durationMs}ms`), and includes durationMs in the returned object
    - Success return path (after fetchAllPrices + opportunity detection) computes const durationMs = Date.now() - startTime, logs logger.info(`Price collection took ${durationMs}ms`), and includes durationMs in the returned object
    - Error catch return path computes const durationMs = Date.now() - startTime, logs logger.info(`Price collection took ${durationMs}ms`), and includes durationMs in the returned object
    - scheduleMetagameRefresh is NOT modified — phase scope is strictly schedulePriceCollection + executePriceCollection
  </behavior>
  <action>
    Modify src/scheduler/jobs.ts with TWO additions per D-03, D-05, D-06.

    Addition A — cron.validate guard in schedulePriceCollection (D-03, D-04):
    Insert a guard block after the three schedule constants are read (morningSchedule, afternoonSchedule, eveningSchedule at lines ~196–198 of the current file) and BEFORE the existing logger.info call at line ~200. The guard iterates [morningSchedule, afternoonSchedule, eveningSchedule]; for any expression where `cron.validate(expr)` returns false, immediately `throw new Error(\`Invalid cron expression: ${expr}\`)`. The exact thrown message MUST start with the literal prefix "Invalid cron expression:" so the test assertion `toThrow('Invalid cron expression')` matches. No new import is needed — `cron` is already imported as `import cron from 'node-cron'` at line 22.

    Addition B — durationMs tracking in executePriceCollection (D-05, D-06, D-07):
    1. Update the return type literal at lines ~74–79 to add `durationMs: number` after `failed: number`. Preserve the existing four fields' order and types.
    2. Add `const startTime = Date.now()` as the first statement inside the function body, BEFORE the `if (isRunning)` guard at line ~81.
    3. The early "already running" return at lines ~83–88 returns `{ total: 0, fetched: 0, skipped: 0, failed: 0, durationMs: 0 }`. Use a literal `0` for durationMs on this path (NOT `Date.now() - startTime`), per Claude's discretion documented in CONTEXT.md and required by the concurrency test's exact-equality assertion.
    4. The no-cards return at lines ~99–106: just before the return, declare `const durationMs = Date.now() - startTime`, then `logger.info(\`Price collection took ${durationMs}ms\`)`, then return `{ total: 0, fetched: 0, skipped: 0, failed: 0, durationMs }`.
    5. The success return at lines ~147–152: just before the return, declare `const durationMs = Date.now() - startTime`, then `logger.info(\`Price collection took ${durationMs}ms\`)`, then return `{ total: cardIds.length, fetched: stats.fetched, skipped: stats.skipped, failed: stats.failed, durationMs }`.
    6. The error catch return at lines ~154–160: just before the return, declare `const durationMs = Date.now() - startTime`, then `logger.info(\`Price collection took ${durationMs}ms\`)`, then return `{ total: 0, fetched: 0, skipped: 0, failed: 1, durationMs }`.

    Each `durationMs` declaration is `const` at each return site (they are in mutually exclusive branches — no `let` declaration needed). Use `Date.now()` (NOT `performance.now()`) for consistency with the existing logger style. Do NOT compute durationMs in `finally` — D-05 requires per-return-path computation so the early "already running" path can return literal 0.

    Do NOT modify scheduleMetagameRefresh, getMonitoredCardIds, the opportunity-detection inner try/catch logic, or any cron.schedule call sites. The existing `finally { isRunning = false }` block stays exactly as-is.

    After the edit, verify `src/scheduler/index.ts` still re-exports cleanly — TypeScript propagates the widened return type automatically through the named re-export at line 7 (per RESEARCH.md). No manual edit to index.ts.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit && grep -c "Invalid cron expression:" src/scheduler/jobs.ts | xargs -I{} test {} -ge 1 && grep -c "durationMs" src/scheduler/jobs.ts | xargs -I{} test {} -ge 5 && grep -c "const startTime = Date.now()" src/scheduler/jobs.ts | xargs -I{} test {} -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm tsc --noEmit` exits 0 (no type errors from the widened return type)
    - `grep -c "Invalid cron expression:" src/scheduler/jobs.ts` returns >= 1 (guard throws with required prefix)
    - `grep -c "cron.validate(" src/scheduler/jobs.ts` returns >= 1 (validate is called at least once)
    - `grep -c "durationMs" src/scheduler/jobs.ts` returns >= 5 (return type field + 4 return paths)
    - `grep -c "const startTime = Date.now()" src/scheduler/jobs.ts` returns exactly 1 (single capture at function entry, not duplicated)
    - `grep -c "durationMs: 0" src/scheduler/jobs.ts` returns >= 1 (literal-zero on isRunning early-return path)
    - `grep -E "logger.info\\(\\\`Price collection took \\\$\\{durationMs\\}ms\\\`\\)" src/scheduler/jobs.ts` returns >= 3 matches (no-cards, success, error paths each log once)
    - `pnpm test:run src/scheduler/__tests__/jobs.test.ts` reports the 5 pre-existing scheduleMetagameRefresh tests still pass (no regression from the validate-mock missing — at this checkpoint validate has not yet been added to the mock; metagame tests do NOT call schedulePriceCollection so they remain green)
    - scheduleMetagameRefresh function body is unchanged (compare against git diff: only schedulePriceCollection and executePriceCollection regions differ)
  </acceptance_criteria>
  <done>
    src/scheduler/jobs.ts compiles under strict TypeScript with the new durationMs field; schedulePriceCollection throws on invalid cron expressions before scheduling; executePriceCollection captures startTime once and includes durationMs in every return path. scheduleMetagameRefresh is untouched.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend test mocks — add validate to node-cron mock and add @/lib/opportunities mock</name>
  <files>src/scheduler/__tests__/jobs.test.ts</files>
  <read_first>
    - src/scheduler/__tests__/jobs.test.ts (full file — must see existing vi.mock declarations at lines 5–43 before extending them; must NOT remove the @/scraper/metagame mock used by scheduleMetagameRefresh tests)
    - .planning/phases/10-scheduler-tests/10-CONTEXT.md (D-08, D-10, D-11 locked decisions on test infrastructure)
    - .planning/phases/10-scheduler-tests/10-PATTERNS.md ("Top-level vi.mock block — extend node-cron mock" + "New top-level vi.mock for @/lib/opportunities" sections)
    - .planning/phases/10-scheduler-tests/10-RESEARCH.md ("Pattern 3: Extend node-cron Mock to Include validate" + "Pitfall 2: Top-level mock for node-cron missing validate key" + "Pitfall 4: opportunities mock missing loadDetectionConfig")
    - src/scheduler/jobs.ts (POST-Task-1 state — verify mocked exports match the three identifiers actually called: detectOpportunitiesForWishlist, sendDigestAndPersist, loadDetectionConfig)
  </read_first>
  <behavior>
    - The top-level `vi.mock('node-cron', ...)` factory exposes a `validate` mock on its `default` object that returns `true` by default
    - A new top-level `vi.mock('@/lib/opportunities', ...)` declaration exists and exposes three mocked exports: detectOpportunitiesForWishlist, sendDigestAndPersist, loadDetectionConfig (all three are required per Pitfall 4 — loadDetectionConfig is called at jobs.ts line ~130)
    - All other vi.mock declarations (node-cron schedule, @/scraper/metagame, @/db, @/scraper/orchestrator, @/lib/logger) remain functionally equivalent; the schedule mock continues to return `{ start: vi.fn(), stop: vi.fn() }`
    - The 5 pre-existing scheduleMetagameRefresh tests still pass after the mock extensions
    - No new it() or test() blocks are added in this task; only the top-level vi.mock declarations change
  </behavior>
  <action>
    Edit the top-of-file vi.mock declarations in src/scheduler/__tests__/jobs.test.ts. Two changes only — do NOT touch any describe/it/test blocks in this task.

    Change A — Extend the node-cron mock factory (currently lines 9–20):
    Inside the existing factory function, declare `const mockValidate = vi.fn().mockReturnValue(true)` alongside the existing `mockSchedule`. Add `validate: mockValidate` to the `default` object so the factory returns `{ default: { schedule: mockSchedule, validate: mockValidate }, schedule: mockSchedule }`. Do NOT add `validate` as a named export on the returned object — only the `default` key needs it because production code calls `cron.validate(...)` via the default import (per RESEARCH.md verified node-cron@4.2.1 export shape).

    Change B — Add a new top-level vi.mock for @/lib/opportunities:
    Insert a new `vi.mock('@/lib/opportunities', () => ({ ... }))` block AFTER the existing `@/lib/logger` mock (after line 43) and BEFORE the `describe('scheduleMetagameRefresh')` block at line 45. The factory returns an object with three keys: `detectOpportunitiesForWishlist: vi.fn().mockResolvedValue([])`, `sendDigestAndPersist: vi.fn().mockResolvedValue({ persisted: 0, sent: false })`, `loadDetectionConfig: vi.fn().mockReturnValue({})`. All three exports are required because executePriceCollection calls all three (Pitfall 4 in RESEARCH.md, jobs.ts line ~130–133).

    Preserve biome import order (imports first, then `vi.mock` calls, then describe blocks). Do NOT add `vi.mock('@/lib/opportunities', ...)` inside any describe/beforeEach — it must be top-level so it is hoisted by Vitest.

    Do NOT change the existing `vi.mock('@/scraper/metagame', ...)`, `vi.mock('@/db', ...)`, `vi.mock('@/scraper/orchestrator', ...)`, or `vi.mock('@/lib/logger', ...)` declarations.
  </action>
  <verify>
    <automated>grep -c "validate: mockValidate" src/scheduler/__tests__/jobs.test.ts | xargs -I{} test {} -eq 1 && grep -c "vi.mock('@/lib/opportunities'" src/scheduler/__tests__/jobs.test.ts | xargs -I{} test {} -eq 1 && grep -c "loadDetectionConfig" src/scheduler/__tests__/jobs.test.ts | xargs -I{} test {} -ge 1 && pnpm test:run src/scheduler/__tests__/jobs.test.ts 2>&1 | grep -E "Tests +5 passed"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "validate: mockValidate" src/scheduler/__tests__/jobs.test.ts` returns exactly 1
    - `grep -c "vi.mock('@/lib/opportunities'" src/scheduler/__tests__/jobs.test.ts` returns exactly 1
    - `grep -c "detectOpportunitiesForWishlist" src/scheduler/__tests__/jobs.test.ts` returns >= 1
    - `grep -c "sendDigestAndPersist" src/scheduler/__tests__/jobs.test.ts` returns >= 1
    - `grep -c "loadDetectionConfig" src/scheduler/__tests__/jobs.test.ts` returns >= 1
    - The 5 scheduleMetagameRefresh tests continue to pass: `pnpm test:run src/scheduler/__tests__/jobs.test.ts` output contains a line matching `Tests.*5 passed` (or `5 passed`) and 16 skipped tests are still skipped — no test counts regressed
    - No test currently active reports a TypeError about `cron.validate` or `loadDetectionConfig`
    - The `vi.mock('@/lib/opportunities', ...)` block sits at file top-level (not inside any describe), so Vitest hoists it
  </acceptance_criteria>
  <done>
    The node-cron mock factory exposes a `validate` mock on `default` returning `true`; a top-level `vi.mock('@/lib/opportunities', ...)` factory exposes `detectOpportunitiesForWishlist`, `sendDigestAndPersist`, and `loadDetectionConfig`. The 5 pre-existing scheduleMetagameRefresh tests remain green; the 16 stubs remain skipped (none accidentally activated).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Activate the 7 test.skip stubs covering schedulePriceCollection and executePriceCollection</name>
  <files>src/scheduler/__tests__/jobs.test.ts</files>
  <read_first>
    - src/scheduler/__tests__/jobs.test.ts (POST-Task-2 state — full file, especially lines 128–197 where the 7 target stubs live; also re-read lines 45–126 for the scheduleMetagameRefresh canonical pattern)
    - src/scheduler/jobs.ts (POST-Task-1 state — confirm exact return type, return literals, and that durationMs literal 0 is on the isRunning early-return path)
    - src/scraper/__tests__/orchestrator.test.ts lines 1–80 (canonical vi.resetModules() + vi.doMock() pattern for the concurrency test)
    - .planning/phases/10-scheduler-tests/10-CONTEXT.md (D-01, D-02, D-04, D-07, D-09, D-11 — defines exact assertions and the in-scope/out-of-scope test boundary)
    - .planning/phases/10-scheduler-tests/10-PATTERNS.md (every snippet under "schedulePriceCollection describe block" and "executePriceCollection describe block" — the executor should mirror these patterns verbatim, adapted to the schedulePriceCollection identifiers)
    - .planning/phases/10-scheduler-tests/10-RESEARCH.md ("Pattern 1: Dynamic Import for Env-Var-Sensitive Tests" + "Pattern 2: vi.resetModules() for Module-Level State Reset" + Pitfalls 2, 3, 4, 5)
  </read_first>
  <behavior>
    - All 3 stubs inside `describe('schedulePriceCollection')` (lines 129–157) are converted from `test.skip(...)` to active `it(...)` blocks with real assertion bodies
    - All 4 stubs inside `describe('executePriceCollection')` (lines 159–198) are converted from `test.skip(...)` to active `it(...)` blocks with real assertion bodies
    - The 4th `executePriceCollection` stub ("should handle concurrent executions") lives inside its OWN nested `describe('concurrent executions')` block that has its own `beforeEach` using `vi.resetModules()` + `vi.doMock()` (per D-09 + Pattern 2). The other 3 executePriceCollection tests share an outer `beforeEach` that uses `vi.clearAllMocks()` only
    - The 9 stubs in `describe('validateScheduleTimes')` (lines 200–227), `describe('stopScheduler')` (lines 229–255), and `describe('Integration scenarios')` (lines 257–294) remain UNCHANGED as `test.skip` (per D-01) — out of scope for this phase
    - Each active test uses dynamic `await import('../jobs')` inside the it() body (NOT a top-level import) so per-test env-var changes are visible to the imported module
    - schedulePriceCollection cron-registration test asserts `cron.schedule` called with `'0 9 * * *'`, `'0 15 * * *'`, `'0 21 * * *'` and `expect.objectContaining({ scheduled: false })` (matches the current jobs.ts implementation that passes `{ scheduled: false }` to all three cron.schedule calls)
    - schedulePriceCollection custom-schedule test sets `process.env.CRON_MORNING = '0 6 * * *'` and asserts the first cron.schedule call used that expression
    - schedulePriceCollection invalid-expression test sets `process.env.CRON_MORNING = 'not-a-cron'`, mocks `cron.validate` to return false for the invalid expression (via `mockReturnValueOnce(false)`), and asserts `expect(() => schedulePriceCollection()).toThrow('Invalid cron expression')`
    - executePriceCollection orchestration test overrides `db.query.cards.findMany` to resolve `[{ oracleId: 'test-id' }]` (per D-11) and asserts `stats.durationMs >= 0` and `stats.fetched >= 0`
    - executePriceCollection error-handling test makes `fetchAllPrices` reject and asserts the returned `stats.failed === 1` and `stats.durationMs >= 0`
    - executePriceCollection duration-metrics test asserts `typeof stats.durationMs === 'number'` and `stats.durationMs >= 0`
    - executePriceCollection concurrency test holds an unresolved fetchAllPrices promise on the first call; the second call returns immediately with `{ total: 0, fetched: 0, skipped: 0, failed: 0, durationMs: 0 }` (exact equality, per D-09)
  </behavior>
  <action>
    Edit src/scheduler/__tests__/jobs.test.ts to replace 7 specific test.skip stubs with active it() bodies. Do NOT touch the scheduleMetagameRefresh describe (lines 45–126), the validateScheduleTimes describe (lines 200–227), the stopScheduler describe (lines 229–255), or the Integration scenarios describe (lines 257–294).

    A. describe('schedulePriceCollection') — outer beforeEach: add a `beforeEach(() => { vi.clearAllMocks(); /* biome-ignore lint/performance/noDelete: process.env requires delete to truly unset a var */ delete process.env.CRON_MORNING; delete process.env.CRON_AFTERNOON; delete process.env.CRON_EVENING; })` at the top of the schedulePriceCollection describe block. Include the biome-ignore comment exactly as written in the scheduleMetagameRefresh describe (line 48).

    B. Replace stub "should configure node-cron for 2-3x daily execution" (currently lines 130–138) with an active `it('should configure node-cron for 2-3x daily execution', async () => { ... })`. Inside the body: `const { schedulePriceCollection } = await import('../jobs')`; `const cronModule = await import('node-cron')`; call `schedulePriceCollection()`; assert `expect(cronModule.default.schedule).toHaveBeenCalledTimes(3)` AND assert that calls 0, 1, 2 used expressions `'0 9 * * *'`, `'0 15 * * *'`, `'0 21 * * *'` respectively (use `expect.objectContaining({ scheduled: false })` for the third argument).

    C. Replace stub "should accept custom schedule times" (lines 140–147) with an active `it('should accept custom schedule times', async () => { ... })`. Inside: set `process.env.CRON_MORNING = '0 6 * * *'`; `const { schedulePriceCollection } = await import('../jobs')`; `const cronModule = await import('node-cron')`; call `schedulePriceCollection()`; assert `expect(cronModule.default.schedule).toHaveBeenCalledWith('0 6 * * *', expect.any(Function), expect.anything())`.

    D. Replace stub "should handle invalid cron expressions" (lines 149–156) with an active `it('should handle invalid cron expressions', async () => { ... })`. Inside: set `process.env.CRON_MORNING = 'not-a-cron'`; `const cronModule = await import('node-cron')`; `vi.mocked(cronModule.default.validate).mockReturnValueOnce(false)`; `const { schedulePriceCollection } = await import('../jobs')`; assert `expect(() => schedulePriceCollection()).toThrow('Invalid cron expression')`.

    E. describe('executePriceCollection') — outer beforeEach: `beforeEach(() => { vi.clearAllMocks() })`. This applies to the first 3 tests below; the concurrency test in step I lives in a nested describe with its own beforeEach.

    F. Replace stub "should run full fetch orchestration" (lines 160–168) with an active `it('should run full fetch orchestration', async () => { ... })`. Inside: `const { db } = await import('@/db')`; `vi.mocked(db.query.cards.findMany).mockResolvedValueOnce([{ oracleId: 'test-id' }])`; `const { executePriceCollection } = await import('../jobs')`; `const stats = await executePriceCollection()`; assert `expect(stats.fetched).toBeGreaterThanOrEqual(0)` AND `expect(stats.durationMs).toBeGreaterThanOrEqual(0)`. The findMany mock signature: the existing top-level mock factory returns `findMany: vi.fn().mockResolvedValue([])` — the per-test override uses `mockResolvedValueOnce` so subsequent tests revert to the empty default.

    G. Replace stub "should handle execution errors gracefully" (lines 170–178) with an active `it('should handle execution errors gracefully', async () => { ... })`. Inside: import the orchestrator default `const fetchAllPricesModule = await import('@/scraper/orchestrator')`; `vi.mocked(fetchAllPricesModule.default).mockRejectedValueOnce(new Error('fetch failed'))`; override findMany to return `[{ oracleId: 'test-id' }]` so the function reaches the fetchAllPrices call; `const { executePriceCollection } = await import('../jobs')`; `const stats = await executePriceCollection()`; assert `expect(stats.failed).toBe(1)` AND `expect(stats.durationMs).toBeGreaterThanOrEqual(0)`.

    H. Replace stub "should record execution duration" (lines 180–188) with an active `it('should record execution duration', async () => { ... })`. Inside: `const { executePriceCollection } = await import('../jobs')`; `const stats = await executePriceCollection()`; assert `expect(typeof stats.durationMs).toBe('number')` AND `expect(stats.durationMs).toBeGreaterThanOrEqual(0)`. This test does NOT override findMany — the empty-cards path still produces a numeric durationMs.

    I. Replace stub "should handle concurrent executions" (lines 190–197) by wrapping it in a NEW nested `describe('concurrent executions', () => { ... })` block placed at the end of describe('executePriceCollection'), with its own beforeEach using `vi.resetModules()` + `vi.doMock()` calls for ALL FOUR mocked dependencies: '@/scraper/orchestrator', '@/db', '@/lib/opportunities', '@/lib/logger' (per D-09 + RESEARCH.md Pattern 2 — vi.mock hoisted declarations are cleared by resetModules and must be re-registered via vi.doMock before the dynamic re-import). The '@/db' doMock factory must include `db: { query: { cards: { findMany: vi.fn().mockResolvedValue([{ oracleId: 'test-id' }]) } } }`. The '@/lib/opportunities' doMock factory must include all three exports: detectOpportunitiesForWishlist, sendDigestAndPersist, loadDetectionConfig (per Pitfall 4). The '@/scraper/orchestrator' doMock factory exposes `default: vi.fn()` and the test body overrides it per-test. The '@/lib/logger' doMock factory exposes `logger: { info: vi.fn(), warn: vi.fn(), error: vi.fn(), debug: vi.fn() }`. Inside the it body: import the orchestrator and hold a never-resolving promise via mockImplementationOnce — e.g. `let resolveFirst!: () => void; const fetchAllPricesModule = await import('@/scraper/orchestrator'); vi.mocked(fetchAllPricesModule.default).mockImplementationOnce(() => new Promise((resolve) => { resolveFirst = () => resolve({ total: 1, fetched: 0, skipped: 0, failed: 0, errors: [] }) }))`; then `const { executePriceCollection } = await import('../jobs')`; start the first call WITHOUT awaiting: `const firstRun = executePriceCollection()`; immediately await the second: `const secondStats = await executePriceCollection()`; assert `expect(secondStats).toEqual({ total: 0, fetched: 0, skipped: 0, failed: 0, durationMs: 0 })` (exact equality per D-09 — this is why Task 1 used literal `durationMs: 0` on the isRunning early return); finally unblock: `resolveFirst(); await firstRun`.

    J. Leave the 9 out-of-scope stubs in describe('validateScheduleTimes') (lines 200–227), describe('stopScheduler') (lines 229–255), and describe('Integration scenarios') (lines 257–294) UNCHANGED as `test.skip` (per D-01). Do not delete them and do not convert them.

    Use `it(...)` (not `test(...)`) for the activated tests to match the convention in the already-passing scheduleMetagameRefresh describe block. Import `test` is still needed at the top of the file because the 9 remaining stubs still use `test.skip` — leave the existing vitest import line unchanged.
  </action>
  <verify>
    <automated>pnpm test:run src/scheduler/__tests__/jobs.test.ts 2>&1 | tee /tmp/phase10-test-out.txt | grep -E "Tests +12 passed" && grep -v '^#' src/scheduler/__tests__/jobs.test.ts | grep -c "test.skip" | xargs -I{} test {} -eq 9 && grep -c "vi.resetModules()" src/scheduler/__tests__/jobs.test.ts | xargs -I{} test {} -ge 1 && grep -c "vi.doMock(" src/scheduler/__tests__/jobs.test.ts | xargs -I{} test {} -ge 4</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test:run src/scheduler/__tests__/jobs.test.ts` exits 0
    - Test run output contains the line `Tests  12 passed | 9 skipped (21)` (or equivalent format: 12 passed, 9 skipped, 21 total) — exactly +7 newly-active tests vs the pre-phase baseline of 5 passed / 16 skipped
    - `grep -v '^#' src/scheduler/__tests__/jobs.test.ts | grep -c "test.skip"` returns exactly 9 (the 9 out-of-scope stubs remain skipped per D-01)
    - `grep -c "vi.resetModules()" src/scheduler/__tests__/jobs.test.ts` returns >= 1 (concurrency test's nested beforeEach uses it)
    - `grep -c "vi.doMock(" src/scheduler/__tests__/jobs.test.ts` returns >= 4 (the four re-registered mocks: orchestrator, db, opportunities, logger)
    - `grep -c "expect(() => schedulePriceCollection()).toThrow('Invalid cron expression')" src/scheduler/__tests__/jobs.test.ts` returns >= 1 (the invalid-expression assertion exists verbatim)
    - `grep -c "toEqual({ total: 0, fetched: 0, skipped: 0, failed: 0, durationMs: 0 })" src/scheduler/__tests__/jobs.test.ts` returns >= 1 (concurrency exact-equality assertion exists verbatim)
    - The 5 pre-existing scheduleMetagameRefresh tests still pass (no regression to lines 45–126)
    - Full project suite: `pnpm test:run` exits 0 — no other test file regresses as a result of the new top-level `@/lib/opportunities` mock (the mock only applies inside this test file's module scope; other test files are unaffected)
    - No biome-ignore comments are missing where `delete process.env.CRON_*` is used in the schedulePriceCollection beforeEach
  </acceptance_criteria>
  <done>
    7 stubs are now active it() blocks and all 7 pass; 9 stubs remain test.skip per D-01; the 5 scheduleMetagameRefresh tests are still green; full project suite (`pnpm test:run`) passes; the concurrency test verifies the isRunning guard by holding a never-resolving fetchAllPrices and asserting exact-equality return shape on the second call.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner ↔ production source | Test file imports from production source via dynamic `await import('../jobs')`. No untrusted input crosses this boundary at runtime; the test file is only executed in dev/CI. |
| node-cron mock ↔ scheduler code | Production code calls `cron.validate(expr)` where `expr` is read from `process.env.CRON_MORNING`/`CRON_AFTERNOON`/`CRON_EVENING`. In production, these env vars come from the deployment config (trusted source). The mock simulates both valid and invalid responses. |

## STRIDE Threat Register

> Phase 10 introduces no new authentication, session management, access control, network I/O, or persistent storage. Threat surface is limited to input validation on cron expression env vars — already handled by `cron.validate()` (library-managed, not hand-rolled).

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering (input) | `schedulePriceCollection` reading `process.env.CRON_*` | mitigate | `cron.validate(expr)` guard rejects malformed cron expressions and throws `Error('Invalid cron expression: ${expr}')` before any `cron.schedule` call; prevents accidental scheduling at unintended times due to typo/misconfiguration. |
| T-10-02 | Denial of Service | `executePriceCollection` concurrency guard | mitigate | Existing module-level `isRunning` flag (verified by the new concurrency test) prevents overlapping price-collection runs from exhausting downstream rate limits. No production-code change needed; test activation verifies the existing guard works. |
| T-10-03 | Information Disclosure | `logger.info` duration messages | accept | Duration log lines contain only millisecond counts (no PII, no card identifiers, no prices). Low-value target; standard backend logging. |
</threat_model>

<verification>
Phase-wide checks (run after all 3 tasks complete):

1. **Type compilation:** `pnpm tsc --noEmit` exits 0 — confirms the widened return type on `executePriceCollection` propagates through `src/scheduler/index.ts` without manual edit.

2. **Scoped test run:** `pnpm test:run src/scheduler/__tests__/jobs.test.ts` reports `Tests  12 passed | 9 skipped (21)` — the 7 newly-active tests pass, the 5 scheduleMetagameRefresh tests stay green, the 9 out-of-scope stubs stay skipped.

3. **Full suite:** `pnpm test:run` exits 0 with no new failures elsewhere — the new top-level `@/lib/opportunities` mock is scoped to the test module and does not leak.

4. **Scope discipline:** `grep -v '^#' src/scheduler/__tests__/jobs.test.ts | grep -c "test.skip"` returns exactly 9 — confirms D-01 boundary (only the 7 in-scope stubs were activated).

5. **Guard behavior:** `grep -c "Invalid cron expression:" src/scheduler/jobs.ts` returns >= 1 and `grep -c "cron.validate(" src/scheduler/jobs.ts` returns >= 1.

6. **Duration tracking integrity:** `grep -c "durationMs" src/scheduler/jobs.ts` returns >= 5 (type field + 4 return paths) and `grep -c "const startTime = Date.now()" src/scheduler/jobs.ts` returns exactly 1.

7. **No regression in scheduleMetagameRefresh:** lines 45–126 of jobs.test.ts are unchanged (git diff shows zero edits to that range).
</verification>

<success_criteria>
- [ ] TEST-10 fully active: 3 schedulePriceCollection tests pass (cron registration, custom schedule override, invalid expression rejection)
- [ ] TEST-11 fully active: 4 executePriceCollection tests pass (orchestration, error handling, duration metrics, concurrency guard)
- [ ] `pnpm test:run src/scheduler/__tests__/jobs.test.ts` reports 12 passed / 9 skipped (was 5 passed / 16 skipped)
- [ ] `pnpm test:run` (full suite) exits 0 with no new failures
- [ ] `pnpm tsc --noEmit` exits 0
- [ ] Production source (`src/scheduler/jobs.ts`) gains exactly two additions: `cron.validate()` guard in schedulePriceCollection + `durationMs` capture in executePriceCollection — no other behavioral change
- [ ] The 9 out-of-scope stubs (`validateScheduleTimes` ×3, `stopScheduler` ×3, `Integration scenarios` ×3) remain `test.skip` (D-01 enforced)
- [ ] No real network calls, no real DB writes, no real Telegram calls execute during scheduler tests (D-08, D-10 mocks block all I/O)
- [ ] All locked decisions D-01 through D-11 are implemented exactly as specified in CONTEXT.md
</success_criteria>

<output>
After completion, create `.planning/phases/10-scheduler-tests/10-01-SUMMARY.md` per the standard summary template. Include:
- Test count delta (before: 5 passed/16 skipped; after: 12 passed/9 skipped)
- Implementation deltas in `src/scheduler/jobs.ts` (lines/locations of the two additions)
- Confirmation that D-01 boundary was respected (which 9 stubs stayed skipped and why)
- Any deviations from the plan (auto-fixes during execution)
</output>
